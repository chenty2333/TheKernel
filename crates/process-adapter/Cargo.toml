[package]
name = "thekernel-linux-process-adapter"
version = "0.1.0"
edition = "2024"
publish = false
description = "TheKernel zombie payload and type aliases over the explicit Linux process domain"
license = "Apache-2.0"

[lints]
workspace = true

[lib]
name = "thekernel_linux_process_adapter"
path = "src/lib.rs"

[dependencies]
thekernel-linux-process.workspace = true

[package.metadata.thekernel]
layer = "integration"
