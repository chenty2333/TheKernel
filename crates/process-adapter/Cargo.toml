[package]
repository.workspace = true
rust-version.workspace = true
name = "tk-linux-process-adapter"
version = "0.1.0"
edition = "2024"
publish = ["crates-io"]
description = "TheKernel zombie payload and type aliases over the explicit Linux process domain"
license = "Apache-2.0"

[lints]
workspace = true

[lib]
name = "tk_linux_process_adapter"
path = "src/lib.rs"

[dependencies]
tk-linux-process.workspace = true

[package.metadata.thekernel]
layer = "integration"
