[package]
publish = ["crates-io"]
name = "tk-axfault"
version = "0.1.0"
edition = "2024"
rust-version.workspace = true
authors = ["TheKernel contributors"]
description = "Bounded generation-safe fault-request broker state"
license = "Apache-2.0"
repository.workspace = true
readme = "README.md"

[lib]
name = "axfault"
path = "src/lib.rs"

[package.metadata.thekernel]
layer = "mechanism"
