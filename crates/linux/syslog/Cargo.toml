[package]
name = "thekernel-linux-syslog"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "Apache-2.0"

[lints.rust]
unsafe_op_in_unsafe_fn = "deny"

[lints.clippy]
undocumented_unsafe_blocks = "deny"

[package.metadata.thekernel]
layer = "linux_abi"
