[package]
repository.workspace = true
rust-version.workspace = true
name = "tk-readiness-adapter"
version = "0.1.0"
edition = "2024"
publish.workspace = true
description = "TheKernel object-readiness adapter over bounded ax and Linux FD contracts"
license = "Apache-2.0"

[lints]
workspace = true

[lib]
name = "axpoll"
path = "src/lib.rs"

[dependencies]
axerrno = "0.2"
axpoll-core = { package = "tk-axpoll", path = "../ax/tk-axpoll", version = "0.1.0" }

[package.metadata.thekernel]
layer = "integration"
