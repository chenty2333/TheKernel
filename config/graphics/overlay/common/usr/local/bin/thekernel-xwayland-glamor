#!/bin/sh
# Xwayland uses glamor by default whenever EGL/DRI3 are available.  Make the
# choice explicit so a DRM desktop cannot silently request the SHM fallback;
# the wrapper still delegates all X server argument parsing upstream.
set -eu
unset XWAYLAND_NO_GLAMOR
exec /usr/bin/Xwayland "$@"
