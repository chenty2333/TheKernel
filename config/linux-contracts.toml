# Reviewed syscall cells only. Missing entries are intentionally unknown.
# The initial 335/336 and 463..469 reviews originated with Linux v6.18.
# Each cell now separates implementation limitations from validation gaps;
# release selection or a test-file binding alone does not establish runtime coverage.
# The terminal routing totals live solely in linux-abi.toml [terminal].
schema = 3
linux_manifest = "linux-abi.toml"

[progress]
reviewed = 385
resolved = 249
implemented = 230
explicit_enosys = 19
fallback = 0
partial = 136
unknown = 0

[[contract]]
id = "linux-explicit-enosys"
flags = ["explicit-none"]
structs = ["explicit-none"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:ENOSYS"]
usercopy = ["explicit-none"]
state = ["explicit-none"]
concurrency = ["explicit-none"]
teardown = ["explicit-none"]

[[contract]]
id = "v723-unimplemented"
flags = ["explicit-none"]
structs = ["explicit-none"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:ENOSYS"]
usercopy = ["explicit-none"]
state = ["explicit-none"]
concurrency = ["explicit-none"]
teardown = ["explicit-none"]

[[contract]]
id = "linux-bpf-routing-partial"
flags = ["flag:bpf-command"]
structs = ["struct:bpf_attr"]
multiplexer_commands = ["mux:BPF_CMD"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:command decode EINVAL then attr size E2BIG or EINVAL then attr copy EFAULT then command-specific validation"]
usercopy = ["usercopy:bpf_attr-read"]
state = ["state:bpf-object-table"]
concurrency = ["concurrency:bpf-subsystem"]
teardown = ["teardown:bpf-object-release"]

[[contract]]
id = "linux-uretprobe"
flags = ["explicit-none"]
structs = ["struct:kernel RX trampoline saved frame {r11,rcx,rax}"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:external or stale trampoline => SIGILL and return -1", "errno:saved-frame fault => SIGILL and return -1"]
usercopy = ["usercopy:nofault pin/read all three saved words before consuming return instances", "usercopy:validate saved rax before an all-pinned trampoline-frame rewrite"]
state = ["state:consume the equal-stack ReturnInstance chain only after provenance and saved-frame validation", "state:restore RIP RSP RAX RCX R11 and publish every retained return hit"]
concurrency = ["concurrency:mm trampoline generation and per-thread return stack are revalidated before commit"]
teardown = ["teardown:exec exit detach and last-consumer retirement invalidate trampoline ownership and stale return instances"]

[[contract]]
id = "linux-uprobe"
flags = ["explicit-none"]
structs = ["struct:kernel optimized-call trampoline saved frame {rax,r11,rcx}"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:external invocation or stale token => ENXIO", "errno:saved-frame fault after live provenance => SIGILL and return -1"]
usercopy = ["usercopy:nofault pin/read the complete saved register frame before consuming the pending token"]
state = ["state:consume exactly one per-thread pending optimized-call token and restore consumer-visible registers"]
concurrency = ["concurrency:pending token binds task mm id XOL generation token and syscall stack under registry validation"]
teardown = ["teardown:fork rebinds only authorized pending state while exec exit and XOL replacement invalidate it"]

[[contract]]
id = "linux-setxattrat"
flags = ["flag:AT_EMPTY_PATH", "flag:AT_SYMLINK_NOFOLLOW", "flag:XATTR_CREATE", "flag:XATTR_REPLACE"]
structs = ["struct:xattr_args v0 size 16 with zero extension tail"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:structure size EINVAL or E2BIG then structure EFAULT then AT and XATTR flags then name/value copy then pathname/fd resolution then permission/provider"]
usercopy = ["usercopy:copy_struct_from_user validates nonzero extension before importing name and at-most-64KiB value"]
state = ["state:provider create replace decision and xattr mutation commit under one inode serialization boundary"]
concurrency = ["concurrency:AT_EMPTY_PATH retains the OFD mount topology and the provider serializes existence with mutation"]
teardown = ["teardown:all copied buffers and OFD authority are request-owned and released without mutation on precommit failure"]

[[contract]]
id = "linux-getxattrat"
flags = ["flag:AT_EMPTY_PATH", "flag:AT_SYMLINK_NOFOLLOW", "flag:xattr_args.flags must be zero"]
structs = ["struct:xattr_args v0 size 16 with zero extension tail"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:structure size and copy then get flags then AT flags then name copy then pathname/fd resolution then permission/provider then ERANGE or output EFAULT"]
usercopy = ["usercopy:snapshot provider value before size probe or exact output copy; size zero copies no bytes"]
state = ["state:read-only snapshot of one named attribute"]
concurrency = ["concurrency:provider holds size-and-value snapshot together while AT_EMPTY_PATH retains OFD authority"]
teardown = ["teardown:snapshot and retained description release after output or fault"]

[[contract]]
id = "linux-listxattrat"
flags = ["flag:AT_EMPTY_PATH", "flag:AT_SYMLINK_NOFOLLOW"]
structs = ["explicit-none"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:AT flags then pathname/fd copy and resolution then permission/provider snapshot then ERANGE or output EFAULT"]
usercopy = ["usercopy:copy one provider snapshot of NUL-terminated names; size zero reports required bytes"]
state = ["state:read-only snapshot of the inode xattr name set"]
concurrency = ["concurrency:provider list snapshot is serialized against xattr mutation"]
teardown = ["teardown:name snapshot and retained description release after output or fault"]

[[contract]]
id = "linux-removexattrat"
flags = ["flag:AT_EMPTY_PATH", "flag:AT_SYMLINK_NOFOLLOW"]
structs = ["explicit-none"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:AT flags then name copy then pathname/fd resolution then permission and provider missing-name decision"]
usercopy = ["usercopy:bounded xattr-name and pathname import completes before provider mutation"]
state = ["state:permission check and named-attribute removal commit against one inode"]
concurrency = ["concurrency:provider serializes missing-name decision with removal"]
teardown = ["teardown:precommit failure leaves attribute state unchanged"]

[[contract]]
id = "linux-open_tree_attr"
flags = ["flag:OPEN_TREE_CLONE", "flag:OPEN_TREE_CLOEXEC", "flag:AT_EMPTY_PATH", "flag:AT_RECURSIVE", "flag:mount_attr set clear propagation idmap domains"]
structs = ["struct:mount_attr v0 size 32 with PAGE_SIZE ceiling and zero extension tail"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:null attr with nonzero size then open_tree preparation then E2BIG or EINVAL attr size then CAP_SYS_ADMIN then usercopy and mount-attribute validation"]
usercopy = ["usercopy:pathname is imported by prepare_open_tree; optional mount_attr is copied only after unpublished tree preparation"]
state = ["state:prepare detached mount FD apply the complete attribute transaction then allocate and publish fd last"]
concurrency = ["concurrency:mount namespace operation serializes topology and recursive attribute/idmap ledger commit"]
teardown = ["teardown:unpublished FsMountFd Drop releases detached topology and provider registrations on every pre-fd failure"]

[[contract]]
id = "linux-file_getattr"
flags = ["flag:AT_EMPTY_PATH", "flag:AT_SYMLINK_NOFOLLOW"]
structs = ["struct:file_attr v0 size 24 with zeroed output extension"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["ioctl:shares native FileAttrProvider snapshot with FS_IOC_FSGETXATTR and FS_IOC_GETFLAGS"]
errno_order = ["errno:AT flags then output-size plan then pathname/fd resolution then permission/provider then output EFAULT"]
usercopy = ["usercopy:copy_struct_to_user writes the provider snapshot and zeroes a larger user extension"]
state = ["state:read-only snapshot of xflags extent hints extent count project id and cow extent hint"]
concurrency = ["concurrency:stable per-object provider returns one coherent fileattr snapshot"]
teardown = ["teardown:retained OFD topology and snapshot release after copyout"]

[[contract]]
id = "linux-file_setattr"
flags = ["flag:AT_EMPTY_PATH", "flag:AT_SYMLINK_NOFOLLOW", "flag:FS_XFLAG supported mask and read-only field filtering"]
structs = ["struct:file_attr v0 size 24 with zero extension tail"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["ioctl:shares FileAttrProvider mutation gate with FS_IOC_FSSETXATTR and FS_IOC_SETFLAGS"]
errno_order = ["errno:AT flags then structure plan and copy then xflag validation then pathname/fd resolution then userns capability permission and provider constraints"]
usercopy = ["usercopy:copy_struct_from_user completes and validates extension bytes before acquiring mutation authority"]
state = ["state:publish xflags project id and allocation hints through one stable per-object update gate"]
concurrency = ["concurrency:exclusive fileattr update excludes every retained data metadata and range mutation guard"]
teardown = ["teardown:failed validation permission or provider commit leaves prior attributes visible"]

# Each entry independently binds the pinned Linux number/name to its handler.
[[cell]]
number = 134
name = "uselib"
status = "explicit-enosys"
contract = "linux-explicit-enosys"
handler = "kernel/src/syscall/dispatch.rs:sys_ni_syscall"
conditional = "explicit-none"
tests = ["tests/guest/portable/native-ni-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 321
name = "bpf"
status = "partial"
contract = "linux-bpf-routing-partial"
handler = "kernel/src/syscall/bpf/mod.rs:sys_bpf"
conditional = "bpf"
tests = []
validation_gaps = ["BPF command families lack a complete Linux 7.2.3 differential matrix"]
limitations = ["BPF map creation rejects inner-map NUMA and BTF spin-lock attributes; map-in-map types are not supported", "program verifier accepts a bounded raw-context and LSM type set; helper support is limited to map lookup/update/delete, ktime, pid/uid/comm and ringbuf", "batch operations support only selected hash array and LPM map types; unsupported map-operation pairs return EOPNOTSUPP"]

[[cell]]
number = 335
name = "uretprobe"
status = "implemented"
contract = "linux-uretprobe"
handler = "kernel/src/syscall/task/uprobe.rs:sys_uretprobe"
conditional = "explicit-none"
tests = []
validation_gaps = ["uretprobe has a source-reviewed contract but no bound Linux 7.2.3 guest behavior test"]
limitations = ["explicit-none"]

[[cell]]
number = 336
name = "uprobe"
status = "implemented"
contract = "linux-uprobe"
handler = "kernel/src/syscall/task/uprobe.rs:sys_uprobe"
conditional = "explicit-none"
tests = []
validation_gaps = ["uprobe has a source-reviewed contract but no bound Linux 7.2.3 guest behavior test"]
limitations = ["explicit-none"]

[[cell]]
number = 463
name = "setxattrat"
status = "implemented"
contract = "linux-setxattrat"
handler = "kernel/src/syscall/fs/xattr.rs:sys_setxattrat"
conditional = "explicit-none"
tests = ["tests/guest/portable/fsattrs-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 464
name = "getxattrat"
status = "implemented"
contract = "linux-getxattrat"
handler = "kernel/src/syscall/fs/xattr.rs:sys_getxattrat"
conditional = "explicit-none"
tests = ["tests/guest/portable/fsattrs-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 465
name = "listxattrat"
status = "implemented"
contract = "linux-listxattrat"
handler = "kernel/src/syscall/fs/xattr.rs:sys_listxattrat"
conditional = "explicit-none"
tests = ["tests/guest/portable/fsattrs-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 466
name = "removexattrat"
status = "implemented"
contract = "linux-removexattrat"
handler = "kernel/src/syscall/fs/xattr.rs:sys_removexattrat"
conditional = "explicit-none"
tests = ["tests/guest/portable/fsattrs-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 467
name = "open_tree_attr"
status = "partial"
contract = "linux-open_tree_attr"
handler = "kernel/src/syscall/fs/mount.rs:sys_open_tree_attr"
conditional = "explicit-none"
tests = ["tests/guest/portable/fsattrs-differential.c:main"]
validation_gaps = ["Current paired clone/provider/error cases pass; OPEN_TREE_NAMESPACE behavior remains unimplemented and unverified"]
limitations = ["OPEN_TREE_NAMESPACE is not accepted by the shared open-tree validator"]

[[cell]]
number = 468
name = "file_getattr"
status = "implemented"
contract = "linux-file_getattr"
handler = "kernel/src/syscall/fs/fileattr.rs:sys_file_getattr"
conditional = "explicit-none"
tests = ["tests/guest/portable/fsattrs-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 469
name = "file_setattr"
status = "implemented"
contract = "linux-file_setattr"
handler = "kernel/src/syscall/fs/fileattr.rs:sys_file_setattr"
conditional = "explicit-none"
tests = ["tests/guest/portable/fsattrs-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 156
name = "_sysctl"
status = "explicit-enosys"
contract = "linux-explicit-enosys"
handler = "kernel/src/syscall/dispatch.rs:sys_ni_syscall"
conditional = "explicit-none"
tests = ["tests/guest/portable/native-ni-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 174
name = "create_module"
status = "explicit-enosys"
contract = "linux-explicit-enosys"
handler = "kernel/src/syscall/dispatch.rs:sys_ni_syscall"
conditional = "explicit-none"
tests = ["tests/guest/portable/native-ni-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 177
name = "get_kernel_syms"
status = "explicit-enosys"
contract = "linux-explicit-enosys"
handler = "kernel/src/syscall/dispatch.rs:sys_ni_syscall"
conditional = "explicit-none"
tests = ["tests/guest/portable/native-ni-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 178
name = "query_module"
status = "explicit-enosys"
contract = "linux-explicit-enosys"
handler = "kernel/src/syscall/dispatch.rs:sys_ni_syscall"
conditional = "explicit-none"
tests = ["tests/guest/portable/native-ni-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 180
name = "nfsservctl"
status = "explicit-enosys"
contract = "linux-explicit-enosys"
handler = "kernel/src/syscall/dispatch.rs:sys_ni_syscall"
conditional = "explicit-none"
tests = ["tests/guest/portable/native-ni-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 181
name = "getpmsg"
status = "explicit-enosys"
contract = "linux-explicit-enosys"
handler = "kernel/src/syscall/dispatch.rs:sys_ni_syscall"
conditional = "explicit-none"
tests = ["tests/guest/portable/native-ni-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 182
name = "putpmsg"
status = "explicit-enosys"
contract = "linux-explicit-enosys"
handler = "kernel/src/syscall/dispatch.rs:sys_ni_syscall"
conditional = "explicit-none"
tests = ["tests/guest/portable/native-ni-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 183
name = "afs_syscall"
status = "explicit-enosys"
contract = "linux-explicit-enosys"
handler = "kernel/src/syscall/dispatch.rs:sys_ni_syscall"
conditional = "explicit-none"
tests = ["tests/guest/portable/native-ni-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 184
name = "tuxcall"
status = "explicit-enosys"
contract = "linux-explicit-enosys"
handler = "kernel/src/syscall/dispatch.rs:sys_ni_syscall"
conditional = "explicit-none"
tests = ["tests/guest/portable/native-ni-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 185
name = "security"
status = "explicit-enosys"
contract = "linux-explicit-enosys"
handler = "kernel/src/syscall/dispatch.rs:sys_ni_syscall"
conditional = "explicit-none"
tests = ["tests/guest/portable/native-ni-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 205
name = "set_thread_area"
status = "explicit-enosys"
contract = "linux-explicit-enosys"
handler = "kernel/src/syscall/dispatch.rs:sys_ni_syscall"
conditional = "explicit-none"
tests = ["tests/guest/portable/native-ni-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 211
name = "get_thread_area"
status = "explicit-enosys"
contract = "linux-explicit-enosys"
handler = "kernel/src/syscall/dispatch.rs:sys_ni_syscall"
conditional = "explicit-none"
tests = ["tests/guest/portable/native-ni-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 212
name = "lookup_dcookie"
status = "explicit-enosys"
contract = "linux-explicit-enosys"
handler = "kernel/src/syscall/dispatch.rs:sys_ni_syscall"
conditional = "explicit-none"
tests = ["tests/guest/portable/native-ni-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 214
name = "epoll_ctl_old"
status = "explicit-enosys"
contract = "linux-explicit-enosys"
handler = "kernel/src/syscall/dispatch.rs:sys_ni_syscall"
conditional = "explicit-none"
tests = ["tests/guest/portable/native-ni-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 215
name = "epoll_wait_old"
status = "explicit-enosys"
contract = "linux-explicit-enosys"
handler = "kernel/src/syscall/dispatch.rs:sys_ni_syscall"
conditional = "explicit-none"
tests = ["tests/guest/portable/native-ni-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 236
name = "vserver"
status = "explicit-enosys"
contract = "linux-explicit-enosys"
handler = "kernel/src/syscall/dispatch.rs:sys_ni_syscall"
conditional = "explicit-none"
tests = ["tests/guest/portable/native-ni-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[cell]]
number = 470
name = "listns"
status = "explicit-enosys"
contract = "v723-unimplemented"
handler = "kernel/src/syscall/dispatch.rs:sys_ni_syscall"
conditional = "explicit-none"
tests = []
validation_gaps = ["Linux 7.2.3 listns rejection has dispatch validation but no guest rejection test"]
limitations = ["explicit-none"]

[[cell]]
number = 471
name = "rseq_slice_yield"
status = "explicit-enosys"
contract = "v723-unimplemented"
handler = "kernel/src/syscall/dispatch.rs:sys_ni_syscall"
conditional = "explicit-none"
tests = []
validation_gaps = ["Linux 7.2.3 rseq_slice_yield rejection has dispatch validation but no guest rejection test"]
limitations = ["explicit-none"]


[[contract]]
id = "linux-time"
flags = ["explicit-none"]
structs = ["struct:x86_64 __kernel_old_time_t signed 64-bit seconds"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:sample realtime seconds then optional tloc copyout EFAULT"]
usercopy = ["usercopy:NULL tloc copies nothing; unaligned eight-byte and cross-page stores use fault-aware usercopy"]
state = ["state:return and optional tloc use one realtime sample"]
concurrency = ["concurrency:realtime sample is read-only and independent of caller address mapping changes"]
teardown = ["teardown:temporary realtime sample has no persistent ownership"]

[[cell]]
number = 201
name = "time"
status = "implemented"
contract = "linux-time"
handler = "kernel/src/syscall/time.rs:sys_time"
conditional = "explicit-none"
tests = ["tests/guest/portable/time-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-umask"
flags = ["flag:mask argument truncated to u32 and restricted to octal 0777"]
structs = ["explicit-none"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:no failure path; return previous fs-context mask"]
usercopy = ["explicit-none"]
state = ["state:replace shared fs-context umask; fork copies CLONE_FS shares unshare separates and exec preserves"]
concurrency = ["concurrency:fs-context lock serializes mask exchange across CLONE_FS tasks"]
teardown = ["teardown:mask lifetime follows retained fs-context ownership"]

[[cell]]
number = 95
name = "umask"
status = "implemented"
contract = "linux-umask"
handler = "kernel/src/syscall/task/ctl.rs:sys_umask"
conditional = "explicit-none"
tests = ["tests/guest/portable/umask-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-eventfd"
flags = ["flag:legacy eventfd uses eventfd2 flags zero"]
structs = ["struct:native-endian u64 eventfd read and write value"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:invalid creation flags EINVAL before descriptor allocation; I/O size/value validation then blocking or EAGAIN"]
usercopy = ["usercopy:eight-byte value import/export; failed value import leaves counter unchanged"]
state = ["state:counter max UINT64_MAX-1; ordinary read consumes counter and semaphore read consumes one; poll reflects counter capacity"]
concurrency = ["concurrency:atomic counter compare-and-update with readiness wakeups after state transition"]
teardown = ["teardown:close releases OFD references; CLOEXEC closes descriptor on exec"]

[[cell]]
number = 284
name = "eventfd"
status = "implemented"
contract = "linux-eventfd"
handler = "kernel/src/syscall/dispatch.rs:compat_eventfd"
conditional = "explicit-none"
tests = ["tests/guest/portable/eventfd-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-eventfd2"
flags = ["flag:EFD_CLOEXEC EFD_NONBLOCK EFD_SEMAPHORE; other bits return EINVAL"]
structs = ["struct:native-endian u64 eventfd read and write value"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:invalid creation flags EINVAL before descriptor allocation; I/O size/value validation then blocking or EAGAIN"]
usercopy = ["usercopy:eight-byte value import/export; failed value import leaves counter unchanged"]
state = ["state:counter max UINT64_MAX-1; ordinary read consumes counter and semaphore read consumes one; poll reflects counter capacity"]
concurrency = ["concurrency:atomic counter compare-and-update with readiness wakeups after state transition"]
teardown = ["teardown:close releases OFD references; CLOEXEC closes descriptor on exec"]

[[cell]]
number = 290
name = "eventfd2"
status = "implemented"
contract = "linux-eventfd2"
handler = "kernel/src/syscall/fs/event.rs:sys_eventfd2"
conditional = "explicit-none"
tests = ["tests/guest/portable/eventfd-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-creat"
flags = ["flag:fixed O_CREAT O_WRONLY O_TRUNC O_LARGEFILE, no O_CLOEXEC"]
structs = ["struct:x86_64 mode_t low permission and special bits"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:bounded pathname import EFAULT before pathname lookup and openat permission/provider errors"]
usercopy = ["usercopy:import NUL-terminated path through existing openat capability path"]
state = ["state:create applies fs umask; existing regular file is truncated without replacing mode"]
concurrency = ["concurrency:shared openat VFS creation and truncation serialization"]
teardown = ["teardown:shared openat failure cleanup; returned fd owns retained OFD until close"]

[[cell]]
number = 85
name = "creat"
status = "implemented"
contract = "linux-creat"
handler = "kernel/src/syscall/fs/fd_ops.rs:sys_creat"
conditional = "explicit-none"
tests = ["tests/guest/portable/creat-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-ioctl"
flags = ["flag:ioctl command and provider-specific flag domains"]
structs = ["struct:FIONBIO signed int; FIEMAP and fileattr native x86_64 layouts"]
multiplexer_commands = ["mux:FIONBIO FIONREAD TIOCINQ and namespace commands before object-provider ioctl dispatch"]
provider_ioctls = ["ioctl:regular files FIEMAP FICLONE FICLONERANGE FIDEDUPERANGE FS_IOC_GETFLAGS FS_IOC_SETFLAGS FS_IOC_FSGETXATTR FS_IOC_FSSETXATTR; FUSE handles FIBMAP"]
errno_order = ["errno:fd lookup EBADF then shared command usercopy and object-specific command validation; unhandled provider commands ENOTTY"]
usercopy = ["usercopy:IoctlContext retains address-space capability for command-specific import and export"]
state = ["state:FIONBIO updates shared OFD status; mutating provider ioctls commit through object-owned operations"]
concurrency = ["concurrency:OFD and provider-specific serialization; FIEMAP snapshots extent state"]
teardown = ["teardown:request-owned usercopy state and retained OFD released on completion or error"]

[[cell]]
number = 16
name = "ioctl"
status = "partial"
contract = "linux-ioctl"
handler = "kernel/src/syscall/fs/ctl.rs:sys_ioctl"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:fionbio_uses_the_complete_int_and_treats_every_nonzero_as_enabled", "kernel/src/file/fiemap.rs:linux_fiemap_layout_matches_x86_64_uapi"]
validation_gaps = ["The complete tty DRM socket filesystem and namespace ioctl command matrix has no combined Linux 7.2.3 differential test"]
limitations = ["regular file provider lacks FIGETBSZ FIFREEZE FITHAW FS_IOC_GETFSUUID and FS_IOC_GETFSSYSFSPATH and returns ENOTTY for those commands"]

[[contract]]
id = "linux-landlock_create_ruleset"
flags = ["flag:ABI7 FS mask0xffff TCP bind/connect mask3 scoped UNIX/signal mask3; restrict-self logging flags"]
structs = ["struct:24-byte ruleset_attr with minimum8-byte prefix and zero extension; packed12-byte path_beneath_attr;16-byte net_port_attr"]
multiplexer_commands = ["mux:create VERSION/ERRATA queries and ruleset allocation; add PATH_BENEATH or NET_PORT; restrict current domain"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:create query validation then null EFAULT size EINVAL/E2BIG mask EINVAL or empty ENOMSG; add fd and rule-specific errors; restrict privilege EPERM before flags/fd"]
usercopy = ["usercopy:copy_struct zero-fills short ruleset suffix and rejects nonzero extension; copy path/port attributes before publication"]
state = ["state:ruleset retains path locations and TCP ports; restrict publishes immutable layered domain"]
concurrency = ["concurrency:ruleset snapshot gate serializes add with restrict; task domain publication retains snapshot"]
teardown = ["teardown:ruleset fd and domain references retain VFS locations until last owner release"]

[[cell]]
number = 444
name = "landlock_create_ruleset"
status = "partial"
contract = "linux-landlock_create_ruleset"
handler = "kernel/src/syscall/landlock.rs:sys_landlock_create_ruleset"
conditional = "explicit-none"
tests = ["kernel/src/syscall/landlock.rs:create_ruleset_errata_query_requires_null_zero_arguments"]
validation_gaps = ["No guest differential test for ruleset enforcement path/network denial or thread-group synchronization"]
limitations = ["returns Landlock ABI7 instead of Linux7.2.3 ABI10; rejects FS_RESOLVE_UNIX and UDP access rights", "ruleset lacks ABI10 quiet_access_fs quiet_access_net quiet_scoped extension fields"]

[[contract]]
id = "linux-landlock_add_rule"
flags = ["flag:ABI7 FS mask0xffff TCP bind/connect mask3 scoped UNIX/signal mask3; restrict-self logging flags"]
structs = ["struct:24-byte ruleset_attr with minimum8-byte prefix and zero extension; packed12-byte path_beneath_attr;16-byte net_port_attr"]
multiplexer_commands = ["mux:create VERSION/ERRATA queries and ruleset allocation; add PATH_BENEATH or NET_PORT; restrict current domain"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:create query validation then null EFAULT size EINVAL/E2BIG mask EINVAL or empty ENOMSG; add fd and rule-specific errors; restrict privilege EPERM before flags/fd"]
usercopy = ["usercopy:copy_struct zero-fills short ruleset suffix and rejects nonzero extension; copy path/port attributes before publication"]
state = ["state:ruleset retains path locations and TCP ports; restrict publishes immutable layered domain"]
concurrency = ["concurrency:ruleset snapshot gate serializes add with restrict; task domain publication retains snapshot"]
teardown = ["teardown:ruleset fd and domain references retain VFS locations until last owner release"]

[[cell]]
number = 445
name = "landlock_add_rule"
status = "partial"
contract = "linux-landlock_add_rule"
handler = "kernel/src/syscall/landlock.rs:sys_landlock_add_rule"
conditional = "explicit-none"
tests = ["kernel/src/syscall/landlock.rs:masks_exclude_unknown_bits"]
validation_gaps = ["No guest differential test for ruleset enforcement path/network denial or thread-group synchronization"]
limitations = ["rejects LANDLOCK_ADD_RULE_QUIET and UDP/FS_RESOLVE_UNIX rules supported by Linux7.2.3"]

[[contract]]
id = "linux-landlock_restrict_self"
flags = ["flag:ABI7 FS mask0xffff TCP bind/connect mask3 scoped UNIX/signal mask3; restrict-self logging flags"]
structs = ["struct:24-byte ruleset_attr with minimum8-byte prefix and zero extension; packed12-byte path_beneath_attr;16-byte net_port_attr"]
multiplexer_commands = ["mux:create VERSION/ERRATA queries and ruleset allocation; add PATH_BENEATH or NET_PORT; restrict current domain"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:create query validation then null EFAULT size EINVAL/E2BIG mask EINVAL or empty ENOMSG; add fd and rule-specific errors; restrict privilege EPERM before flags/fd"]
usercopy = ["usercopy:copy_struct zero-fills short ruleset suffix and rejects nonzero extension; copy path/port attributes before publication"]
state = ["state:ruleset retains path locations and TCP ports; restrict publishes immutable layered domain"]
concurrency = ["concurrency:ruleset snapshot gate serializes add with restrict; task domain publication retains snapshot"]
teardown = ["teardown:ruleset fd and domain references retain VFS locations until last owner release"]

[[cell]]
number = 446
name = "landlock_restrict_self"
status = "partial"
contract = "linux-landlock_restrict_self"
handler = "kernel/src/syscall/landlock.rs:sys_landlock_restrict_self"
conditional = "explicit-none"
tests = ["kernel/src/syscall/landlock.rs:abi_shapes_are_linux_612"]
validation_gaps = ["No guest differential test for ruleset enforcement path/network denial or thread-group synchronization"]
limitations = ["rejects LANDLOCK_RESTRICT_SELF_TSYNC and cannot atomically synchronize domain across the thread group"]

[[contract]]
id = "linux-memfd_create"
structs = ["struct:NUL-terminated memfd name at most249 bytes"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:creation flags validation before object and descriptor publication"]
usercopy = ["usercopy:bounded name import before object publication"]
state = ["state:memory-backed file supports sealing with write-admission serialization"]
concurrency = ["concurrency:descriptor reservation precedes publication and object lifetime is reference counted"]
teardown = ["teardown:failed creation releases unpublished resources; last OFD reference releases object"]
flags = ["flag:MFD_CLOEXEC MFD_ALLOW_SEALING"]

[[cell]]
number = 319
name = "memfd_create"
status = "partial"
contract = "linux-memfd_create"
handler = "kernel/src/syscall/fs/memfd.rs:sys_memfd_create"
conditional = "explicit-none"
tests = ["kernel/src/file/memfd.rs:writable_registration_is_idempotent_and_blocks_seal_write"]
validation_gaps = ["No complete Linux7.2.3 guest differential covers creation flags mapping lifetime and teardown"]
limitations = ["MFD_HUGETLB MFD_NOEXEC_SEAL and MFD_EXEC are rejected with EINVAL although Linux7.2.3 supports them"]

[[contract]]
id = "linux-userfaultfd"
structs = ["explicit-none"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["ioctl:userfault file provides bounded UFFD API registration and page resolution"]
errno_order = ["errno:missing USER_MODE_ONLY EPERM before unrecognized flags EINVAL and descriptor allocation"]
usercopy = ["explicit-none"]
state = ["state:publish one anonymous open-file description only after construction succeeds"]
concurrency = ["concurrency:descriptor reservation precedes publication and object lifetime is reference counted"]
teardown = ["teardown:failed creation releases unpublished resources; last OFD reference releases object"]
flags = ["flag:requires UFFD_USER_MODE_ONLY; optional O_CLOEXEC O_NONBLOCK"]

[[cell]]
number = 323
name = "userfaultfd"
status = "partial"
contract = "linux-userfaultfd"
handler = "kernel/src/syscall/fs/userfaultfd.rs:sys_userfaultfd"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/userfaultfd.rs:profile_gate_distinguishes_permission_from_unknown_bits", "tests/guest/tools/userfaultfd-smoke.c:main"]
validation_gaps = ["No complete Linux7.2.3 guest differential covers creation flags mapping lifetime and teardown"]
limitations = ["privileged creation without UFFD_USER_MODE_ONLY is not supported; local profile always returns EPERM"]

[[contract]]
id = "linux-memfd_secret"
structs = ["explicit-none"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:creation flags validation before object and descriptor publication"]
usercopy = ["explicit-none"]
state = ["state:secret file exposes O_RDWR O_LARGEFILE; backing pages are owned by the secret file and fixed after initial nonzero sizing"]
concurrency = ["concurrency:descriptor reservation precedes publication and object lifetime is reference counted"]
teardown = ["teardown:failed creation releases unpublished resources; last OFD reference releases object"]
flags = ["flag:O_CLOEXEC only; MFD_CLOEXEC value is not accepted"]

[[cell]]
number = 447
name = "memfd_secret"
status = "implemented"
contract = "linux-memfd_secret"
handler = "kernel/src/syscall/fs/secretmem.rs:sys_memfd_secret"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/secretmem.rs:secretmem_uses_open_cloexec_not_memfd_cloexec", "kernel/src/file/secretmem.rs:zero_length_secret_mmap_reuses_backing_for_first_truncate", "kernel/src/file/secretmem.rs:first_nonzero_size_freezes_later_zero_resize"]
validation_gaps = ["No complete Linux7.2.3 guest differential covers creation flags mapping lifetime and teardown"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-seccomp"
flags = ["flag:TSYNC TSYNC_ESRCH NEW_LISTENER LOG SPEC_ALLOW WAIT_KILLABLE_RECV with combination checks"]
structs = ["struct:16-byte native sock_fprog; seccomp_data64; notification sizes80/24/64"]
multiplexer_commands = ["mux:SET_MODE_STRICT SET_MODE_FILTER GET_ACTION_AVAIL GET_NOTIF_SIZES"]
provider_ioctls = ["ioctl:notification listener RECV SEND ID_VALID ADDFD SET_FLAGS"]
errno_order = ["errno:operation and flags validation then usercopy EFAULT and filter admission EINVAL/EACCES; unavailable action EOPNOTSUPP"]
usercopy = ["usercopy:unaligned fprog/action imports and bounded filter instruction copy before publication"]
state = ["state:strict allowlist or immutable filter chain; thread-group synchronization and listener ownership"]
concurrency = ["concurrency:TSYNC commits under filter-publication transaction; notifications retain exact task identity"]
teardown = ["teardown:filter and listener references survive sharing; task exit invalidates pending notification identity"]

[[cell]]
number = 317
name = "seccomp"
status = "partial"
contract = "linux-seccomp"
handler = "kernel/src/syscall/seccomp.rs:sys_seccomp"
conditional = "explicit-none"
tests = ["kernel/src/syscall/seccomp.rs:strict_allowlist_is_exact", "tests/guest/portable/seccomp-smoke.c:main"]
validation_gaps = ["The full action notification and ptrace interaction matrix lacks a Linux7.2.3 differential run"]
limitations = ["SECCOMP_RET_TRACE has no ptrace ownership lifecycle: execution always returns ENOSYS and GET_ACTION_AVAIL deliberately returns EOPNOTSUPP instead of Linux7.2.3 support"]

[[contract]]
id = "linux-mmap"
flags = ["flag:PRIVATE SHARED SHARED_VALIDATE FIXED FIXED_NOREPLACE ANONYMOUS POPULATE LOCKED STACK MAP_32BIT and huge-page sizes"]
structs = ["explicit-none"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:mapping geometry/flags/offset then VFS permissions and VM admission before publication"]
usercopy = ["usercopy:validate user virtual range; population faults backing through VM policy"]
state = ["state:insert VMA and mapping ownership; fixed replacement commits through address-space mutation"]
concurrency = ["concurrency:address-space locking and mapping generation protect replacement"]
teardown = ["teardown:failed preparation releases unpublished backing and leaves prior VMA state"]

[[cell]]
number = 9
name = "mmap"
status = "partial"
contract = "linux-mmap"
handler = "kernel/src/syscall/mm/mmap.rs:sys_mmap"
conditional = "explicit-none"
tests = ["kernel/src/syscall/mm/mmap.rs:mmap_hooks_run_file_then_address_and_stop_on_denial"]
validation_gaps = ["No complete Linux7.2.3 differential covers the declared flag and concurrency matrix"]
limitations = ["MAP_NORESERVE is parsed but anonymous writable mappings still use the same overcommit reservation admission"]

[[contract]]
id = "linux-madvise"
flags = ["flag:MADV_NORMAL RANDOM SEQUENTIAL WILLNEED DONTNEED FREE REMOVE HUGEPAGE NOHUGEPAGE COLLAPSE COLD PAGEOUT and dump/fork policy"]
structs = ["explicit-none"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:mapping geometry/flags/offset then VFS permissions and VM admission before publication"]
usercopy = ["usercopy:validate user virtual range; population faults backing through VM policy"]
state = ["state:apply range-specific VMA policy, invalidation or file-backed eviction work"]
concurrency = ["concurrency:address-space locking and mapping generation protect replacement"]
teardown = ["teardown:failed preparation releases unpublished backing and leaves prior VMA state"]

[[cell]]
number = 28
name = "madvise"
status = "partial"
contract = "linux-madvise"
handler = "kernel/src/syscall/mm/mmap.rs:sys_madvise"
conditional = "explicit-none"
tests = ["kernel/src/syscall/mm/mmap.rs:mseal_madvise_discard_set_matches_linux_612"]
validation_gaps = ["No complete Linux7.2.3 differential covers the declared flag and concurrency matrix"]
limitations = ["MADV_MERGEABLE and MADV_UNMERGEABLE only validate range and succeed without KSM state", "MADV_SOFT_OFFLINE discards instead of migrating pages; anonymous COLD/PAGEOUT lack swap reclaim"]

[[contract]]
id = "linux-select"
flags = ["flag:nfds and per-descriptor read/write/exception bitmaps"]
structs = ["struct:native fd_set; select timeval; pselect6 timespec and sigmask pointer/size pair"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:fd-set and timeout usercopy before readiness wait; invalid descriptors EBADF"]
usercopy = ["usercopy:request-owned fd bitmap imports and readiness bitmap copyout"]
state = ["state:wait for requested OFD readiness; pselect temporarily replaces blocked signal mask"]
concurrency = ["concurrency:retained OFDs survive fd-table changes; readiness poll registration closes wake race"]
teardown = ["teardown:drop poll registrations and restore temporary signal mask on completion"]

[[cell]]
number = 23
name = "select"
status = "implemented"
contract = "linux-select"
handler = "kernel/src/syscall/io_mpx/select.rs:sys_select"
conditional = "explicit-none"
tests = ["kernel/src/syscall/io_mpx/select.rs:opath_select_keeps_all_requested_classes_ready", "tests/guest/portable/select-smoke.c:main"]
validation_gaps = ["select-smoke passed host Linux and TheKernel KVM; matched Linux 7.2.3 oracle execution remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-pselect6"
flags = ["flag:nfds and per-descriptor read/write/exception bitmaps"]
structs = ["struct:native fd_set; select timeval; pselect6 timespec and sigmask pointer/size pair"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:fd-set and timeout usercopy before readiness wait; invalid descriptors EBADF"]
usercopy = ["usercopy:request-owned fd bitmap imports and readiness bitmap copyout"]
state = ["state:wait for requested OFD readiness; pselect temporarily replaces blocked signal mask"]
concurrency = ["concurrency:retained OFDs survive fd-table changes; readiness poll registration closes wake race"]
teardown = ["teardown:drop poll registrations and restore temporary signal mask on completion"]

[[cell]]
number = 270
name = "pselect6"
status = "implemented"
contract = "linux-pselect6"
handler = "kernel/src/syscall/io_mpx/select.rs:sys_pselect6"
conditional = "explicit-none"
tests = ["kernel/src/syscall/io_mpx/select.rs:opath_select_keeps_all_requested_classes_ready", "tests/guest/portable/select-smoke.c:main"]
validation_gaps = ["select-smoke passed host Linux and TheKernel KVM; matched Linux 7.2.3 oracle execution remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-io_uring_setup"
flags = ["flag:CQSIZE CLAMP NO_SQARRAY SQPOLL SQ_AFF IOPOLL DEFER_TASKRUN COOP_TASKRUN SINGLE_ISSUER R_DISABLED"]
structs = ["struct:native io_uring_params;64-byte SQE;16-byte CQE;registered buffer iovecs and file arrays"]
multiplexer_commands = ["mux:setup ring; enter submission/wait; register buffers/files/eventfd/probe/ring-fds/memory-region"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:malformed setup and flags EINVAL; usercopy EFAULT; admitted SQ prefix survives later wait error; supported-but-unimplemented operations EOPNOTSUPP"]
usercopy = ["usercopy:fixed resources retain registration address-space ownership; SQE batch and registration arrays imported before publication"]
state = ["state:SQ admission reserves CQ credits; request lifecycle retains ring and fixed-resource generations"]
concurrency = ["concurrency:request completion and cancellation consume exactly one terminal credit; registration retirement preserves admitted ownership"]
teardown = ["teardown:ring close drains outstanding work and retired resource references before release"]

[[cell]]
number = 425
name = "io_uring_setup"
status = "partial"
contract = "linux-io_uring_setup"
handler = "kernel/src/syscall/fs/io_uring.rs:sys_io_uring_setup"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/io_uring.rs:policy_failures_keep_malformed_and_buffer_range_distinct", "tests/guest/tools/io-uring-smoke.c:main"]
validation_gaps = ["The full setup/enter/registration opcode matrix lacks Linux7.2.3 differential coverage"]
limitations = ["setup rejects Linux SQE128 CQE32 ATTACH_WQ NO_MMAP and other flags outside the declared subset with EINVAL"]

[[contract]]
id = "linux-io_uring_enter"
flags = ["flag:CQSIZE CLAMP NO_SQARRAY SQPOLL SQ_AFF IOPOLL DEFER_TASKRUN COOP_TASKRUN SINGLE_ISSUER R_DISABLED"]
structs = ["struct:native io_uring_params;64-byte SQE;16-byte CQE;registered buffer iovecs and file arrays"]
multiplexer_commands = ["mux:setup ring; enter submission/wait; register buffers/files/eventfd/probe/ring-fds/memory-region"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:malformed setup and flags EINVAL; usercopy EFAULT; admitted SQ prefix survives later wait error; supported-but-unimplemented operations EOPNOTSUPP"]
usercopy = ["usercopy:fixed resources retain registration address-space ownership; SQE batch and registration arrays imported before publication"]
state = ["state:SQ admission reserves CQ credits; request lifecycle retains ring and fixed-resource generations"]
concurrency = ["concurrency:request completion and cancellation consume exactly one terminal credit; registration retirement preserves admitted ownership"]
teardown = ["teardown:ring close drains outstanding work and retired resource references before release"]

[[cell]]
number = 426
name = "io_uring_enter"
status = "partial"
contract = "linux-io_uring_enter"
handler = "kernel/src/syscall/fs/io_uring.rs:sys_io_uring_enter"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/io_uring.rs:fixed_submission_keeps_registration_address_space", "tests/guest/tools/io-uring-smoke.c:main"]
validation_gaps = ["The full setup/enter/registration opcode matrix lacks Linux7.2.3 differential coverage"]
limitations = ["SQE decoder omits SENDMSG RECVMSG CONNECT OPENAT STATX SPLICE and other valid Linux7.2.3 opcodes; unsupported SQEs complete with EOPNOTSUPP"]

[[contract]]
id = "linux-io_uring_register"
flags = ["flag:CQSIZE CLAMP NO_SQARRAY SQPOLL SQ_AFF IOPOLL DEFER_TASKRUN COOP_TASKRUN SINGLE_ISSUER R_DISABLED"]
structs = ["struct:native io_uring_params;64-byte SQE;16-byte CQE;registered buffer iovecs and file arrays"]
multiplexer_commands = ["mux:setup ring; enter submission/wait; register buffers/files/eventfd/probe/ring-fds/memory-region"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:malformed setup and flags EINVAL; usercopy EFAULT; admitted SQ prefix survives later wait error; supported-but-unimplemented operations EOPNOTSUPP"]
usercopy = ["usercopy:fixed resources retain registration address-space ownership; SQE batch and registration arrays imported before publication"]
state = ["state:SQ admission reserves CQ credits; request lifecycle retains ring and fixed-resource generations"]
concurrency = ["concurrency:request completion and cancellation consume exactly one terminal credit; registration retirement preserves admitted ownership"]
teardown = ["teardown:ring close drains outstanding work and retired resource references before release"]

[[cell]]
number = 427
name = "io_uring_register"
status = "partial"
contract = "linux-io_uring_register"
handler = "kernel/src/syscall/fs/io_uring.rs:sys_io_uring_register"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/io_uring.rs:registered_buffer_count_keeps_null_usercopy_precedence_bounded", "tests/guest/tools/io-uring-smoke.c:main"]
validation_gaps = ["The full setup/enter/registration opcode matrix lacks Linux7.2.3 differential coverage"]
limitations = ["registration decodes only opcodes0..5,8,12,20,21,34; other opcodes below36 return EOPNOTSUPP; Linux7.2.3 ZCRX_CTRL36 and BPF_FILTER37 are treated as invalid and return EINVAL"]

[[contract]]
id = "linux-futex"
flags = ["flag:private/shared u32 futex keys and bitset masks; selected realtime deadlines"]
structs = ["struct:u32 futex word; native timespec; futex_waitv value/address/flags/reserved descriptors"]
multiplexer_commands = ["mux:WAIT WAIT_BITSET WAKE WAKE_BITSET REQUEUE CMP_REQUEUE and futex2 wait/wake/requeue"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:flag/value/alignment validation before user-word snapshot and wait-queue mutation"]
usercopy = ["usercopy:caller-owned user memory capability; waitv descriptor snapshot before queue admission"]
state = ["state:queue waiters against private mm or shared backing identity; wake/requeue claims waiter once"]
concurrency = ["concurrency:queue locking and value recheck prevent lost wake; signal/timeout completion competes for waiter ownership"]
teardown = ["teardown:timeout signal exit and invalidation unlink waiter and release retained backing"]

[[cell]]
number = 202
name = "futex"
status = "partial"
contract = "linux-futex"
handler = "kernel/src/syscall/sync/futex.rs:sys_futex"
conditional = "explicit-none"
tests = ["tests/guest/portable/futex-smoke.c:main"]
validation_gaps = ["No Linux7.2.3 differential covers the priority-inheritance or NUMA command matrix"]
limitations = ["priority-inheritance LOCK_PI UNLOCK_PI TRYLOCK_PI WAIT_REQUEUE_PI and CMP_REQUEUE_PI operations are not implemented and return ENOSYS"]

[[contract]]
id = "linux-futex_waitv"
flags = ["flag:private/shared u32 futex keys and bitset masks; selected realtime deadlines"]
structs = ["struct:u32 futex word; native timespec; futex_waitv value/address/flags/reserved descriptors"]
multiplexer_commands = ["mux:WAIT WAIT_BITSET WAKE WAKE_BITSET REQUEUE CMP_REQUEUE and futex2 wait/wake/requeue"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:flag/value/alignment validation before user-word snapshot and wait-queue mutation"]
usercopy = ["usercopy:caller-owned user memory capability; waitv descriptor snapshot before queue admission"]
state = ["state:queue waiters against private mm or shared backing identity; wake/requeue claims waiter once"]
concurrency = ["concurrency:queue locking and value recheck prevent lost wake; signal/timeout completion competes for waiter ownership"]
teardown = ["teardown:timeout signal exit and invalidation unlink waiter and release retained backing"]

[[cell]]
number = 449
name = "futex_waitv"
status = "partial"
contract = "linux-futex_waitv"
handler = "kernel/src/syscall/sync/futex.rs:sys_futex_waitv"
conditional = "explicit-none"
tests = ["kernel/src/syscall/sync/futex.rs:futex2_only_accepts_private_or_shared_u32_words"]
validation_gaps = ["No Linux7.2.3 differential covers the priority-inheritance or NUMA command matrix"]
limitations = ["FUTEX2_NUMA and FUTEX2_MPOL are rejected EINVAL instead of Linux7.2.3 NUMA-aware key placement"]

[[contract]]
id = "linux-futex_wake"
flags = ["flag:private/shared u32 futex keys and bitset masks; selected realtime deadlines"]
structs = ["struct:u32 futex word; native timespec; futex_waitv value/address/flags/reserved descriptors"]
multiplexer_commands = ["mux:WAIT WAIT_BITSET WAKE WAKE_BITSET REQUEUE CMP_REQUEUE and futex2 wait/wake/requeue"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:flag/value/alignment validation before user-word snapshot and wait-queue mutation"]
usercopy = ["usercopy:caller-owned user memory capability; waitv descriptor snapshot before queue admission"]
state = ["state:queue waiters against private mm or shared backing identity; wake/requeue claims waiter once"]
concurrency = ["concurrency:queue locking and value recheck prevent lost wake; signal/timeout completion competes for waiter ownership"]
teardown = ["teardown:timeout signal exit and invalidation unlink waiter and release retained backing"]

[[cell]]
number = 454
name = "futex_wake"
status = "partial"
contract = "linux-futex_wake"
handler = "kernel/src/syscall/sync/futex.rs:sys_futex_wake"
conditional = "explicit-none"
tests = ["kernel/src/syscall/sync/futex.rs:futex2_only_accepts_private_or_shared_u32_words"]
validation_gaps = ["No Linux7.2.3 differential covers the priority-inheritance or NUMA command matrix"]
limitations = ["FUTEX2_NUMA and FUTEX2_MPOL are rejected EINVAL instead of Linux7.2.3 NUMA-aware key placement"]

[[contract]]
id = "linux-futex_wait"
flags = ["flag:private/shared u32 futex keys and bitset masks; selected realtime deadlines"]
structs = ["struct:u32 futex word; native timespec; futex_waitv value/address/flags/reserved descriptors"]
multiplexer_commands = ["mux:WAIT WAIT_BITSET WAKE WAKE_BITSET REQUEUE CMP_REQUEUE and futex2 wait/wake/requeue"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:flag/value/alignment validation before user-word snapshot and wait-queue mutation"]
usercopy = ["usercopy:caller-owned user memory capability; waitv descriptor snapshot before queue admission"]
state = ["state:queue waiters against private mm or shared backing identity; wake/requeue claims waiter once"]
concurrency = ["concurrency:queue locking and value recheck prevent lost wake; signal/timeout completion competes for waiter ownership"]
teardown = ["teardown:timeout signal exit and invalidation unlink waiter and release retained backing"]

[[cell]]
number = 455
name = "futex_wait"
status = "partial"
contract = "linux-futex_wait"
handler = "kernel/src/syscall/sync/futex.rs:sys_futex_wait"
conditional = "explicit-none"
tests = ["kernel/src/syscall/sync/futex.rs:futex2_only_accepts_private_or_shared_u32_words"]
validation_gaps = ["No Linux7.2.3 differential covers the priority-inheritance or NUMA command matrix"]
limitations = ["FUTEX2_NUMA and FUTEX2_MPOL are rejected EINVAL instead of Linux7.2.3 NUMA-aware key placement"]

[[contract]]
id = "linux-futex_requeue"
flags = ["flag:private/shared u32 futex keys and bitset masks; selected realtime deadlines"]
structs = ["struct:u32 futex word; native timespec; futex_waitv value/address/flags/reserved descriptors"]
multiplexer_commands = ["mux:WAIT WAIT_BITSET WAKE WAKE_BITSET REQUEUE CMP_REQUEUE and futex2 wait/wake/requeue"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:flag/value/alignment validation before user-word snapshot and wait-queue mutation"]
usercopy = ["usercopy:caller-owned user memory capability; waitv descriptor snapshot before queue admission"]
state = ["state:queue waiters against private mm or shared backing identity; wake/requeue claims waiter once"]
concurrency = ["concurrency:queue locking and value recheck prevent lost wake; signal/timeout completion competes for waiter ownership"]
teardown = ["teardown:timeout signal exit and invalidation unlink waiter and release retained backing"]

[[cell]]
number = 456
name = "futex_requeue"
status = "partial"
contract = "linux-futex_requeue"
handler = "kernel/src/syscall/sync/futex.rs:sys_futex_requeue"
conditional = "explicit-none"
tests = ["kernel/src/syscall/sync/futex.rs:futex2_only_accepts_private_or_shared_u32_words"]
validation_gaps = ["No Linux7.2.3 differential covers the priority-inheritance or NUMA command matrix"]
limitations = ["FUTEX2_NUMA and FUTEX2_MPOL are rejected EINVAL instead of Linux7.2.3 NUMA-aware key placement"]

[[contract]]
id = "linux-membarrier"
flags = ["flag:zero flags; cpu_id ignored for supported commands"]
structs = ["explicit-none"]
multiplexer_commands = ["mux:QUERY PRIVATE_EXPEDITED REGISTER_PRIVATE_EXPEDITED PRIVATE_EXPEDITED_SYNC_CORE REGISTER_PRIVATE_EXPEDITED_SYNC_CORE GET_REGISTRATIONS"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:unsupported command/flags EINVAL; unregistered private command EPERM; failed remote rendezvous EAGAIN"]
usercopy = ["explicit-none"]
state = ["state:per-mm registration and private memory or instruction-stream ordering"]
concurrency = ["concurrency:private expedited CPU rendezvous and completion acknowledgement"]
teardown = ["teardown:temporary rendezvous references released after acknowledgement or error"]

[[cell]]
number = 324
name = "membarrier"
status = "partial"
contract = "linux-membarrier"
handler = "kernel/src/syscall/sync/membarrier.rs:sys_membarrier"
conditional = "explicit-none"
tests = ["tests/guest/tools/membarrier-smoke.c:main"]
validation_gaps = ["No Linux7.2.3 differential covers the complete command and CPU-target matrix"]
limitations = ["GLOBAL GLOBAL_EXPEDITED and RSEQ expedited commands and FLAG_CPU are not supported and return EINVAL"]

[[contract]]
id = "linux-epoll_ctl"
flags = ["flag:EPOLLET EPOLLONESHOT; IN PRI OUT RDNORM RDBAND WRNORM WRBAND RDHUP readiness"]
structs = ["struct:packed native12-byte epoll_event containing events and u64 user data"]
multiplexer_commands = ["mux:EPOLL_CTL_ADD EPOLL_CTL_MOD EPOLL_CTL_DEL"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:epfd and target lookup then self/type/O_PATH checks before command and event validation"]
usercopy = ["usercopy:ADD/MOD import event; DEL does not dereference event pointer"]
state = ["state:watch keys retain OFD identity across dup close and numeric-fd reuse; nested watches track topology"]
concurrency = ["concurrency:watch publication and readiness delivery are serialized; ONESHOT disables until MOD rearm"]
teardown = ["teardown:DEL and final OFD close retire watch and release poll registrations"]

[[cell]]
number = 233
name = "epoll_ctl"
status = "partial"
contract = "linux-epoll_ctl"
handler = "kernel/src/syscall/io_mpx/epoll.rs:sys_epoll_ctl"
conditional = "explicit-none"
tests = ["tests/guest/portable/epoll-smoke.c:main"]
validation_gaps = ["Existing guest smoke uses TheKernel-specific rejection expectations for exclusive watches"]
limitations = ["EPOLLEXCLUSIVE is rejected EINVAL and exclusive wake distribution is absent; EPOLLMSG is rejected EOPNOTSUPP"]

[[contract]]
id = "linux-brk"
flags = ["explicit-none"]
structs = ["explicit-none"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:invalid or unadmitted heap endpoint returns unchanged break rather than negative errno"]
usercopy = ["explicit-none"]
state = ["state:grow or shrink heap VMA and publish break after address-space transaction"]
concurrency = ["concurrency:heap transaction serializes VMA changes with layout publication"]
teardown = ["teardown:shrink releases removed backing and deferred fault wakeups"]

[[cell]]
number = 12
name = "brk"
status = "partial"
contract = "linux-brk"
handler = "kernel/src/syscall/mm/brk.rs:sys_brk"
conditional = "explicit-none"
tests = []
validation_gaps = ["No direct guest brk growth shrink resource-limit and failure-return test"]
limitations = ["heap growth is capped at heap_base+512MiB by USER_HEAP_SIZE_MAX independently of Linux RLIMIT and available virtual address space"]

[[contract]]
id = "linux-mprotect"
flags = ["flag:PROT_READ WRITE EXEC SEM GROWSDOWN GROWSUP and READ_IMPLIES_EXEC personality"]
structs = ["explicit-none"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate address geometry and access permissions before applying address-space changes"]
usercopy = ["explicit-none"]
state = ["state:walk VMAs applying authorized page protections in order; earlier successful VMA changes survive later failure"]
concurrency = ["concurrency:address-space mutation is serialized and retains mapping ownership"]
teardown = ["teardown:request references and deferred backing release complete after mutation"]

[[cell]]
number = 10
name = "mprotect"
status = "implemented"
contract = "linux-mprotect"
handler = "kernel/src/syscall/mm/mmap.rs:sys_mprotect"
conditional = "explicit-none"
tests = ["kernel/src/syscall/mm/mmap.rs:mprotect_commits_each_authorized_vma_before_later_failure", "tests/guest/portable/mm-contracts-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-munmap"
flags = ["flag:page-aligned address and nonzero page-rounded length"]
structs = ["explicit-none"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate address geometry and access permissions before applying address-space changes"]
usercopy = ["explicit-none"]
state = ["state:split or remove VMAs, clear memory-policy ranges, invalidate probe mappings and finish deferred wakes"]
concurrency = ["concurrency:address-space mutation is serialized and retains mapping ownership"]
teardown = ["teardown:request references and deferred backing release complete after mutation"]

[[cell]]
number = 11
name = "munmap"
status = "implemented"
contract = "linux-munmap"
handler = "kernel/src/syscall/mm/mmap.rs:sys_munmap"
conditional = "explicit-none"
tests = ["tests/guest/portable/time-differential.c:main", "tests/guest/portable/mm-contracts-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-mincore"
flags = ["explicit-none"]
structs = ["struct:one residency byte per page"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:address alignment EINVAL, range or holes ENOMEM, vec access/copy EFAULT"]
usercopy = ["usercopy:zero-length vec needs no mapping or bytes; nonempty vec snapshots page-table/file-cache residency"]
state = ["state:read-only residency query"]
concurrency = ["concurrency:address-space mutation is serialized and retains mapping ownership"]
teardown = ["teardown:request references and deferred backing release complete after mutation"]

[[cell]]
number = 27
name = "mincore"
status = "implemented"
contract = "linux-mincore"
handler = "kernel/src/syscall/mm/mincore.rs:sys_mincore"
conditional = "explicit-none"
tests = ["kernel/src/syscall/mm/mincore.rs:zero_length_accepts_null_vec_without_mapping_or_usercopy", "tests/guest/portable/mm-contracts-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-process_vm_readv"
flags = ["flag:flags must be zero; bounded local and remote iovec counts"]
structs = ["struct:native iovec address/length arrays"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:descriptor import before transfer; AttachReal permission required; return completed positive prefix on later fault"]
usercopy = ["usercopy:import all descriptors from caller space then page-bounded remote-to-local transfer"]
state = ["state:copy remote bytes without changing mapping ownership"]
concurrency = ["concurrency:address-space mutation is serialized and retains mapping ownership"]
teardown = ["teardown:request references and deferred backing release complete after mutation"]

[[cell]]
number = 310
name = "process_vm_readv"
status = "implemented"
contract = "linux-process_vm_readv"
handler = "kernel/src/syscall/mm/process_vm.rs:sys_process_vm_readv"
conditional = "explicit-none"
tests = ["kernel/src/syscall/mm/process_vm.rs:process_vm_copy_returns_prefix_when_remote_second_page_faults", "tests/guest/portable/mm-contracts-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-process_vm_writev"
flags = ["flag:flags must be zero; bounded local and remote iovec counts"]
structs = ["struct:native iovec address/length arrays"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:descriptor import before transfer; AttachReal permission required; return completed positive prefix on later fault"]
usercopy = ["usercopy:import all descriptors from caller space then page-bounded local-to-remote transfer"]
state = ["state:write remote bytes through target mapping permission checks"]
concurrency = ["concurrency:address-space mutation is serialized and retains mapping ownership"]
teardown = ["teardown:request references and deferred backing release complete after mutation"]

[[cell]]
number = 311
name = "process_vm_writev"
status = "implemented"
contract = "linux-process_vm_writev"
handler = "kernel/src/syscall/mm/process_vm.rs:sys_process_vm_writev"
conditional = "explicit-none"
tests = ["kernel/src/syscall/mm/process_vm.rs:process_vm_copy_reports_efault_before_the_first_byte", "tests/guest/portable/mm-contracts-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-mseal"
flags = ["flag:flags must be zero; page-aligned address with rounded length"]
structs = ["explicit-none"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate address geometry and access permissions before applying address-space changes"]
usercopy = ["explicit-none"]
state = ["state:seal VMA geometry against later unmap remap protection and destructive advice operations"]
concurrency = ["concurrency:address-space mutation is serialized and retains mapping ownership"]
teardown = ["teardown:request references and deferred backing release complete after mutation"]

[[cell]]
number = 462
name = "mseal"
status = "implemented"
contract = "linux-mseal"
handler = "kernel/src/syscall/mm/mmap.rs:sys_mseal"
conditional = "explicit-none"
tests = ["kernel/src/syscall/mm/mmap.rs:mseal_normalizes_length_and_rejects_invalid_geometry", "tests/guest/portable/mm-contracts-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-pkey_alloc"
flags = ["flag:flags must be zero; init rights are PKRU access-disable/write-disable bits"]
structs = ["explicit-none"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:full-width flags and rights EINVAL before CPU support/free-key ENOSPC"]
usercopy = ["explicit-none"]
state = ["state:allocate lowest free nonzero protection key and initialize current task PKRU rights"]
concurrency = ["concurrency:process key allocation and per-task PKRU initialization form a rollback transaction"]
teardown = ["teardown:failed initialization returns allocated key to the free set"]

[[cell]]
number = 330
name = "pkey_alloc"
status = "implemented"
contract = "linux-pkey_alloc"
handler = "kernel/src/syscall/mm/mmap.rs:sys_pkey_alloc"
conditional = "explicit-none"
tests = ["tests/guest/tools/cpu-smoke.c:pku_state"]
validation_gaps = ["New high-word and exhausted-key CPU regression awaits TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-setxattr"
flags = ["flag:XATTR_CREATE XATTR_REPLACE with contradictory bits rejected"]
structs = ["struct:bounded xattr name and byte value; list returns NUL-terminated name sequence"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:bounded imports before path/fd and namespace/provider validation; set duplicate EEXIST, absent get/remove ENODATA, output size ERANGE"]
usercopy = ["usercopy:request-owned name/value/path import and snapshot-based output copy"]
state = ["state:ordinary path follows symlink; l-prefix retains final symlink; f-prefix targets retained fd; inode provider owns attributes"]
concurrency = ["concurrency:provider serializes create/replace/remove and coherent value/list snapshot"]
teardown = ["teardown:copied buffers and retained inode/OFD references release on every completion path"]

[[cell]]
number = 188
name = "setxattr"
status = "implemented"
contract = "linux-setxattr"
handler = "kernel/src/syscall/fs/xattr.rs:sys_setxattr"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/xattr.rs:xattr_name_import_requires_only_nonempty_bounded_bytes", "kernel/src/file/xattr_provider.rs:fifo_user_xattr_access_denies_reads_and_preserves_provider_records"]
validation_gaps = ["Legacy pathname symlink and fd syscall entry/error behavior lacks a direct guest differential matrix"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-lsetxattr"
flags = ["flag:XATTR_CREATE XATTR_REPLACE with contradictory bits rejected"]
structs = ["struct:bounded xattr name and byte value; list returns NUL-terminated name sequence"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:bounded imports before path/fd and namespace/provider validation; set duplicate EEXIST, absent get/remove ENODATA, output size ERANGE"]
usercopy = ["usercopy:request-owned name/value/path import and snapshot-based output copy"]
state = ["state:ordinary path follows symlink; l-prefix retains final symlink; f-prefix targets retained fd; inode provider owns attributes"]
concurrency = ["concurrency:provider serializes create/replace/remove and coherent value/list snapshot"]
teardown = ["teardown:copied buffers and retained inode/OFD references release on every completion path"]

[[cell]]
number = 189
name = "lsetxattr"
status = "implemented"
contract = "linux-lsetxattr"
handler = "kernel/src/syscall/fs/xattr.rs:sys_lsetxattr"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/xattr.rs:xattr_name_import_requires_only_nonempty_bounded_bytes", "kernel/src/file/xattr_provider.rs:fifo_user_xattr_access_denies_reads_and_preserves_provider_records"]
validation_gaps = ["Legacy pathname symlink and fd syscall entry/error behavior lacks a direct guest differential matrix"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-fsetxattr"
flags = ["flag:XATTR_CREATE XATTR_REPLACE with contradictory bits rejected"]
structs = ["struct:bounded xattr name and byte value; list returns NUL-terminated name sequence"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:bounded imports before path/fd and namespace/provider validation; set duplicate EEXIST, absent get/remove ENODATA, output size ERANGE"]
usercopy = ["usercopy:request-owned name/value/path import and snapshot-based output copy"]
state = ["state:ordinary path follows symlink; l-prefix retains final symlink; f-prefix targets retained fd; inode provider owns attributes"]
concurrency = ["concurrency:provider serializes create/replace/remove and coherent value/list snapshot"]
teardown = ["teardown:copied buffers and retained inode/OFD references release on every completion path"]

[[cell]]
number = 190
name = "fsetxattr"
status = "implemented"
contract = "linux-fsetxattr"
handler = "kernel/src/syscall/fs/xattr.rs:sys_fsetxattr"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/xattr.rs:xattr_name_import_requires_only_nonempty_bounded_bytes", "kernel/src/file/xattr_provider.rs:fifo_user_xattr_access_denies_reads_and_preserves_provider_records"]
validation_gaps = ["Legacy pathname symlink and fd syscall entry/error behavior lacks a direct guest differential matrix"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-getxattr"
flags = ["explicit-none"]
structs = ["struct:bounded xattr name and byte value; list returns NUL-terminated name sequence"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:bounded imports before path/fd and namespace/provider validation; set duplicate EEXIST, absent get/remove ENODATA, output size ERANGE"]
usercopy = ["usercopy:request-owned name/value/path import and snapshot-based output copy"]
state = ["state:ordinary path follows symlink; l-prefix retains final symlink; f-prefix targets retained fd; inode provider owns attributes"]
concurrency = ["concurrency:provider serializes create/replace/remove and coherent value/list snapshot"]
teardown = ["teardown:copied buffers and retained inode/OFD references release on every completion path"]

[[cell]]
number = 191
name = "getxattr"
status = "implemented"
contract = "linux-getxattr"
handler = "kernel/src/syscall/fs/xattr.rs:sys_getxattr"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/xattr.rs:xattr_name_import_requires_only_nonempty_bounded_bytes", "kernel/src/file/xattr_provider.rs:fifo_user_xattr_access_denies_reads_and_preserves_provider_records"]
validation_gaps = ["Legacy pathname symlink and fd syscall entry/error behavior lacks a direct guest differential matrix"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-lgetxattr"
flags = ["explicit-none"]
structs = ["struct:bounded xattr name and byte value; list returns NUL-terminated name sequence"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:bounded imports before path/fd and namespace/provider validation; set duplicate EEXIST, absent get/remove ENODATA, output size ERANGE"]
usercopy = ["usercopy:request-owned name/value/path import and snapshot-based output copy"]
state = ["state:ordinary path follows symlink; l-prefix retains final symlink; f-prefix targets retained fd; inode provider owns attributes"]
concurrency = ["concurrency:provider serializes create/replace/remove and coherent value/list snapshot"]
teardown = ["teardown:copied buffers and retained inode/OFD references release on every completion path"]

[[cell]]
number = 192
name = "lgetxattr"
status = "implemented"
contract = "linux-lgetxattr"
handler = "kernel/src/syscall/fs/xattr.rs:sys_lgetxattr"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/xattr.rs:xattr_name_import_requires_only_nonempty_bounded_bytes", "kernel/src/file/xattr_provider.rs:fifo_user_xattr_access_denies_reads_and_preserves_provider_records"]
validation_gaps = ["Legacy pathname symlink and fd syscall entry/error behavior lacks a direct guest differential matrix"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-fgetxattr"
flags = ["explicit-none"]
structs = ["struct:bounded xattr name and byte value; list returns NUL-terminated name sequence"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:bounded imports before path/fd and namespace/provider validation; set duplicate EEXIST, absent get/remove ENODATA, output size ERANGE"]
usercopy = ["usercopy:request-owned name/value/path import and snapshot-based output copy"]
state = ["state:ordinary path follows symlink; l-prefix retains final symlink; f-prefix targets retained fd; inode provider owns attributes"]
concurrency = ["concurrency:provider serializes create/replace/remove and coherent value/list snapshot"]
teardown = ["teardown:copied buffers and retained inode/OFD references release on every completion path"]

[[cell]]
number = 193
name = "fgetxattr"
status = "implemented"
contract = "linux-fgetxattr"
handler = "kernel/src/syscall/fs/xattr.rs:sys_fgetxattr"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/xattr.rs:xattr_name_import_requires_only_nonempty_bounded_bytes", "kernel/src/file/xattr_provider.rs:fifo_user_xattr_access_denies_reads_and_preserves_provider_records"]
validation_gaps = ["Legacy pathname symlink and fd syscall entry/error behavior lacks a direct guest differential matrix"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-listxattr"
flags = ["explicit-none"]
structs = ["struct:bounded xattr name and byte value; list returns NUL-terminated name sequence"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:bounded imports before path/fd and namespace/provider validation; set duplicate EEXIST, absent get/remove ENODATA, output size ERANGE"]
usercopy = ["usercopy:request-owned name/value/path import and snapshot-based output copy"]
state = ["state:ordinary path follows symlink; l-prefix retains final symlink; f-prefix targets retained fd; inode provider owns attributes"]
concurrency = ["concurrency:provider serializes create/replace/remove and coherent value/list snapshot"]
teardown = ["teardown:copied buffers and retained inode/OFD references release on every completion path"]

[[cell]]
number = 194
name = "listxattr"
status = "implemented"
contract = "linux-listxattr"
handler = "kernel/src/syscall/fs/xattr.rs:sys_listxattr"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/xattr.rs:xattr_name_import_requires_only_nonempty_bounded_bytes", "kernel/src/file/xattr_provider.rs:fifo_user_xattr_access_denies_reads_and_preserves_provider_records"]
validation_gaps = ["Legacy pathname symlink and fd syscall entry/error behavior lacks a direct guest differential matrix"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-llistxattr"
flags = ["explicit-none"]
structs = ["struct:bounded xattr name and byte value; list returns NUL-terminated name sequence"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:bounded imports before path/fd and namespace/provider validation; set duplicate EEXIST, absent get/remove ENODATA, output size ERANGE"]
usercopy = ["usercopy:request-owned name/value/path import and snapshot-based output copy"]
state = ["state:ordinary path follows symlink; l-prefix retains final symlink; f-prefix targets retained fd; inode provider owns attributes"]
concurrency = ["concurrency:provider serializes create/replace/remove and coherent value/list snapshot"]
teardown = ["teardown:copied buffers and retained inode/OFD references release on every completion path"]

[[cell]]
number = 195
name = "llistxattr"
status = "implemented"
contract = "linux-llistxattr"
handler = "kernel/src/syscall/fs/xattr.rs:sys_llistxattr"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/xattr.rs:xattr_name_import_requires_only_nonempty_bounded_bytes", "kernel/src/file/xattr_provider.rs:fifo_user_xattr_access_denies_reads_and_preserves_provider_records"]
validation_gaps = ["Legacy pathname symlink and fd syscall entry/error behavior lacks a direct guest differential matrix"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-flistxattr"
flags = ["explicit-none"]
structs = ["struct:bounded xattr name and byte value; list returns NUL-terminated name sequence"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:bounded imports before path/fd and namespace/provider validation; set duplicate EEXIST, absent get/remove ENODATA, output size ERANGE"]
usercopy = ["usercopy:request-owned name/value/path import and snapshot-based output copy"]
state = ["state:ordinary path follows symlink; l-prefix retains final symlink; f-prefix targets retained fd; inode provider owns attributes"]
concurrency = ["concurrency:provider serializes create/replace/remove and coherent value/list snapshot"]
teardown = ["teardown:copied buffers and retained inode/OFD references release on every completion path"]

[[cell]]
number = 196
name = "flistxattr"
status = "implemented"
contract = "linux-flistxattr"
handler = "kernel/src/syscall/fs/xattr.rs:sys_flistxattr"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/xattr.rs:xattr_name_import_requires_only_nonempty_bounded_bytes", "kernel/src/file/xattr_provider.rs:fifo_user_xattr_access_denies_reads_and_preserves_provider_records"]
validation_gaps = ["Legacy pathname symlink and fd syscall entry/error behavior lacks a direct guest differential matrix"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-removexattr"
flags = ["explicit-none"]
structs = ["struct:bounded xattr name and byte value; list returns NUL-terminated name sequence"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:bounded imports before path/fd and namespace/provider validation; set duplicate EEXIST, absent get/remove ENODATA, output size ERANGE"]
usercopy = ["usercopy:request-owned name/value/path import and snapshot-based output copy"]
state = ["state:ordinary path follows symlink; l-prefix retains final symlink; f-prefix targets retained fd; inode provider owns attributes"]
concurrency = ["concurrency:provider serializes create/replace/remove and coherent value/list snapshot"]
teardown = ["teardown:copied buffers and retained inode/OFD references release on every completion path"]

[[cell]]
number = 197
name = "removexattr"
status = "implemented"
contract = "linux-removexattr"
handler = "kernel/src/syscall/fs/xattr.rs:sys_removexattr"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/xattr.rs:xattr_name_import_requires_only_nonempty_bounded_bytes", "kernel/src/file/xattr_provider.rs:fifo_user_xattr_access_denies_reads_and_preserves_provider_records"]
validation_gaps = ["Legacy pathname symlink and fd syscall entry/error behavior lacks a direct guest differential matrix"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-lremovexattr"
flags = ["explicit-none"]
structs = ["struct:bounded xattr name and byte value; list returns NUL-terminated name sequence"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:bounded imports before path/fd and namespace/provider validation; set duplicate EEXIST, absent get/remove ENODATA, output size ERANGE"]
usercopy = ["usercopy:request-owned name/value/path import and snapshot-based output copy"]
state = ["state:ordinary path follows symlink; l-prefix retains final symlink; f-prefix targets retained fd; inode provider owns attributes"]
concurrency = ["concurrency:provider serializes create/replace/remove and coherent value/list snapshot"]
teardown = ["teardown:copied buffers and retained inode/OFD references release on every completion path"]

[[cell]]
number = 198
name = "lremovexattr"
status = "implemented"
contract = "linux-lremovexattr"
handler = "kernel/src/syscall/fs/xattr.rs:sys_lremovexattr"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/xattr.rs:xattr_name_import_requires_only_nonempty_bounded_bytes", "kernel/src/file/xattr_provider.rs:fifo_user_xattr_access_denies_reads_and_preserves_provider_records"]
validation_gaps = ["Legacy pathname symlink and fd syscall entry/error behavior lacks a direct guest differential matrix"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-fremovexattr"
flags = ["explicit-none"]
structs = ["struct:bounded xattr name and byte value; list returns NUL-terminated name sequence"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:bounded imports before path/fd and namespace/provider validation; set duplicate EEXIST, absent get/remove ENODATA, output size ERANGE"]
usercopy = ["usercopy:request-owned name/value/path import and snapshot-based output copy"]
state = ["state:ordinary path follows symlink; l-prefix retains final symlink; f-prefix targets retained fd; inode provider owns attributes"]
concurrency = ["concurrency:provider serializes create/replace/remove and coherent value/list snapshot"]
teardown = ["teardown:copied buffers and retained inode/OFD references release on every completion path"]

[[cell]]
number = 199
name = "fremovexattr"
status = "implemented"
contract = "linux-fremovexattr"
handler = "kernel/src/syscall/fs/xattr.rs:sys_fremovexattr"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/xattr.rs:xattr_name_import_requires_only_nonempty_bounded_bytes", "kernel/src/file/xattr_provider.rs:fifo_user_xattr_access_denies_reads_and_preserves_provider_records"]
validation_gaps = ["Legacy pathname symlink and fd syscall entry/error behavior lacks a direct guest differential matrix"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-stat"
flags = ["flag:pathname or retained fd target; at variants use empty-path and symlink flags"]
structs = ["struct:native x86_64 stat/statx/statfs or legacy ustat output"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate flags and request shape; resolve pathname or fd; provider read then output copy"]
usercopy = ["usercopy:bounded pathname import and initialized output record copy"]
state = ["state:read retained inode and mount metadata without mutating target"]
concurrency = ["concurrency:retain resolved location or OFD during provider access"]
teardown = ["teardown:release request-owned pathname and target references on completion"]

[[cell]]
number = 4
name = "stat"
status = "implemented"
contract = "linux-stat"
handler = "kernel/src/syscall/fs/stat.rs:sys_stat"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/stat.rs:empty_path_fd_detection_requires_the_flag_and_an_empty_path"]
validation_gaps = ["Direct Linux 7.2.3 guest entry and provider/error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-fstat"
flags = ["flag:pathname or retained fd target; at variants use empty-path and symlink flags"]
structs = ["struct:native x86_64 stat/statx/statfs or legacy ustat output"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate flags and request shape; resolve pathname or fd; provider read then output copy"]
usercopy = ["usercopy:bounded pathname import and initialized output record copy"]
state = ["state:read retained inode and mount metadata without mutating target"]
concurrency = ["concurrency:retain resolved location or OFD during provider access"]
teardown = ["teardown:release request-owned pathname and target references on completion"]

[[cell]]
number = 5
name = "fstat"
status = "implemented"
contract = "linux-fstat"
handler = "kernel/src/syscall/fs/stat.rs:sys_fstat"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/stat.rs:empty_path_fd_detection_requires_the_flag_and_an_empty_path"]
validation_gaps = ["Direct Linux 7.2.3 guest entry and provider/error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-lstat"
flags = ["flag:pathname or retained fd target; at variants use empty-path and symlink flags"]
structs = ["struct:native x86_64 stat/statx/statfs or legacy ustat output"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate flags and request shape; resolve pathname or fd; provider read then output copy"]
usercopy = ["usercopy:bounded pathname import and initialized output record copy"]
state = ["state:read retained inode and mount metadata without mutating target"]
concurrency = ["concurrency:retain resolved location or OFD during provider access"]
teardown = ["teardown:release request-owned pathname and target references on completion"]

[[cell]]
number = 6
name = "lstat"
status = "implemented"
contract = "linux-lstat"
handler = "kernel/src/syscall/fs/stat.rs:sys_lstat"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/stat.rs:empty_path_fd_detection_requires_the_flag_and_an_empty_path"]
validation_gaps = ["Direct Linux 7.2.3 guest entry and provider/error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-newfstatat"
flags = ["flag:pathname or retained fd target; at variants use empty-path and symlink flags"]
structs = ["struct:native x86_64 stat/statx/statfs or legacy ustat output"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate flags and request shape; resolve pathname or fd; provider read then output copy"]
usercopy = ["usercopy:bounded pathname import and initialized output record copy"]
state = ["state:read retained inode and mount metadata without mutating target"]
concurrency = ["concurrency:retain resolved location or OFD during provider access"]
teardown = ["teardown:release request-owned pathname and target references on completion"]

[[cell]]
number = 262
name = "newfstatat"
status = "implemented"
contract = "linux-newfstatat"
handler = "kernel/src/syscall/fs/stat.rs:sys_fstatat"
conditional = "explicit-none"
tests = ["tests/guest/portable/stat-access-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-access"
flags = ["flag:R_OK W_OK X_OK; faccessat2 AT_EACCESS AT_SYMLINK_NOFOLLOW AT_EMPTY_PATH"]
structs = ["explicit-none"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate flags and request shape; resolve pathname or fd; provider read then output copy"]
usercopy = ["usercopy:bounded pathname import and initialized output record copy"]
state = ["state:evaluate real or effective credentials against inode permissions and read-only mount policy"]
concurrency = ["concurrency:retain resolved location or OFD during provider access"]
teardown = ["teardown:release request-owned pathname and target references on completion"]

[[cell]]
number = 21
name = "access"
status = "implemented"
contract = "linux-access"
handler = "kernel/src/syscall/fs/stat.rs:sys_access"
conditional = "explicit-none"
tests = ["tests/guest/portable/stat-access-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-faccessat"
flags = ["flag:R_OK W_OK X_OK; faccessat2 AT_EACCESS AT_SYMLINK_NOFOLLOW AT_EMPTY_PATH"]
structs = ["explicit-none"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate flags and request shape; resolve pathname or fd; provider read then output copy"]
usercopy = ["usercopy:bounded pathname import and initialized output record copy"]
state = ["state:evaluate real or effective credentials against inode permissions and read-only mount policy"]
concurrency = ["concurrency:retain resolved location or OFD during provider access"]
teardown = ["teardown:release request-owned pathname and target references on completion"]

[[cell]]
number = 269
name = "faccessat"
status = "implemented"
contract = "linux-faccessat"
handler = "kernel/src/syscall/fs/stat.rs:sys_faccessat"
conditional = "explicit-none"
tests = ["tests/guest/portable/stat-access-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-faccessat2"
flags = ["flag:R_OK W_OK X_OK; faccessat2 AT_EACCESS AT_SYMLINK_NOFOLLOW AT_EMPTY_PATH"]
structs = ["explicit-none"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate flags and request shape; resolve pathname or fd; provider read then output copy"]
usercopy = ["usercopy:bounded pathname import and initialized output record copy"]
state = ["state:evaluate real or effective credentials against inode permissions and read-only mount policy"]
concurrency = ["concurrency:retain resolved location or OFD during provider access"]
teardown = ["teardown:release request-owned pathname and target references on completion"]

[[cell]]
number = 439
name = "faccessat2"
status = "implemented"
contract = "linux-faccessat2"
handler = "kernel/src/syscall/fs/stat.rs:sys_faccessat2"
conditional = "explicit-none"
tests = ["tests/guest/portable/stat-access-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-ustat"
flags = ["flag:pathname or retained fd target; at variants use empty-path and symlink flags"]
structs = ["struct:native x86_64 stat/statx/statfs or legacy ustat output"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate flags and request shape; resolve pathname or fd; provider read then output copy"]
usercopy = ["usercopy:bounded pathname import and initialized output record copy"]
state = ["state:read retained inode and mount metadata without mutating target"]
concurrency = ["concurrency:retain resolved location or OFD during provider access"]
teardown = ["teardown:release request-owned pathname and target references on completion"]

[[cell]]
number = 136
name = "ustat"
status = "implemented"
contract = "linux-ustat"
handler = "kernel/src/syscall/fs/stat.rs:sys_ustat"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/stat.rs:ustat_has_complete_zeroed_native_layout"]
validation_gaps = ["Direct Linux 7.2.3 guest entry and provider/error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-statfs"
flags = ["flag:pathname or retained fd target; at variants use empty-path and symlink flags"]
structs = ["struct:native x86_64 stat/statx/statfs or legacy ustat output"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate flags and request shape; resolve pathname or fd; provider read then output copy"]
usercopy = ["usercopy:bounded pathname import and initialized output record copy"]
state = ["state:read retained inode and mount metadata without mutating target"]
concurrency = ["concurrency:retain resolved location or OFD during provider access"]
teardown = ["teardown:release request-owned pathname and target references on completion"]

[[cell]]
number = 137
name = "statfs"
status = "implemented"
contract = "linux-statfs"
handler = "kernel/src/syscall/fs/stat.rs:sys_statfs"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest entry and provider/error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-fstatfs"
flags = ["flag:pathname or retained fd target; at variants use empty-path and symlink flags"]
structs = ["struct:native x86_64 stat/statx/statfs or legacy ustat output"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate flags and request shape; resolve pathname or fd; provider read then output copy"]
usercopy = ["usercopy:bounded pathname import and initialized output record copy"]
state = ["state:read retained inode and mount metadata without mutating target"]
concurrency = ["concurrency:retain resolved location or OFD during provider access"]
teardown = ["teardown:release request-owned pathname and target references on completion"]

[[cell]]
number = 138
name = "fstatfs"
status = "implemented"
contract = "linux-fstatfs"
handler = "kernel/src/syscall/fs/stat.rs:sys_fstatfs"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest entry and provider/error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-statx"
flags = ["flag:pathname or retained fd target; at variants use empty-path and symlink flags"]
structs = ["struct:native x86_64 stat/statx/statfs or legacy ustat output"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate flags and request shape; resolve pathname or fd; provider read then output copy"]
usercopy = ["usercopy:bounded pathname import and initialized output record copy"]
state = ["state:read retained inode and mount metadata without mutating target"]
concurrency = ["concurrency:retain resolved location or OFD during provider access"]
teardown = ["teardown:release request-owned pathname and target references on completion"]

[[cell]]
number = 332
name = "statx"
status = "implemented"
contract = "linux-statx"
handler = "kernel/src/syscall/fs/stat.rs:sys_statx"
conditional = "explicit-none"
tests = ["tests/guest/portable/stat-access-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-exit"
flags = ["explicit-none"]
structs = ["explicit-none"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["explicit-none"]
usercopy = ["explicit-none"]
state = ["state:encode the low eight exit-code bits into wait status; terminate caller thread"]
concurrency = ["concurrency:coordinate exiting task with group lifetime and waiter notification"]
teardown = ["teardown:release exiting task resources and publish reaping state"]

[[cell]]
number = 60
name = "exit"
status = "implemented"
contract = "linux-exit"
handler = "kernel/src/syscall/task/exit.rs:sys_exit"
conditional = "explicit-none"
tests = ["tests/guest/portable/exit-status.c:main"]
validation_gaps = ["Raw status regression passed host Linux and TheKernel KVM; matched Linux 7.2.3 oracle and multithread group teardown matrix remain pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-exit_group"
flags = ["explicit-none"]
structs = ["explicit-none"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["explicit-none"]
usercopy = ["explicit-none"]
state = ["state:encode the low eight exit-code bits into wait status; terminate caller thread and its thread group"]
concurrency = ["concurrency:coordinate exiting task with group lifetime and waiter notification"]
teardown = ["teardown:release exiting task resources and publish reaping state"]

[[cell]]
number = 231
name = "exit_group"
status = "implemented"
contract = "linux-exit_group"
handler = "kernel/src/syscall/task/exit.rs:sys_exit_group"
conditional = "explicit-none"
tests = ["tests/guest/portable/exit-status.c:main"]
validation_gaps = ["Raw status regression passed host Linux and TheKernel KVM; matched Linux 7.2.3 oracle and multithread group teardown matrix remain pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-socket"
flags = ["flag:socket type CLOEXEC NONBLOCK; operation-specific message or address flags"]
structs = ["struct:native sockaddr and socklen; message entries use msghdr iovec cmsghdr; option entries use length-delimited values"]
multiplexer_commands = ["mux:socket family protocol and option level/name select provider behavior"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:pin fd and capture authority; import operation-owned inputs; run provider admission and output-copy validation"]
usercopy = ["usercopy:bounded sockaddr message and option snapshots; explicit short output and length copyout"]
state = ["state:retain socket OFD and apply operation to its protocol endpoint"]
concurrency = ["concurrency:poll readiness and endpoint synchronization coordinate blocking calls and peer activity"]
teardown = ["teardown:release retained endpoint and owned message/control data on completion"]

[[cell]]
number = 41
name = "socket"
status = "partial"
contract = "linux-socket"
handler = "kernel/src/syscall/net/socket.rs:sys_socket"
conditional = "explicit-none"
tests = []
validation_gaps = ["Protocol/provider and combined-error raw guest differential matrix is not yet bound and executed"]
limitations = ["SOCK_PACKET and AF_UNIX SOCK_RAW compatibility are absent; NETLINK_USERSOCK protocol is rejected"]

[[contract]]
id = "linux-socketpair"
flags = ["flag:socket type CLOEXEC NONBLOCK; operation-specific message or address flags"]
structs = ["struct:native sockaddr and socklen; message entries use msghdr iovec cmsghdr; option entries use length-delimited values"]
multiplexer_commands = ["mux:socket family protocol and option level/name select provider behavior"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:pin fd and capture authority; import operation-owned inputs; run provider admission and output-copy validation"]
usercopy = ["usercopy:bounded sockaddr message and option snapshots; explicit short output and length copyout"]
state = ["state:retain socket OFD and apply operation to its protocol endpoint"]
concurrency = ["concurrency:poll readiness and endpoint synchronization coordinate blocking calls and peer activity"]
teardown = ["teardown:release retained endpoint and owned message/control data on completion"]

[[cell]]
number = 53
name = "socketpair"
status = "partial"
contract = "linux-socketpair"
handler = "kernel/src/syscall/net/socket.rs:sys_socketpair"
conditional = "explicit-none"
tests = []
validation_gaps = ["Protocol/provider and combined-error raw guest differential matrix is not yet bound and executed"]
limitations = ["AF_UNIX SOCK_RAW compatibility and nonzero protocol validation differ from Linux"]

[[contract]]
id = "linux-bind"
flags = ["flag:socket type CLOEXEC NONBLOCK; operation-specific message or address flags"]
structs = ["struct:native sockaddr and socklen; message entries use msghdr iovec cmsghdr; option entries use length-delimited values"]
multiplexer_commands = ["mux:socket family protocol and option level/name select provider behavior"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:pin fd and capture authority; import operation-owned inputs; run provider admission and output-copy validation"]
usercopy = ["usercopy:bounded sockaddr message and option snapshots; explicit short output and length copyout"]
state = ["state:retain socket OFD and apply operation to its protocol endpoint"]
concurrency = ["concurrency:poll readiness and endpoint synchronization coordinate blocking calls and peer activity"]
teardown = ["teardown:release retained endpoint and owned message/control data on completion"]

[[cell]]
number = 49
name = "bind"
status = "partial"
contract = "linux-bind"
handler = "kernel/src/syscall/net/socket.rs:sys_bind"
conditional = "explicit-none"
tests = ["tests/guest/portable/network-basic-differential.c:main"]
validation_gaps = ["Network basic regression passed host Linux; matched TheKernel and Linux 7.2.3 guest execution remains pending; broader provider matrix remains untested"]
limitations = ["IPv4 and IPv6 addresses longer than sockaddr_storage are accepted; Netlink requires exactly twelve bytes instead of at least twelve"]

[[contract]]
id = "linux-connect"
flags = ["flag:socket type CLOEXEC NONBLOCK; operation-specific message or address flags"]
structs = ["struct:native sockaddr and socklen; message entries use msghdr iovec cmsghdr; option entries use length-delimited values"]
multiplexer_commands = ["mux:socket family protocol and option level/name select provider behavior"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:pin fd and capture authority; import operation-owned inputs; run provider admission and output-copy validation"]
usercopy = ["usercopy:bounded sockaddr message and option snapshots; explicit short output and length copyout"]
state = ["state:retain socket OFD and apply operation to its protocol endpoint"]
concurrency = ["concurrency:poll readiness and endpoint synchronization coordinate blocking calls and peer activity"]
teardown = ["teardown:release retained endpoint and owned message/control data on completion"]

[[cell]]
number = 42
name = "connect"
status = "partial"
contract = "linux-connect"
handler = "kernel/src/syscall/net/socket.rs:sys_connect"
conditional = "explicit-none"
tests = ["tests/guest/portable/network-basic-differential.c:main"]
validation_gaps = ["Network basic regression passed host Linux; matched TheKernel and Linux 7.2.3 guest execution remains pending; broader provider matrix remains untested"]
limitations = ["Netlink peer connect and AF_UNSPEC disconnect are absent; overlong IP sockaddr is accepted"]

[[contract]]
id = "linux-getsockname"
flags = ["flag:socket type CLOEXEC NONBLOCK; operation-specific message or address flags"]
structs = ["struct:native sockaddr and socklen; message entries use msghdr iovec cmsghdr; option entries use length-delimited values"]
multiplexer_commands = ["mux:socket family protocol and option level/name select provider behavior"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:pin fd and capture authority; import operation-owned inputs; run provider admission and output-copy validation"]
usercopy = ["usercopy:bounded sockaddr message and option snapshots; explicit short output and length copyout"]
state = ["state:retain socket OFD and apply operation to its protocol endpoint"]
concurrency = ["concurrency:poll readiness and endpoint synchronization coordinate blocking calls and peer activity"]
teardown = ["teardown:release retained endpoint and owned message/control data on completion"]

[[cell]]
number = 51
name = "getsockname"
status = "partial"
contract = "linux-getsockname"
handler = "kernel/src/syscall/net/name.rs:sys_getsockname"
conditional = "explicit-none"
tests = []
validation_gaps = ["Protocol/provider and combined-error raw guest differential matrix is not yet bound and executed"]
limitations = ["AF_ALG returns ENOTSOCK instead of EOPNOTSUPP and XDP exposes an address where Linux rejects getname"]

[[contract]]
id = "linux-getpeername"
flags = ["flag:socket type CLOEXEC NONBLOCK; operation-specific message or address flags"]
structs = ["struct:native sockaddr and socklen; message entries use msghdr iovec cmsghdr; option entries use length-delimited values"]
multiplexer_commands = ["mux:socket family protocol and option level/name select provider behavior"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:pin fd and capture authority; import operation-owned inputs; run provider admission and output-copy validation"]
usercopy = ["usercopy:bounded sockaddr message and option snapshots; explicit short output and length copyout"]
state = ["state:retain socket OFD and apply operation to its protocol endpoint"]
concurrency = ["concurrency:poll readiness and endpoint synchronization coordinate blocking calls and peer activity"]
teardown = ["teardown:release retained endpoint and owned message/control data on completion"]

[[cell]]
number = 52
name = "getpeername"
status = "partial"
contract = "linux-getpeername"
handler = "kernel/src/syscall/net/name.rs:sys_getpeername"
conditional = "explicit-none"
tests = ["tests/guest/portable/network-basic-differential.c:main"]
validation_gaps = ["Network basic regression passed host Linux; matched TheKernel and Linux 7.2.3 guest execution remains pending; broader provider matrix remains untested"]
limitations = ["Netlink peer address retrieval is absent and falls through to ENOTSOCK"]

[[contract]]
id = "linux-listen"
flags = ["flag:socket type CLOEXEC NONBLOCK; operation-specific message or address flags"]
structs = ["struct:native sockaddr and socklen; message entries use msghdr iovec cmsghdr; option entries use length-delimited values"]
multiplexer_commands = ["mux:socket family protocol and option level/name select provider behavior"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:pin fd and capture authority; import operation-owned inputs; run provider admission and output-copy validation"]
usercopy = ["usercopy:bounded sockaddr message and option snapshots; explicit short output and length copyout"]
state = ["state:retain socket OFD and apply operation to its protocol endpoint"]
concurrency = ["concurrency:poll readiness and endpoint synchronization coordinate blocking calls and peer activity"]
teardown = ["teardown:release retained endpoint and owned message/control data on completion"]

[[cell]]
number = 50
name = "listen"
status = "partial"
contract = "linux-listen"
handler = "kernel/src/syscall/net/socket.rs:sys_listen"
conditional = "explicit-none"
tests = []
validation_gaps = ["Protocol/provider and combined-error raw guest differential matrix is not yet bound and executed"]
limitations = ["Netlink and AF_ALG socket providers return ENOTSOCK instead of Linux EOPNOTSUPP"]

[[contract]]
id = "linux-accept"
flags = ["flag:socket type CLOEXEC NONBLOCK; operation-specific message or address flags"]
structs = ["struct:native sockaddr and socklen; message entries use msghdr iovec cmsghdr; option entries use length-delimited values"]
multiplexer_commands = ["mux:socket family protocol and option level/name select provider behavior"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:pin fd and capture authority; import operation-owned inputs; run provider admission and output-copy validation"]
usercopy = ["usercopy:bounded sockaddr message and option snapshots; explicit short output and length copyout"]
state = ["state:retain socket OFD and apply operation to its protocol endpoint"]
concurrency = ["concurrency:poll readiness and endpoint synchronization coordinate blocking calls and peer activity"]
teardown = ["teardown:release retained endpoint and owned message/control data on completion"]

[[cell]]
number = 43
name = "accept"
status = "partial"
contract = "linux-accept"
handler = "kernel/src/syscall/net/socket.rs:sys_accept"
conditional = "explicit-none"
tests = []
validation_gaps = ["Protocol/provider and combined-error raw guest differential matrix is not yet bound and executed"]
limitations = ["Netlink provider returns ENOTSOCK instead of Linux EOPNOTSUPP"]

[[contract]]
id = "linux-accept4"
flags = ["flag:socket type CLOEXEC NONBLOCK; operation-specific message or address flags"]
structs = ["struct:native sockaddr and socklen; message entries use msghdr iovec cmsghdr; option entries use length-delimited values"]
multiplexer_commands = ["mux:socket family protocol and option level/name select provider behavior"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:pin fd and capture authority; import operation-owned inputs; run provider admission and output-copy validation"]
usercopy = ["usercopy:bounded sockaddr message and option snapshots; explicit short output and length copyout"]
state = ["state:retain socket OFD and apply operation to its protocol endpoint"]
concurrency = ["concurrency:poll readiness and endpoint synchronization coordinate blocking calls and peer activity"]
teardown = ["teardown:release retained endpoint and owned message/control data on completion"]

[[cell]]
number = 288
name = "accept4"
status = "partial"
contract = "linux-accept4"
handler = "kernel/src/syscall/net/socket.rs:sys_accept4"
conditional = "explicit-none"
tests = []
validation_gaps = ["Protocol/provider and combined-error raw guest differential matrix is not yet bound and executed"]
limitations = ["Netlink provider returns ENOTSOCK instead of Linux EOPNOTSUPP"]

[[contract]]
id = "linux-shutdown"
flags = ["flag:socket type CLOEXEC NONBLOCK; operation-specific message or address flags"]
structs = ["struct:native sockaddr and socklen; message entries use msghdr iovec cmsghdr; option entries use length-delimited values"]
multiplexer_commands = ["mux:socket family protocol and option level/name select provider behavior"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:pin fd and capture authority; import operation-owned inputs; run provider admission and output-copy validation"]
usercopy = ["usercopy:bounded sockaddr message and option snapshots; explicit short output and length copyout"]
state = ["state:retain socket OFD and apply operation to its protocol endpoint"]
concurrency = ["concurrency:poll readiness and endpoint synchronization coordinate blocking calls and peer activity"]
teardown = ["teardown:release retained endpoint and owned message/control data on completion"]

[[cell]]
number = 48
name = "shutdown"
status = "partial"
contract = "linux-shutdown"
handler = "kernel/src/syscall/net/socket.rs:sys_shutdown"
conditional = "explicit-none"
tests = []
validation_gaps = ["Protocol/provider and combined-error raw guest differential matrix is not yet bound and executed"]
limitations = ["Netlink and AF_ALG providers return ENOTSOCK instead of Linux EOPNOTSUPP"]

[[contract]]
id = "linux-sendto"
flags = ["flag:socket type CLOEXEC NONBLOCK; operation-specific message or address flags"]
structs = ["struct:native sockaddr and socklen; message entries use msghdr iovec cmsghdr; option entries use length-delimited values"]
multiplexer_commands = ["mux:socket family protocol and option level/name select provider behavior"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:pin fd and capture authority; import operation-owned inputs; run provider admission and output-copy validation"]
usercopy = ["usercopy:bounded sockaddr message and option snapshots; explicit short output and length copyout"]
state = ["state:retain socket OFD and apply operation to its protocol endpoint"]
concurrency = ["concurrency:poll readiness and endpoint synchronization coordinate blocking calls and peer activity"]
teardown = ["teardown:release retained endpoint and owned message/control data on completion"]

[[cell]]
number = 44
name = "sendto"
status = "partial"
contract = "linux-sendto"
handler = "kernel/src/syscall/net/io.rs:sys_sendto"
conditional = "explicit-none"
tests = ["tests/guest/portable/network-basic-differential.c:main"]
validation_gaps = ["Network basic regression passed host Linux; matched TheKernel and Linux 7.2.3 guest execution remains pending; broader provider matrix remains untested"]
limitations = ["Ordinary socket send path rejects protocol-supported MSG_MORE MSG_DONTROUTE and MSG_CONFIRM flags"]

[[contract]]
id = "linux-sendmsg"
flags = ["flag:socket type CLOEXEC NONBLOCK; operation-specific message or address flags"]
structs = ["struct:native sockaddr and socklen; message entries use msghdr iovec cmsghdr; option entries use length-delimited values"]
multiplexer_commands = ["mux:socket family protocol and option level/name select provider behavior"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:pin fd and capture authority; import operation-owned inputs; run provider admission and output-copy validation"]
usercopy = ["usercopy:bounded sockaddr message and option snapshots; explicit short output and length copyout"]
state = ["state:retain socket OFD and apply operation to its protocol endpoint"]
concurrency = ["concurrency:poll readiness and endpoint synchronization coordinate blocking calls and peer activity"]
teardown = ["teardown:release retained endpoint and owned message/control data on completion"]

[[cell]]
number = 46
name = "sendmsg"
status = "partial"
contract = "linux-sendmsg"
handler = "kernel/src/syscall/net/io.rs:sys_sendmsg"
conditional = "explicit-none"
tests = []
validation_gaps = ["Protocol/provider and combined-error raw guest differential matrix is not yet bound and executed"]
limitations = ["Ordinary socket send path rejects protocol-supported MSG_MORE MSG_DONTROUTE and MSG_CONFIRM flags"]

[[contract]]
id = "linux-sendmmsg"
flags = ["flag:socket type CLOEXEC NONBLOCK; operation-specific message or address flags"]
structs = ["struct:native sockaddr and socklen; message entries use msghdr iovec cmsghdr; option entries use length-delimited values"]
multiplexer_commands = ["mux:socket family protocol and option level/name select provider behavior"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:pin fd and capture authority; import operation-owned inputs; run provider admission and output-copy validation"]
usercopy = ["usercopy:bounded sockaddr message and option snapshots; explicit short output and length copyout"]
state = ["state:retain socket OFD and apply operation to its protocol endpoint"]
concurrency = ["concurrency:poll readiness and endpoint synchronization coordinate blocking calls and peer activity"]
teardown = ["teardown:release retained endpoint and owned message/control data on completion"]

[[cell]]
number = 307
name = "sendmmsg"
status = "partial"
contract = "linux-sendmmsg"
handler = "kernel/src/syscall/net/io.rs:sys_sendmmsg"
conditional = "explicit-none"
tests = ["kernel/src/syscall/net/io.rs:mmsg_vlen_matches_linux_send_cap_and_unbounded_recv_batch"]
validation_gaps = ["Protocol/provider and combined-error raw guest differential matrix is not yet bound and executed"]
limitations = ["Each message inherits sendmsg rejection of protocol-supported MSG_MORE MSG_DONTROUTE and MSG_CONFIRM flags"]

[[contract]]
id = "linux-recvfrom"
flags = ["flag:socket type CLOEXEC NONBLOCK; operation-specific message or address flags"]
structs = ["struct:native sockaddr and socklen; message entries use msghdr iovec cmsghdr; option entries use length-delimited values"]
multiplexer_commands = ["mux:socket family protocol and option level/name select provider behavior"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:pin fd and capture authority; import operation-owned inputs; run provider admission and output-copy validation"]
usercopy = ["usercopy:bounded sockaddr message and option snapshots; explicit short output and length copyout"]
state = ["state:retain socket OFD and apply operation to its protocol endpoint"]
concurrency = ["concurrency:poll readiness and endpoint synchronization coordinate blocking calls and peer activity"]
teardown = ["teardown:release retained endpoint and owned message/control data on completion"]

[[cell]]
number = 45
name = "recvfrom"
status = "partial"
contract = "linux-recvfrom"
handler = "kernel/src/syscall/net/io.rs:sys_recvfrom"
conditional = "explicit-none"
tests = ["kernel/src/syscall/net/io.rs:stream_copyout_returns_a_prefix_but_datagram_copyout_keeps_efault"]
validation_gaps = ["Protocol/provider and combined-error raw guest differential matrix is not yet bound and executed"]
limitations = ["Ordinary stream receive rejects MSG_WAITALL rather than implementing its receive completion semantics"]

[[contract]]
id = "linux-recvmsg"
flags = ["flag:socket type CLOEXEC NONBLOCK; operation-specific message or address flags"]
structs = ["struct:native sockaddr and socklen; message entries use msghdr iovec cmsghdr; option entries use length-delimited values"]
multiplexer_commands = ["mux:socket family protocol and option level/name select provider behavior"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:pin fd and capture authority; import operation-owned inputs; run provider admission and output-copy validation"]
usercopy = ["usercopy:bounded sockaddr message and option snapshots; explicit short output and length copyout"]
state = ["state:retain socket OFD and apply operation to its protocol endpoint"]
concurrency = ["concurrency:poll readiness and endpoint synchronization coordinate blocking calls and peer activity"]
teardown = ["teardown:release retained endpoint and owned message/control data on completion"]

[[cell]]
number = 47
name = "recvmsg"
status = "partial"
contract = "linux-recvmsg"
handler = "kernel/src/syscall/net/io.rs:sys_recvmsg"
conditional = "explicit-none"
tests = ["kernel/src/syscall/net/io.rs:stream_copyout_returns_a_prefix_but_datagram_copyout_keeps_efault"]
validation_gaps = ["Protocol/provider and combined-error raw guest differential matrix is not yet bound and executed"]
limitations = ["Ordinary stream receive rejects MSG_WAITALL rather than implementing its receive completion semantics"]

[[contract]]
id = "linux-recvmmsg"
flags = ["flag:socket type CLOEXEC NONBLOCK; operation-specific message or address flags"]
structs = ["struct:native sockaddr and socklen; message entries use msghdr iovec cmsghdr; option entries use length-delimited values"]
multiplexer_commands = ["mux:socket family protocol and option level/name select provider behavior"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:pin fd and capture authority; import operation-owned inputs; run provider admission and output-copy validation"]
usercopy = ["usercopy:bounded sockaddr message and option snapshots; explicit short output and length copyout"]
state = ["state:retain socket OFD and apply operation to its protocol endpoint"]
concurrency = ["concurrency:poll readiness and endpoint synchronization coordinate blocking calls and peer activity"]
teardown = ["teardown:release retained endpoint and owned message/control data on completion"]

[[cell]]
number = 299
name = "recvmmsg"
status = "partial"
contract = "linux-recvmmsg"
handler = "kernel/src/syscall/net/io.rs:sys_recvmmsg"
conditional = "explicit-none"
tests = ["kernel/src/syscall/net/io.rs:mmsg_vlen_matches_linux_send_cap_and_unbounded_recv_batch"]
validation_gaps = ["Protocol/provider and combined-error raw guest differential matrix is not yet bound and executed"]
limitations = ["Nonzero batches with a non-null timeout are explicitly rejected with EOPNOTSUPP instead of using and updating a relative deadline"]

[[contract]]
id = "linux-getsockopt"
flags = ["flag:socket type CLOEXEC NONBLOCK; operation-specific message or address flags"]
structs = ["struct:native sockaddr and socklen; message entries use msghdr iovec cmsghdr; option entries use length-delimited values"]
multiplexer_commands = ["mux:socket family protocol and option level/name select provider behavior"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:pin fd and capture authority; import operation-owned inputs; run provider admission and output-copy validation"]
usercopy = ["usercopy:bounded sockaddr message and option snapshots; explicit short output and length copyout"]
state = ["state:retain socket OFD and apply operation to its protocol endpoint"]
concurrency = ["concurrency:poll readiness and endpoint synchronization coordinate blocking calls and peer activity"]
teardown = ["teardown:release retained endpoint and owned message/control data on completion"]

[[cell]]
number = 55
name = "getsockopt"
status = "partial"
contract = "linux-getsockopt"
handler = "kernel/src/syscall/net/opt.rs:sys_getsockopt"
conditional = "explicit-none"
tests = ["kernel/src/syscall/net/opt.rs:generic_option_copyout_reports_the_copied_short_or_zero_length"]
validation_gaps = ["Protocol/provider and combined-error raw guest differential matrix is not yet bound and executed"]
limitations = ["Netlink lacks generic SOL_SOCKET getters such as SO_TYPE and SO_DOMAIN plus NETLINK_PKTINFO and LIST_MEMBERSHIPS"]

[[contract]]
id = "linux-setsockopt"
flags = ["flag:socket type CLOEXEC NONBLOCK; operation-specific message or address flags"]
structs = ["struct:native sockaddr and socklen; message entries use msghdr iovec cmsghdr; option entries use length-delimited values"]
multiplexer_commands = ["mux:socket family protocol and option level/name select provider behavior"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:pin fd and capture authority; import operation-owned inputs; run provider admission and output-copy validation"]
usercopy = ["usercopy:bounded sockaddr message and option snapshots; explicit short output and length copyout"]
state = ["state:retain socket OFD and apply operation to its protocol endpoint"]
concurrency = ["concurrency:poll readiness and endpoint synchronization coordinate blocking calls and peer activity"]
teardown = ["teardown:release retained endpoint and owned message/control data on completion"]

[[cell]]
number = 54
name = "setsockopt"
status = "partial"
contract = "linux-setsockopt"
handler = "kernel/src/syscall/net/opt.rs:sys_setsockopt"
conditional = "explicit-none"
tests = []
validation_gaps = ["Protocol/provider and combined-error raw guest differential matrix is not yet bound and executed"]
limitations = ["Netlink lacks generic SOL_SOCKET setters and NETLINK_PKTINFO LISTEN_ALL_NSID options"]

[[contract]]
id = "linux-getuid"
flags = ["flag:native syscall integer widths; operation-specific identity resource or query selector"]
structs = ["struct:native uid/gid arrays and UTS/sysinfo/rlimit/rusage records where required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific admission and usercopy order; preserve visible prefix on ordered multi-output faults"]
usercopy = ["usercopy:request-owned input and initialized native output; scalar-only calls do not touch user memory"]
state = ["state:namespace-mapped real effective saved and filesystem identities or supplementary groups; credential admission precedes publication"]
concurrency = ["concurrency:retain credential namespace or process snapshot; mutations serialize through their owning subsystem"]
teardown = ["teardown:release imported buffers and retained snapshots on completion"]

[[cell]]
number = 102
name = "getuid"
status = "implemented"
contract = "linux-getuid"
handler = "kernel/src/syscall/sys.rs:sys_getuid"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest entry namespace/capability and combined-error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-geteuid"
flags = ["flag:native syscall integer widths; operation-specific identity resource or query selector"]
structs = ["struct:native uid/gid arrays and UTS/sysinfo/rlimit/rusage records where required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific admission and usercopy order; preserve visible prefix on ordered multi-output faults"]
usercopy = ["usercopy:request-owned input and initialized native output; scalar-only calls do not touch user memory"]
state = ["state:namespace-mapped real effective saved and filesystem identities or supplementary groups; credential admission precedes publication"]
concurrency = ["concurrency:retain credential namespace or process snapshot; mutations serialize through their owning subsystem"]
teardown = ["teardown:release imported buffers and retained snapshots on completion"]

[[cell]]
number = 107
name = "geteuid"
status = "implemented"
contract = "linux-geteuid"
handler = "kernel/src/syscall/sys.rs:sys_geteuid"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest entry namespace/capability and combined-error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-getresuid"
flags = ["flag:native syscall integer widths; operation-specific identity resource or query selector"]
structs = ["struct:native uid/gid arrays and UTS/sysinfo/rlimit/rusage records where required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific admission and usercopy order; preserve visible prefix on ordered multi-output faults"]
usercopy = ["usercopy:request-owned input and initialized native output; scalar-only calls do not touch user memory"]
state = ["state:namespace-mapped real effective saved and filesystem identities or supplementary groups; credential admission precedes publication"]
concurrency = ["concurrency:retain credential namespace or process snapshot; mutations serialize through their owning subsystem"]
teardown = ["teardown:release imported buffers and retained snapshots on completion"]

[[cell]]
number = 118
name = "getresuid"
status = "implemented"
contract = "linux-getresuid"
handler = "kernel/src/syscall/sys.rs:sys_getresuid"
conditional = "explicit-none"
tests = ["kernel/src/syscall/sys.rs:getresids_keeps_first_id_prefix_on_second_fault"]
validation_gaps = ["Direct Linux 7.2.3 guest entry namespace/capability and combined-error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-getgid"
flags = ["flag:native syscall integer widths; operation-specific identity resource or query selector"]
structs = ["struct:native uid/gid arrays and UTS/sysinfo/rlimit/rusage records where required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific admission and usercopy order; preserve visible prefix on ordered multi-output faults"]
usercopy = ["usercopy:request-owned input and initialized native output; scalar-only calls do not touch user memory"]
state = ["state:namespace-mapped real effective saved and filesystem identities or supplementary groups; credential admission precedes publication"]
concurrency = ["concurrency:retain credential namespace or process snapshot; mutations serialize through their owning subsystem"]
teardown = ["teardown:release imported buffers and retained snapshots on completion"]

[[cell]]
number = 104
name = "getgid"
status = "implemented"
contract = "linux-getgid"
handler = "kernel/src/syscall/sys.rs:sys_getgid"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest entry namespace/capability and combined-error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-getegid"
flags = ["flag:native syscall integer widths; operation-specific identity resource or query selector"]
structs = ["struct:native uid/gid arrays and UTS/sysinfo/rlimit/rusage records where required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific admission and usercopy order; preserve visible prefix on ordered multi-output faults"]
usercopy = ["usercopy:request-owned input and initialized native output; scalar-only calls do not touch user memory"]
state = ["state:namespace-mapped real effective saved and filesystem identities or supplementary groups; credential admission precedes publication"]
concurrency = ["concurrency:retain credential namespace or process snapshot; mutations serialize through their owning subsystem"]
teardown = ["teardown:release imported buffers and retained snapshots on completion"]

[[cell]]
number = 108
name = "getegid"
status = "implemented"
contract = "linux-getegid"
handler = "kernel/src/syscall/sys.rs:sys_getegid"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest entry namespace/capability and combined-error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-getresgid"
flags = ["flag:native syscall integer widths; operation-specific identity resource or query selector"]
structs = ["struct:native uid/gid arrays and UTS/sysinfo/rlimit/rusage records where required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific admission and usercopy order; preserve visible prefix on ordered multi-output faults"]
usercopy = ["usercopy:request-owned input and initialized native output; scalar-only calls do not touch user memory"]
state = ["state:namespace-mapped real effective saved and filesystem identities or supplementary groups; credential admission precedes publication"]
concurrency = ["concurrency:retain credential namespace or process snapshot; mutations serialize through their owning subsystem"]
teardown = ["teardown:release imported buffers and retained snapshots on completion"]

[[cell]]
number = 120
name = "getresgid"
status = "implemented"
contract = "linux-getresgid"
handler = "kernel/src/syscall/sys.rs:sys_getresgid"
conditional = "explicit-none"
tests = ["kernel/src/syscall/sys.rs:getresids_keeps_first_id_prefix_on_second_fault"]
validation_gaps = ["Direct Linux 7.2.3 guest entry namespace/capability and combined-error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-setuid"
flags = ["flag:native syscall integer widths; operation-specific identity resource or query selector"]
structs = ["struct:native uid/gid arrays and UTS/sysinfo/rlimit/rusage records where required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific admission and usercopy order; preserve visible prefix on ordered multi-output faults"]
usercopy = ["usercopy:request-owned input and initialized native output; scalar-only calls do not touch user memory"]
state = ["state:namespace-mapped real effective saved and filesystem identities or supplementary groups; credential admission precedes publication"]
concurrency = ["concurrency:retain credential namespace or process snapshot; mutations serialize through their owning subsystem"]
teardown = ["teardown:release imported buffers and retained snapshots on completion"]

[[cell]]
number = 105
name = "setuid"
status = "implemented"
contract = "linux-setuid"
handler = "kernel/src/syscall/sys.rs:sys_setuid"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest entry namespace/capability and combined-error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-setgid"
flags = ["flag:native syscall integer widths; operation-specific identity resource or query selector"]
structs = ["struct:native uid/gid arrays and UTS/sysinfo/rlimit/rusage records where required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific admission and usercopy order; preserve visible prefix on ordered multi-output faults"]
usercopy = ["usercopy:request-owned input and initialized native output; scalar-only calls do not touch user memory"]
state = ["state:namespace-mapped real effective saved and filesystem identities or supplementary groups; credential admission precedes publication"]
concurrency = ["concurrency:retain credential namespace or process snapshot; mutations serialize through their owning subsystem"]
teardown = ["teardown:release imported buffers and retained snapshots on completion"]

[[cell]]
number = 106
name = "setgid"
status = "implemented"
contract = "linux-setgid"
handler = "kernel/src/syscall/sys.rs:sys_setgid"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest entry namespace/capability and combined-error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-setfsuid"
flags = ["flag:native syscall integer widths; operation-specific identity resource or query selector"]
structs = ["struct:native uid/gid arrays and UTS/sysinfo/rlimit/rusage records where required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific admission and usercopy order; preserve visible prefix on ordered multi-output faults"]
usercopy = ["usercopy:request-owned input and initialized native output; scalar-only calls do not touch user memory"]
state = ["state:namespace-mapped real effective saved and filesystem identities or supplementary groups; credential admission precedes publication"]
concurrency = ["concurrency:retain credential namespace or process snapshot; mutations serialize through their owning subsystem"]
teardown = ["teardown:release imported buffers and retained snapshots on completion"]

[[cell]]
number = 122
name = "setfsuid"
status = "implemented"
contract = "linux-setfsuid"
handler = "kernel/src/syscall/sys.rs:sys_setfsuid"
conditional = "explicit-none"
tests = ["kernel/src/syscall/sys.rs:invalid_or_unmapped_setfsid_returns_old_without_calling_writer"]
validation_gaps = ["Direct Linux 7.2.3 guest entry namespace/capability and combined-error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-setfsgid"
flags = ["flag:native syscall integer widths; operation-specific identity resource or query selector"]
structs = ["struct:native uid/gid arrays and UTS/sysinfo/rlimit/rusage records where required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific admission and usercopy order; preserve visible prefix on ordered multi-output faults"]
usercopy = ["usercopy:request-owned input and initialized native output; scalar-only calls do not touch user memory"]
state = ["state:namespace-mapped real effective saved and filesystem identities or supplementary groups; credential admission precedes publication"]
concurrency = ["concurrency:retain credential namespace or process snapshot; mutations serialize through their owning subsystem"]
teardown = ["teardown:release imported buffers and retained snapshots on completion"]

[[cell]]
number = 123
name = "setfsgid"
status = "implemented"
contract = "linux-setfsgid"
handler = "kernel/src/syscall/sys.rs:sys_setfsgid"
conditional = "explicit-none"
tests = ["kernel/src/syscall/sys.rs:invalid_or_unmapped_setfsid_returns_old_without_calling_writer"]
validation_gaps = ["Direct Linux 7.2.3 guest entry namespace/capability and combined-error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-getgroups"
flags = ["flag:native syscall integer widths; operation-specific identity resource or query selector"]
structs = ["struct:native uid/gid arrays and UTS/sysinfo/rlimit/rusage records where required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific admission and usercopy order; preserve visible prefix on ordered multi-output faults"]
usercopy = ["usercopy:request-owned input and initialized native output; scalar-only calls do not touch user memory"]
state = ["state:namespace-mapped real effective saved and filesystem identities or supplementary groups; credential admission precedes publication"]
concurrency = ["concurrency:retain credential namespace or process snapshot; mutations serialize through their owning subsystem"]
teardown = ["teardown:release imported buffers and retained snapshots on completion"]

[[cell]]
number = 115
name = "getgroups"
status = "implemented"
contract = "linux-getgroups"
handler = "kernel/src/syscall/sys.rs:sys_getgroups"
conditional = "explicit-none"
tests = ["kernel/src/syscall/sys.rs:getgroups_zero_size_and_empty_groups_do_not_touch_pointer"]
validation_gaps = ["Direct Linux 7.2.3 guest entry namespace/capability and combined-error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-setgroups"
flags = ["flag:native syscall integer widths; operation-specific identity resource or query selector"]
structs = ["struct:native uid/gid arrays and UTS/sysinfo/rlimit/rusage records where required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific admission and usercopy order; preserve visible prefix on ordered multi-output faults"]
usercopy = ["usercopy:request-owned input and initialized native output; scalar-only calls do not touch user memory"]
state = ["state:namespace-mapped real effective saved and filesystem identities or supplementary groups; credential admission precedes publication"]
concurrency = ["concurrency:retain credential namespace or process snapshot; mutations serialize through their owning subsystem"]
teardown = ["teardown:release imported buffers and retained snapshots on completion"]

[[cell]]
number = 116
name = "setgroups"
status = "implemented"
contract = "linux-setgroups"
handler = "kernel/src/syscall/sys.rs:sys_setgroups"
conditional = "explicit-none"
tests = ["kernel/src/syscall/sys.rs:setgroups_stops_usercopy_at_first_unmapped_gid"]
validation_gaps = ["Direct Linux 7.2.3 guest entry namespace/capability and combined-error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-uname"
flags = ["flag:native syscall integer widths; operation-specific identity resource or query selector"]
structs = ["struct:native uid/gid arrays and UTS/sysinfo/rlimit/rusage records where required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific admission and usercopy order; preserve visible prefix on ordered multi-output faults"]
usercopy = ["usercopy:request-owned input and initialized native output; scalar-only calls do not touch user memory"]
state = ["state:UTS namespace names with native byte layout and personality override"]
concurrency = ["concurrency:retain credential namespace or process snapshot; mutations serialize through their owning subsystem"]
teardown = ["teardown:release imported buffers and retained snapshots on completion"]

[[cell]]
number = 63
name = "uname"
status = "implemented"
contract = "linux-uname"
handler = "kernel/src/syscall/sys.rs:sys_uname"
conditional = "explicit-none"
tests = ["kernel/src/syscall/sys.rs:uname26_second_copy_fault_keeps_native_prefix_visible_and_returns_efault"]
validation_gaps = ["Direct Linux 7.2.3 guest entry namespace/capability and combined-error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-sethostname"
flags = ["flag:native syscall integer widths; operation-specific identity resource or query selector"]
structs = ["struct:native uid/gid arrays and UTS/sysinfo/rlimit/rusage records where required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific admission and usercopy order; preserve visible prefix on ordered multi-output faults"]
usercopy = ["usercopy:request-owned input and initialized native output; scalar-only calls do not touch user memory"]
state = ["state:UTS namespace names with native byte layout and personality override"]
concurrency = ["concurrency:retain credential namespace or process snapshot; mutations serialize through their owning subsystem"]
teardown = ["teardown:release imported buffers and retained snapshots on completion"]

[[cell]]
number = 170
name = "sethostname"
status = "implemented"
contract = "linux-sethostname"
handler = "kernel/src/syscall/sys.rs:sys_sethostname"
conditional = "explicit-none"
tests = ["kernel/src/syscall/sys.rs:uts_name_len_matches_linux_int_abi"]
validation_gaps = ["Direct Linux 7.2.3 guest entry namespace/capability and combined-error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-setdomainname"
flags = ["flag:native syscall integer widths; operation-specific identity resource or query selector"]
structs = ["struct:native uid/gid arrays and UTS/sysinfo/rlimit/rusage records where required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific admission and usercopy order; preserve visible prefix on ordered multi-output faults"]
usercopy = ["usercopy:request-owned input and initialized native output; scalar-only calls do not touch user memory"]
state = ["state:UTS namespace names with native byte layout and personality override"]
concurrency = ["concurrency:retain credential namespace or process snapshot; mutations serialize through their owning subsystem"]
teardown = ["teardown:release imported buffers and retained snapshots on completion"]

[[cell]]
number = 171
name = "setdomainname"
status = "implemented"
contract = "linux-setdomainname"
handler = "kernel/src/syscall/sys.rs:sys_setdomainname"
conditional = "explicit-none"
tests = ["kernel/src/syscall/sys.rs:uts_bytes_before_nul_preserves_non_utf8_bytes"]
validation_gaps = ["Direct Linux 7.2.3 guest entry namespace/capability and combined-error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-personality"
flags = ["flag:native syscall integer widths; operation-specific identity resource or query selector"]
structs = ["struct:native uid/gid arrays and UTS/sysinfo/rlimit/rusage records where required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific admission and usercopy order; preserve visible prefix on ordered multi-output faults"]
usercopy = ["usercopy:request-owned input and initialized native output; scalar-only calls do not touch user memory"]
state = ["state:query or replace native u32 personality while returning the prior value"]
concurrency = ["concurrency:retain credential namespace or process snapshot; mutations serialize through their owning subsystem"]
teardown = ["teardown:release imported buffers and retained snapshots on completion"]

[[cell]]
number = 135
name = "personality"
status = "implemented"
contract = "linux-personality"
handler = "kernel/src/syscall/sys.rs:sys_personality"
conditional = "explicit-none"
tests = ["kernel/src/syscall/sys.rs:personality_namespace_preserves_arbitrary_native_u32_bits"]
validation_gaps = ["Direct Linux 7.2.3 guest entry namespace/capability and combined-error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-sysinfo"
flags = ["flag:native syscall integer widths; operation-specific identity resource or query selector"]
structs = ["struct:native uid/gid arrays and UTS/sysinfo/rlimit/rusage records where required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific admission and usercopy order; preserve visible prefix on ordered multi-output faults"]
usercopy = ["usercopy:request-owned input and initialized native output; scalar-only calls do not touch user memory"]
state = ["state:report live memory load uptime and process counts; absent swap and x86 highmem report zero"]
concurrency = ["concurrency:retain credential namespace or process snapshot; mutations serialize through their owning subsystem"]
teardown = ["teardown:release imported buffers and retained snapshots on completion"]

[[cell]]
number = 99
name = "sysinfo"
status = "implemented"
contract = "linux-sysinfo"
handler = "kernel/src/syscall/sys.rs:sys_sysinfo"
conditional = "explicit-none"
tests = ["kernel/src/syscall/sys.rs:sysinfo_memory_fields_use_bytes_and_only_shmem_pages"]
validation_gaps = ["Direct Linux 7.2.3 guest entry namespace/capability and combined-error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-prlimit64"
flags = ["flag:native syscall integer widths; operation-specific identity resource or query selector"]
structs = ["struct:native uid/gid arrays and UTS/sysinfo/rlimit/rusage records where required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific admission and usercopy order; preserve visible prefix on ordered multi-output faults"]
usercopy = ["usercopy:request-owned input and initialized native output; scalar-only calls do not touch user memory"]
state = ["state:read or replace process resource limits under ownership and capability checks; usage reads accounting ledger"]
concurrency = ["concurrency:retain credential namespace or process snapshot; mutations serialize through their owning subsystem"]
teardown = ["teardown:release imported buffers and retained snapshots on completion"]

[[cell]]
number = 302
name = "prlimit64"
status = "implemented"
contract = "linux-prlimit64"
handler = "kernel/src/syscall/resources.rs:sys_prlimit64"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest entry namespace/capability and combined-error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-setrlimit"
flags = ["flag:native syscall integer widths; operation-specific identity resource or query selector"]
structs = ["struct:native uid/gid arrays and UTS/sysinfo/rlimit/rusage records where required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific admission and usercopy order; preserve visible prefix on ordered multi-output faults"]
usercopy = ["usercopy:request-owned input and initialized native output; scalar-only calls do not touch user memory"]
state = ["state:read or replace process resource limits under ownership and capability checks; usage reads accounting ledger"]
concurrency = ["concurrency:retain credential namespace or process snapshot; mutations serialize through their owning subsystem"]
teardown = ["teardown:release imported buffers and retained snapshots on completion"]

[[cell]]
number = 160
name = "setrlimit"
status = "implemented"
contract = "linux-setrlimit"
handler = "kernel/src/syscall/resources.rs:sys_setrlimit"
conditional = "explicit-none"
tests = ["kernel/src/syscall/resources.rs:setrlimit_reads_all_16_bytes_and_maps_any_copy_failure_to_efault"]
validation_gaps = ["Direct Linux 7.2.3 guest entry namespace/capability and combined-error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-getrlimit"
flags = ["flag:native syscall integer widths; operation-specific identity resource or query selector"]
structs = ["struct:native uid/gid arrays and UTS/sysinfo/rlimit/rusage records where required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific admission and usercopy order; preserve visible prefix on ordered multi-output faults"]
usercopy = ["usercopy:request-owned input and initialized native output; scalar-only calls do not touch user memory"]
state = ["state:read or replace process resource limits under ownership and capability checks; usage reads accounting ledger"]
concurrency = ["concurrency:retain credential namespace or process snapshot; mutations serialize through their owning subsystem"]
teardown = ["teardown:release imported buffers and retained snapshots on completion"]

[[cell]]
number = 97
name = "getrlimit"
status = "implemented"
contract = "linux-getrlimit"
handler = "kernel/src/syscall/resources.rs:sys_getrlimit"
conditional = "explicit-none"
tests = ["kernel/src/syscall/resources.rs:getrlimit_snapshot_contains_the_complete_native_value"]
validation_gaps = ["Direct Linux 7.2.3 guest entry namespace/capability and combined-error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-getrusage"
flags = ["flag:native syscall integer widths; operation-specific identity resource or query selector"]
structs = ["struct:native uid/gid arrays and UTS/sysinfo/rlimit/rusage records where required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific admission and usercopy order; preserve visible prefix on ordered multi-output faults"]
usercopy = ["usercopy:request-owned input and initialized native output; scalar-only calls do not touch user memory"]
state = ["state:read or replace process resource limits under ownership and capability checks; usage reads accounting ledger"]
concurrency = ["concurrency:retain credential namespace or process snapshot; mutations serialize through their owning subsystem"]
teardown = ["teardown:release imported buffers and retained snapshots on completion"]

[[cell]]
number = 98
name = "getrusage"
status = "implemented"
contract = "linux-getrusage"
handler = "kernel/src/syscall/resources.rs:sys_getrusage"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest entry namespace/capability and combined-error matrix is not yet bound and executed"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-msgget"
flags = ["flag:IPC_CREAT EXCL NOWAIT; receive COPY EXCEPT NOERROR and signed type selection"]
structs = ["struct:native ipc_perm and msqid_ds msginfo and long-prefixed message bytes"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:resolve namespace object; permission and operation validation precede state publication; explicit usercopy errors preserve Linux-defined prefix effects"]
usercopy = ["usercopy:request-owned native records and bounded arrays; output snapshots copied after releasing owner locks"]
state = ["state:namespace-owned msg objects, creator/owner permissions and IPC_RMID lifecycle"]
concurrency = ["concurrency:retain object through removal races; wait queues and object lock serialize blocking operations"]
teardown = ["teardown:detach msg ownership and wake blocked users when object is removed; release imported buffers on errors"]

[[cell]]
number = 68
name = "msgget"
status = "partial"
contract = "linux-msgget"
handler = "kernel/src/syscall/ipc/msg.rs:sys_msgget"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest creation/removal/reuse, permissions and operation state matrix remains unexecuted"]
limitations = ["Flat IPC identifiers can be reused without sequence validation; a stale identifier can resolve to a successor object"]

[[contract]]
id = "linux-msgsnd"
flags = ["flag:IPC_CREAT EXCL NOWAIT; receive COPY EXCEPT NOERROR and signed type selection"]
structs = ["struct:native ipc_perm and msqid_ds msginfo and long-prefixed message bytes"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:resolve namespace object; permission and operation validation precede state publication; explicit usercopy errors preserve Linux-defined prefix effects"]
usercopy = ["usercopy:request-owned native records and bounded arrays; output snapshots copied after releasing owner locks"]
state = ["state:namespace-owned msg objects, creator/owner permissions and IPC_RMID lifecycle"]
concurrency = ["concurrency:retain object through removal races; wait queues and object lock serialize blocking operations"]
teardown = ["teardown:detach msg ownership and wake blocked users when object is removed; release imported buffers on errors"]

[[cell]]
number = 69
name = "msgsnd"
status = "partial"
contract = "linux-msgsnd"
handler = "kernel/src/syscall/ipc/msg.rs:sys_msgsnd"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest creation/removal/reuse, permissions and operation state matrix remains unexecuted"]
limitations = ["Flat IPC identifiers can be reused without sequence validation; a stale identifier can resolve to a successor object"]

[[contract]]
id = "linux-msgrcv"
flags = ["flag:IPC_CREAT EXCL NOWAIT; receive COPY EXCEPT NOERROR and signed type selection"]
structs = ["struct:native ipc_perm and msqid_ds msginfo and long-prefixed message bytes"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:resolve namespace object; permission and operation validation precede state publication; explicit usercopy errors preserve Linux-defined prefix effects"]
usercopy = ["usercopy:request-owned native records and bounded arrays; output snapshots copied after releasing owner locks"]
state = ["state:namespace-owned msg objects, creator/owner permissions and IPC_RMID lifecycle"]
concurrency = ["concurrency:retain object through removal races; wait queues and object lock serialize blocking operations"]
teardown = ["teardown:detach msg ownership and wake blocked users when object is removed; release imported buffers on errors"]

[[cell]]
number = 70
name = "msgrcv"
status = "partial"
contract = "linux-msgrcv"
handler = "kernel/src/syscall/ipc/msg.rs:sys_msgrcv"
conditional = "explicit-none"
tests = ["kernel/src/syscall/ipc/msg.rs:receive_fault_consumes_normal_message_before_copyout", "kernel/src/syscall/ipc/msg.rs:msg_copy_fault_leaves_message_queued"]
validation_gaps = ["Direct Linux 7.2.3 guest creation/removal/reuse, permissions and operation state matrix remains unexecuted"]
limitations = ["Flat IPC identifiers can be reused without sequence validation; a stale identifier can resolve to a successor object"]

[[contract]]
id = "linux-msgctl"
flags = ["flag:IPC_CREAT EXCL NOWAIT; receive COPY EXCEPT NOERROR and signed type selection"]
structs = ["struct:native ipc_perm and msqid_ds msginfo and long-prefixed message bytes"]
multiplexer_commands = ["mux:IPC_STAT SET RMID INFO MSG_STAT MSG_STAT_ANY MSG_INFO"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:resolve namespace object; permission and operation validation precede state publication; explicit usercopy errors preserve Linux-defined prefix effects"]
usercopy = ["usercopy:request-owned native records and bounded arrays; output snapshots copied after releasing owner locks"]
state = ["state:namespace-owned msg objects, creator/owner permissions and IPC_RMID lifecycle"]
concurrency = ["concurrency:retain object through removal races; wait queues and object lock serialize blocking operations"]
teardown = ["teardown:detach msg ownership and wake blocked users when object is removed; release imported buffers on errors"]

[[cell]]
number = 71
name = "msgctl"
status = "partial"
contract = "linux-msgctl"
handler = "kernel/src/syscall/ipc/msg.rs:sys_msgctl"
conditional = "explicit-none"
tests = ["kernel/src/syscall/ipc/msg.rs:credential_caller_msg_set_resource_failure_rolls_back_every_field"]
validation_gaps = ["Direct Linux 7.2.3 guest creation/removal/reuse, permissions and operation state matrix remains unexecuted"]
limitations = ["Flat IPC identifiers can be reused without sequence validation; a stale identifier can resolve to a successor object", "STAT/STAT_ANY treat the input as a complete flat identifier rather than Linux IDR index; INFO maximum-index reporting shares this representation"]

[[contract]]
id = "linux-semget"
flags = ["flag:IPC_CREAT EXCL NOWAIT and SEM_UNDO; semtimedop relative timeout"]
structs = ["struct:native ipc_perm and semid_ds seminfo sembuf arrays and semun payload"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:resolve namespace object; permission and operation validation precede state publication; explicit usercopy errors preserve Linux-defined prefix effects"]
usercopy = ["usercopy:request-owned native records and bounded arrays; output snapshots copied after releasing owner locks"]
state = ["state:namespace-owned sem objects, creator/owner permissions and IPC_RMID lifecycle"]
concurrency = ["concurrency:retain object through removal races; wait queues and object lock serialize blocking operations"]
teardown = ["teardown:detach sem ownership and wake blocked users when object is removed; release imported buffers on errors"]

[[cell]]
number = 64
name = "semget"
status = "partial"
contract = "linux-semget"
handler = "kernel/src/syscall/ipc/sem.rs:sys_semget"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest creation/removal/reuse, permissions and operation state matrix remains unexecuted"]
limitations = ["Flat IPC identifiers can be reused without sequence validation; a stale identifier can resolve to a successor object"]

[[contract]]
id = "linux-semop"
flags = ["flag:IPC_CREAT EXCL NOWAIT and SEM_UNDO; semtimedop relative timeout"]
structs = ["struct:native ipc_perm and semid_ds seminfo sembuf arrays and semun payload"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:resolve namespace object; permission and operation validation precede state publication; explicit usercopy errors preserve Linux-defined prefix effects"]
usercopy = ["usercopy:request-owned native records and bounded arrays; output snapshots copied after releasing owner locks"]
state = ["state:namespace-owned sem objects, creator/owner permissions and IPC_RMID lifecycle"]
concurrency = ["concurrency:retain object through removal races; wait queues and object lock serialize blocking operations"]
teardown = ["teardown:detach sem ownership and wake blocked users when object is removed; release imported buffers on errors"]

[[cell]]
number = 65
name = "semop"
status = "partial"
contract = "linux-semop"
handler = "kernel/src/syscall/ipc/sem.rs:sys_semop"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest creation/removal/reuse, permissions and operation state matrix remains unexecuted"]
limitations = ["Flat IPC identifiers can be reused without sequence validation; a stale identifier can resolve to a successor object"]

[[contract]]
id = "linux-semctl"
flags = ["flag:IPC_CREAT EXCL NOWAIT and SEM_UNDO; semtimedop relative timeout"]
structs = ["struct:native ipc_perm and semid_ds seminfo sembuf arrays and semun payload"]
multiplexer_commands = ["mux:IPC_STAT SET RMID INFO SEM_STAT SEM_STAT_ANY SEM_INFO GETVAL SETVAL GETALL SETALL GETPID GETNCNT GETZCNT"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:resolve namespace object; permission and operation validation precede state publication; explicit usercopy errors preserve Linux-defined prefix effects"]
usercopy = ["usercopy:request-owned native records and bounded arrays; output snapshots copied after releasing owner locks"]
state = ["state:namespace-owned sem objects, creator/owner permissions and IPC_RMID lifecycle"]
concurrency = ["concurrency:retain object through removal races; wait queues and object lock serialize blocking operations"]
teardown = ["teardown:detach sem ownership and wake blocked users when object is removed; release imported buffers on errors"]

[[cell]]
number = 66
name = "semctl"
status = "partial"
contract = "linux-semctl"
handler = "kernel/src/syscall/ipc/sem.rs:sys_semctl"
conditional = "explicit-none"
tests = ["kernel/src/syscall/ipc/sem.rs:setval_clears_all_prior_owners_only_for_the_selected_semaphore", "kernel/src/syscall/ipc/sem.rs:setall_clears_every_prior_undo_adjustment"]
validation_gaps = ["Direct Linux 7.2.3 guest creation/removal/reuse, permissions and operation state matrix remains unexecuted"]
limitations = ["Flat IPC identifiers can be reused without sequence validation; a stale identifier can resolve to a successor object", "STAT/STAT_ANY treat the input as a complete flat identifier rather than Linux IDR index; INFO maximum-index reporting shares this representation"]

[[contract]]
id = "linux-semtimedop"
flags = ["flag:IPC_CREAT EXCL NOWAIT and SEM_UNDO; semtimedop relative timeout"]
structs = ["struct:native ipc_perm and semid_ds seminfo sembuf arrays and semun payload"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:resolve namespace object; permission and operation validation precede state publication; explicit usercopy errors preserve Linux-defined prefix effects"]
usercopy = ["usercopy:request-owned native records and bounded arrays; output snapshots copied after releasing owner locks"]
state = ["state:namespace-owned sem objects, creator/owner permissions and IPC_RMID lifecycle"]
concurrency = ["concurrency:retain object through removal races; wait queues and object lock serialize blocking operations"]
teardown = ["teardown:detach sem ownership and wake blocked users when object is removed; release imported buffers on errors"]

[[cell]]
number = 220
name = "semtimedop"
status = "partial"
contract = "linux-semtimedop"
handler = "kernel/src/syscall/ipc/sem.rs:sys_semtimedop"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest creation/removal/reuse, permissions and operation state matrix remains unexecuted"]
limitations = ["Flat IPC identifiers can be reused without sequence validation; a stale identifier can resolve to a successor object"]

[[contract]]
id = "linux-shmget"
flags = ["flag:IPC_CREAT EXCL HUGETLB NORESERVE; attachment RDONLY RND REMAP EXEC"]
structs = ["struct:native ipc_perm and shmid_ds shminfo shm_info and page-aligned attachment address"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:resolve namespace object; permission and operation validation precede state publication; explicit usercopy errors preserve Linux-defined prefix effects"]
usercopy = ["usercopy:request-owned native records and bounded arrays; output snapshots copied after releasing owner locks"]
state = ["state:namespace-owned shm objects, creator/owner permissions and IPC_RMID lifecycle"]
concurrency = ["concurrency:retain object through removal races; wait queues and object lock serialize blocking operations"]
teardown = ["teardown:detach shm ownership and wake blocked users when object is removed; release imported buffers on errors"]

[[cell]]
number = 29
name = "shmget"
status = "partial"
contract = "linux-shmget"
handler = "kernel/src/syscall/ipc/shm.rs:sys_shmget"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest creation/removal/reuse, permissions and operation state matrix remains unexecuted"]
limitations = ["Flat IPC identifiers can be reused without sequence validation; a stale identifier can resolve to a successor object", "SHM_HUGETLB returns EINVAL and SHM_NORESERVE reservation policy is not implemented"]

[[contract]]
id = "linux-shmat"
flags = ["flag:IPC_CREAT EXCL HUGETLB NORESERVE; attachment RDONLY RND REMAP EXEC"]
structs = ["struct:native ipc_perm and shmid_ds shminfo shm_info and page-aligned attachment address"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:resolve namespace object; permission and operation validation precede state publication; explicit usercopy errors preserve Linux-defined prefix effects"]
usercopy = ["usercopy:request-owned native records and bounded arrays; output snapshots copied after releasing owner locks"]
state = ["state:namespace-owned shm objects, creator/owner permissions and IPC_RMID lifecycle"]
concurrency = ["concurrency:retain object through removal races; wait queues and object lock serialize blocking operations"]
teardown = ["teardown:detach shm ownership and wake blocked users when object is removed; release imported buffers on errors"]

[[cell]]
number = 30
name = "shmat"
status = "partial"
contract = "linux-shmat"
handler = "kernel/src/syscall/ipc/shm.rs:sys_shmat"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest creation/removal/reuse, permissions and operation state matrix remains unexecuted"]
limitations = ["Flat IPC identifiers can be reused without sequence validation; a stale identifier can resolve to a successor object"]

[[contract]]
id = "linux-shmctl"
flags = ["flag:IPC_CREAT EXCL HUGETLB NORESERVE; attachment RDONLY RND REMAP EXEC"]
structs = ["struct:native ipc_perm and shmid_ds shminfo shm_info and page-aligned attachment address"]
multiplexer_commands = ["mux:IPC_STAT SET RMID INFO SHM_STAT SHM_STAT_ANY SHM_INFO SHM_LOCK SHM_UNLOCK"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:resolve namespace object; permission and operation validation precede state publication; explicit usercopy errors preserve Linux-defined prefix effects"]
usercopy = ["usercopy:request-owned native records and bounded arrays; output snapshots copied after releasing owner locks"]
state = ["state:namespace-owned shm objects, creator/owner permissions and IPC_RMID lifecycle"]
concurrency = ["concurrency:retain object through removal races; wait queues and object lock serialize blocking operations"]
teardown = ["teardown:detach shm ownership and wake blocked users when object is removed; release imported buffers on errors"]

[[cell]]
number = 31
name = "shmctl"
status = "partial"
contract = "linux-shmctl"
handler = "kernel/src/syscall/ipc/shm.rs:sys_shmctl"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest creation/removal/reuse, permissions and operation state matrix remains unexecuted"]
limitations = ["Flat IPC identifiers can be reused without sequence validation; a stale identifier can resolve to a successor object", "STAT/STAT_ANY treat the input as a complete flat identifier rather than Linux IDR index; INFO maximum-index reporting shares this representation", "SHM_INFO reports reserved pages as resident pages instead of actual residency"]

[[contract]]
id = "linux-shmdt"
flags = ["flag:IPC_CREAT EXCL HUGETLB NORESERVE; attachment RDONLY RND REMAP EXEC"]
structs = ["struct:native ipc_perm and shmid_ds shminfo shm_info and page-aligned attachment address"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:resolve namespace object; permission and operation validation precede state publication; explicit usercopy errors preserve Linux-defined prefix effects"]
usercopy = ["usercopy:request-owned native records and bounded arrays; output snapshots copied after releasing owner locks"]
state = ["state:namespace-owned shm objects, creator/owner permissions and IPC_RMID lifecycle"]
concurrency = ["concurrency:retain object through removal races; wait queues and object lock serialize blocking operations"]
teardown = ["teardown:detach shm ownership and wake blocked users when object is removed; release imported buffers on errors"]

[[cell]]
number = 67
name = "shmdt"
status = "implemented"
contract = "linux-shmdt"
handler = "kernel/src/syscall/ipc/shm.rs:sys_shmdt"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest creation/removal/reuse, permissions and operation state matrix remains unexecuted"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-mq_open"
flags = ["flag:open access CREAT EXCL NONBLOCK CLOEXEC; attributes update only O_NONBLOCK; messages carry bounded priority"]
structs = ["struct:native mq_attr sigevent message priority and absolute realtime timespec"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:import bounded queue name or operation record; validate retained queue descriptor and mode; preserve dequeue-on-copyout-fault behavior"]
usercopy = ["usercopy:owned message and attr snapshots; copy priority and message according to receive ABI"]
state = ["state:IPC-namespace named message queues; per-OFD nonblocking state; one-shot notification registration"]
concurrency = ["concurrency:queue lock serializes insertion removal and notification ownership; readiness waits mediate blocking operations"]
teardown = ["teardown:unlinked queues survive retained descriptors; notification ownership is cancelled and refunded on owner exit"]

[[cell]]
number = 240
name = "mq_open"
status = "partial"
contract = "linux-mq_open"
handler = "kernel/src/syscall/ipc/mqueue.rs:sys_mq_open"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest queue permissions, blocking handoff, notification and resource-limit matrix remains unexecuted"]
limitations = ["Configured message count/size admission differs from Linux default and CAP_SYS_RESOURCE hard-limit bypass; RLIMIT_MSGQUEUE omits per-message and priority-tree overhead"]

[[contract]]
id = "linux-mq_unlink"
flags = ["flag:open access CREAT EXCL NONBLOCK CLOEXEC; attributes update only O_NONBLOCK; messages carry bounded priority"]
structs = ["struct:native mq_attr sigevent message priority and absolute realtime timespec"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:import bounded queue name or operation record; validate retained queue descriptor and mode; preserve dequeue-on-copyout-fault behavior"]
usercopy = ["usercopy:owned message and attr snapshots; copy priority and message according to receive ABI"]
state = ["state:IPC-namespace named message queues; per-OFD nonblocking state; one-shot notification registration"]
concurrency = ["concurrency:queue lock serializes insertion removal and notification ownership; readiness waits mediate blocking operations"]
teardown = ["teardown:unlinked queues survive retained descriptors; notification ownership is cancelled and refunded on owner exit"]

[[cell]]
number = 241
name = "mq_unlink"
status = "partial"
contract = "linux-mq_unlink"
handler = "kernel/src/syscall/ipc/mqueue.rs:sys_mq_unlink"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest queue permissions, blocking handoff, notification and resource-limit matrix remains unexecuted"]
limitations = ["Non-owner rejection returns EACCES rather than Linux sticky-directory EPERM; queue-owner check does not implement directory-owner unlink authority"]

[[contract]]
id = "linux-mq_timedsend"
flags = ["flag:open access CREAT EXCL NONBLOCK CLOEXEC; attributes update only O_NONBLOCK; messages carry bounded priority"]
structs = ["struct:native mq_attr sigevent message priority and absolute realtime timespec"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:import bounded queue name or operation record; validate retained queue descriptor and mode; preserve dequeue-on-copyout-fault behavior"]
usercopy = ["usercopy:owned message and attr snapshots; copy priority and message according to receive ABI"]
state = ["state:IPC-namespace named message queues; per-OFD nonblocking state; one-shot notification registration"]
concurrency = ["concurrency:queue lock serializes insertion removal and notification ownership; readiness waits mediate blocking operations"]
teardown = ["teardown:unlinked queues survive retained descriptors; notification ownership is cancelled and refunded on owner exit"]

[[cell]]
number = 242
name = "mq_timedsend"
status = "partial"
contract = "linux-mq_timedsend"
handler = "kernel/src/syscall/ipc/mqueue.rs:sys_mq_timedsend"
conditional = "explicit-none"
tests = []
validation_gaps = ["Direct Linux 7.2.3 guest queue permissions, blocking handoff, notification and resource-limit matrix remains unexecuted"]
limitations = ["No direct handoff to a blocked receiver; an empty-to-nonempty insertion may deliver mq_notify where Linux hands off without a notification"]

[[contract]]
id = "linux-mq_timedreceive"
flags = ["flag:open access CREAT EXCL NONBLOCK CLOEXEC; attributes update only O_NONBLOCK; messages carry bounded priority"]
structs = ["struct:native mq_attr sigevent message priority and absolute realtime timespec"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:import bounded queue name or operation record; validate retained queue descriptor and mode; preserve dequeue-on-copyout-fault behavior"]
usercopy = ["usercopy:owned message and attr snapshots; copy priority and message according to receive ABI"]
state = ["state:IPC-namespace named message queues; per-OFD nonblocking state; one-shot notification registration"]
concurrency = ["concurrency:queue lock serializes insertion removal and notification ownership; readiness waits mediate blocking operations"]
teardown = ["teardown:unlinked queues survive retained descriptors; notification ownership is cancelled and refunded on owner exit"]

[[cell]]
number = 243
name = "mq_timedreceive"
status = "partial"
contract = "linux-mq_timedreceive"
handler = "kernel/src/syscall/ipc/mqueue.rs:sys_mq_timedreceive"
conditional = "explicit-none"
tests = ["kernel/src/syscall/ipc/mqueue.rs:receive_copyout_fault_wakes_sender_after_freeing_capacity"]
validation_gaps = ["Direct Linux 7.2.3 guest queue permissions, blocking handoff, notification and resource-limit matrix remains unexecuted"]
limitations = ["Dequeued capacity wakes competing senders instead of transferring the slot through Linux priority-ordered pipelined sender handoff"]

[[contract]]
id = "linux-mq_notify"
flags = ["flag:open access CREAT EXCL NONBLOCK CLOEXEC; attributes update only O_NONBLOCK; messages carry bounded priority"]
structs = ["struct:native mq_attr sigevent message priority and absolute realtime timespec"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:import bounded queue name or operation record; validate retained queue descriptor and mode; preserve dequeue-on-copyout-fault behavior"]
usercopy = ["usercopy:owned message and attr snapshots; copy priority and message according to receive ABI"]
state = ["state:IPC-namespace named message queues; per-OFD nonblocking state; one-shot notification registration"]
concurrency = ["concurrency:queue lock serializes insertion removal and notification ownership; readiness waits mediate blocking operations"]
teardown = ["teardown:unlinked queues survive retained descriptors; notification ownership is cancelled and refunded on owner exit"]

[[cell]]
number = 244
name = "mq_notify"
status = "partial"
contract = "linux-mq_notify"
handler = "kernel/src/syscall/ipc/mqueue.rs:sys_mq_notify"
conditional = "explicit-none"
tests = ["kernel/src/syscall/ipc/mqueue.rs:owner_exit_cancels_unlinked_but_open_notification"]
validation_gaps = ["Direct Linux 7.2.3 guest queue permissions, blocking handoff, notification and resource-limit matrix remains unexecuted"]
limitations = ["Notification can fire for a message consumed by an already waiting receiver because send does not use direct handoff"]

[[contract]]
id = "linux-mq_getsetattr"
flags = ["flag:open access CREAT EXCL NONBLOCK CLOEXEC; attributes update only O_NONBLOCK; messages carry bounded priority"]
structs = ["struct:native mq_attr sigevent message priority and absolute realtime timespec"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:import bounded queue name or operation record; validate retained queue descriptor and mode; preserve dequeue-on-copyout-fault behavior"]
usercopy = ["usercopy:owned message and attr snapshots; copy priority and message according to receive ABI"]
state = ["state:IPC-namespace named message queues; per-OFD nonblocking state; one-shot notification registration"]
concurrency = ["concurrency:queue lock serializes insertion removal and notification ownership; readiness waits mediate blocking operations"]
teardown = ["teardown:unlinked queues survive retained descriptors; notification ownership is cancelled and refunded on owner exit"]

[[cell]]
number = 245
name = "mq_getsetattr"
status = "implemented"
contract = "linux-mq_getsetattr"
handler = "kernel/src/syscall/ipc/mqueue.rs:sys_mq_getsetattr"
conditional = "explicit-none"
tests = ["kernel/src/syscall/ipc/mqueue.rs:mqueue_usercopy_helpers_snapshot_unaligned_inputs"]
validation_gaps = ["Direct Linux 7.2.3 guest queue permissions, blocking handoff, notification and resource-limit matrix remains unexecuted"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-getitimer"
flags = ["flag:clock id and timer flags; absolute/relative deadlines; timex adjustment mode bits"]
structs = ["struct:native timeval timezone timespec itimerspec itimerval sigevent timex or tms as required"]
multiplexer_commands = ["mux:clock id selects realtime monotonic CPU or timer clock domain"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific scalar and copy-in validation before state admission; snapshot outputs use explicit usercopy errors"]
usercopy = ["usercopy:owned native records and bounded byte outputs; scalar-only operations avoid user access"]
state = ["state:read time or accounting; retain process timer identity; setters update time/timer ownership and delivery state"]
concurrency = ["concurrency:owner locks retain coherent state across competing threads and asynchronous timer or signal delivery"]
teardown = ["teardown:release imported data and temporary references; timer deletion cancels outstanding ownership"]

[[cell]]
number = 36
name = "getitimer"
status = "implemented"
contract = "linux-getitimer"
handler = "kernel/src/syscall/time.rs:sys_getitimer"
conditional = "explicit-none"
tests = ["tests/guest/tools/wait-boundary.c:test_itimer_usercopy_semantics"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest matrix for this entry remains pending; helper tests do not establish full runtime coverage"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-alarm"
flags = ["flag:clock id and timer flags; absolute/relative deadlines; timex adjustment mode bits"]
structs = ["struct:native timeval timezone timespec itimerspec itimerval sigevent timex or tms as required"]
multiplexer_commands = ["mux:clock id selects realtime monotonic CPU or timer clock domain"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific scalar and copy-in validation before state admission; snapshot outputs use explicit usercopy errors"]
usercopy = ["usercopy:owned native records and bounded byte outputs; scalar-only operations avoid user access"]
state = ["state:read time or accounting; retain process timer identity; setters update time/timer ownership and delivery state"]
concurrency = ["concurrency:owner locks retain coherent state across competing threads and asynchronous timer or signal delivery"]
teardown = ["teardown:release imported data and temporary references; timer deletion cancels outstanding ownership"]

[[cell]]
number = 37
name = "alarm"
status = "implemented"
contract = "linux-alarm"
handler = "kernel/src/syscall/time.rs:sys_alarm"
conditional = "explicit-none"
tests = ["kernel/src/syscall/time.rs:alarm_remaining_seconds_matches_linux_half_second_rule_and_wraps"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest matrix for this entry remains pending; helper tests do not establish full runtime coverage"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-setitimer"
flags = ["flag:clock id and timer flags; absolute/relative deadlines; timex adjustment mode bits"]
structs = ["struct:native timeval timezone timespec itimerspec itimerval sigevent timex or tms as required"]
multiplexer_commands = ["mux:clock id selects realtime monotonic CPU or timer clock domain"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific scalar and copy-in validation before state admission; snapshot outputs use explicit usercopy errors"]
usercopy = ["usercopy:owned native records and bounded byte outputs; scalar-only operations avoid user access"]
state = ["state:read time or accounting; retain process timer identity; setters update time/timer ownership and delivery state"]
concurrency = ["concurrency:owner locks retain coherent state across competing threads and asynchronous timer or signal delivery"]
teardown = ["teardown:release imported data and temporary references; timer deletion cancels outstanding ownership"]

[[cell]]
number = 38
name = "setitimer"
status = "implemented"
contract = "linux-setitimer"
handler = "kernel/src/syscall/time.rs:sys_setitimer"
conditional = "explicit-none"
tests = ["tests/guest/tools/wait-boundary.c:test_itimer_periodic"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest matrix for this entry remains pending; helper tests do not establish full runtime coverage"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-gettimeofday"
flags = ["flag:clock id and timer flags; absolute/relative deadlines; timex adjustment mode bits"]
structs = ["struct:native timeval timezone timespec itimerspec itimerval sigevent timex or tms as required"]
multiplexer_commands = ["mux:clock id selects realtime monotonic CPU or timer clock domain"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific scalar and copy-in validation before state admission; snapshot outputs use explicit usercopy errors"]
usercopy = ["usercopy:owned native records and bounded byte outputs; scalar-only operations avoid user access"]
state = ["state:read time or accounting; retain process timer identity; setters update time/timer ownership and delivery state"]
concurrency = ["concurrency:owner locks retain coherent state across competing threads and asynchronous timer or signal delivery"]
teardown = ["teardown:release imported data and temporary references; timer deletion cancels outstanding ownership"]

[[cell]]
number = 96
name = "gettimeofday"
status = "partial"
contract = "linux-gettimeofday"
handler = "kernel/src/syscall/time.rs:sys_gettimeofday"
conditional = "explicit-none"
tests = []
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest matrix for this entry remains pending; helper tests do not establish full runtime coverage"]
limitations = ["Timezone output is always zero instead of the timezone retained by settimeofday"]

[[contract]]
id = "linux-times"
flags = ["flag:clock id and timer flags; absolute/relative deadlines; timex adjustment mode bits"]
structs = ["struct:native timeval timezone timespec itimerspec itimerval sigevent timex or tms as required"]
multiplexer_commands = ["mux:clock id selects realtime monotonic CPU or timer clock domain"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific scalar and copy-in validation before state admission; snapshot outputs use explicit usercopy errors"]
usercopy = ["usercopy:owned native records and bounded byte outputs; scalar-only operations avoid user access"]
state = ["state:read time or accounting; retain process timer identity; setters update time/timer ownership and delivery state"]
concurrency = ["concurrency:owner locks retain coherent state across competing threads and asynchronous timer or signal delivery"]
teardown = ["teardown:release imported data and temporary references; timer deletion cancels outstanding ownership"]

[[cell]]
number = 100
name = "times"
status = "implemented"
contract = "linux-times"
handler = "kernel/src/syscall/time.rs:sys_times"
conditional = "explicit-none"
tests = []
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest matrix for this entry remains pending; helper tests do not establish full runtime coverage"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-adjtimex"
flags = ["flag:clock id and timer flags; absolute/relative deadlines; timex adjustment mode bits"]
structs = ["struct:native timeval timezone timespec itimerspec itimerval sigevent timex or tms as required"]
multiplexer_commands = ["mux:clock id selects realtime monotonic CPU or timer clock domain"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific scalar and copy-in validation before state admission; snapshot outputs use explicit usercopy errors"]
usercopy = ["usercopy:owned native records and bounded byte outputs; scalar-only operations avoid user access"]
state = ["state:read time or accounting; retain process timer identity; setters update time/timer ownership and delivery state"]
concurrency = ["concurrency:owner locks retain coherent state across competing threads and asynchronous timer or signal delivery"]
teardown = ["teardown:release imported data and temporary references; timer deletion cancels outstanding ownership"]

[[cell]]
number = 159
name = "adjtimex"
status = "partial"
contract = "linux-adjtimex"
handler = "kernel/src/syscall/time.rs:sys_adjtimex"
conditional = "explicit-none"
tests = ["kernel/src/syscall/time.rs:timex_planner_accepts_legacy_singleshot_modes"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest matrix for this entry remains pending; helper tests do not establish full runtime coverage"]
limitations = ["ADJ_TAI and ADJ_SETOFFSET are rejected; offset frequency and tick changes only update metadata rather than timekeeping; error/timeconstant clamps differ"]

[[contract]]
id = "linux-settimeofday"
flags = ["flag:clock id and timer flags; absolute/relative deadlines; timex adjustment mode bits"]
structs = ["struct:native timeval timezone timespec itimerspec itimerval sigevent timex or tms as required"]
multiplexer_commands = ["mux:clock id selects realtime monotonic CPU or timer clock domain"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific scalar and copy-in validation before state admission; snapshot outputs use explicit usercopy errors"]
usercopy = ["usercopy:owned native records and bounded byte outputs; scalar-only operations avoid user access"]
state = ["state:read time or accounting; retain process timer identity; setters update time/timer ownership and delivery state"]
concurrency = ["concurrency:owner locks retain coherent state across competing threads and asynchronous timer or signal delivery"]
teardown = ["teardown:release imported data and temporary references; timer deletion cancels outstanding ownership"]

[[cell]]
number = 164
name = "settimeofday"
status = "partial"
contract = "linux-settimeofday"
handler = "kernel/src/syscall/time.rs:sys_settimeofday"
conditional = "explicit-none"
tests = []
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest matrix for this entry remains pending; helper tests do not establish full runtime coverage"]
limitations = ["Timezone input is discarded; oversized wall time is saturated rather than rejected by Linux set-to-time bounds"]

[[contract]]
id = "linux-timer_create"
flags = ["flag:clock id and timer flags; absolute/relative deadlines; timex adjustment mode bits"]
structs = ["struct:native timeval timezone timespec itimerspec itimerval sigevent timex or tms as required"]
multiplexer_commands = ["mux:clock id selects realtime monotonic CPU or timer clock domain"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific scalar and copy-in validation before state admission; snapshot outputs use explicit usercopy errors"]
usercopy = ["usercopy:owned native records and bounded byte outputs; scalar-only operations avoid user access"]
state = ["state:read time or accounting; retain process timer identity; setters update time/timer ownership and delivery state"]
concurrency = ["concurrency:owner locks retain coherent state across competing threads and asynchronous timer or signal delivery"]
teardown = ["teardown:release imported data and temporary references; timer deletion cancels outstanding ownership"]

[[cell]]
number = 222
name = "timer_create"
status = "partial"
contract = "linux-timer_create"
handler = "kernel/src/syscall/time.rs:sys_timer_create"
conditional = "explicit-none"
tests = ["tests/guest/portable/timer-create-validation.c:main"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest matrix for this entry remains pending; helper tests do not establish full runtime coverage"]
limitations = ["ALARM timer admission lacks RTC availability and CAP_WAKE_ALARM checks"]

[[contract]]
id = "linux-timer_settime"
flags = ["flag:clock id and timer flags; absolute/relative deadlines; timex adjustment mode bits"]
structs = ["struct:native timeval timezone timespec itimerspec itimerval sigevent timex or tms as required"]
multiplexer_commands = ["mux:clock id selects realtime monotonic CPU or timer clock domain"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific scalar and copy-in validation before state admission; snapshot outputs use explicit usercopy errors"]
usercopy = ["usercopy:owned native records and bounded byte outputs; scalar-only operations avoid user access"]
state = ["state:read time or accounting; retain process timer identity; setters update time/timer ownership and delivery state"]
concurrency = ["concurrency:owner locks retain coherent state across competing threads and asynchronous timer or signal delivery"]
teardown = ["teardown:release imported data and temporary references; timer deletion cancels outstanding ownership"]

[[cell]]
number = 223
name = "timer_settime"
status = "implemented"
contract = "linux-timer_settime"
handler = "kernel/src/syscall/time.rs:sys_timer_settime"
conditional = "explicit-none"
tests = ["tests/guest/tools/signal-wait-boundary.c:test_unaligned_posix_timer_usercopy"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest matrix for this entry remains pending; helper tests do not establish full runtime coverage"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-timer_gettime"
flags = ["flag:clock id and timer flags; absolute/relative deadlines; timex adjustment mode bits"]
structs = ["struct:native timeval timezone timespec itimerspec itimerval sigevent timex or tms as required"]
multiplexer_commands = ["mux:clock id selects realtime monotonic CPU or timer clock domain"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific scalar and copy-in validation before state admission; snapshot outputs use explicit usercopy errors"]
usercopy = ["usercopy:owned native records and bounded byte outputs; scalar-only operations avoid user access"]
state = ["state:read time or accounting; retain process timer identity; setters update time/timer ownership and delivery state"]
concurrency = ["concurrency:owner locks retain coherent state across competing threads and asynchronous timer or signal delivery"]
teardown = ["teardown:release imported data and temporary references; timer deletion cancels outstanding ownership"]

[[cell]]
number = 224
name = "timer_gettime"
status = "implemented"
contract = "linux-timer_gettime"
handler = "kernel/src/syscall/time.rs:sys_timer_gettime"
conditional = "explicit-none"
tests = ["tests/guest/tools/signal-wait-boundary.c:test_unaligned_posix_timer_usercopy"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest matrix for this entry remains pending; helper tests do not establish full runtime coverage"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-timer_getoverrun"
flags = ["flag:clock id and timer flags; absolute/relative deadlines; timex adjustment mode bits"]
structs = ["struct:native timeval timezone timespec itimerspec itimerval sigevent timex or tms as required"]
multiplexer_commands = ["mux:clock id selects realtime monotonic CPU or timer clock domain"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific scalar and copy-in validation before state admission; snapshot outputs use explicit usercopy errors"]
usercopy = ["usercopy:owned native records and bounded byte outputs; scalar-only operations avoid user access"]
state = ["state:read time or accounting; retain process timer identity; setters update time/timer ownership and delivery state"]
concurrency = ["concurrency:owner locks retain coherent state across competing threads and asynchronous timer or signal delivery"]
teardown = ["teardown:release imported data and temporary references; timer deletion cancels outstanding ownership"]

[[cell]]
number = 225
name = "timer_getoverrun"
status = "partial"
contract = "linux-timer_getoverrun"
handler = "kernel/src/syscall/time.rs:sys_timer_getoverrun"
conditional = "explicit-none"
tests = []
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest matrix for this entry remains pending; helper tests do not establish full runtime coverage"]
limitations = ["Returns the currently accumulating overrun while a signal remains pending instead of the last delivered signal overrun"]

[[contract]]
id = "linux-timer_delete"
flags = ["flag:clock id and timer flags; absolute/relative deadlines; timex adjustment mode bits"]
structs = ["struct:native timeval timezone timespec itimerspec itimerval sigevent timex or tms as required"]
multiplexer_commands = ["mux:clock id selects realtime monotonic CPU or timer clock domain"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific scalar and copy-in validation before state admission; snapshot outputs use explicit usercopy errors"]
usercopy = ["usercopy:owned native records and bounded byte outputs; scalar-only operations avoid user access"]
state = ["state:read time or accounting; retain process timer identity; setters update time/timer ownership and delivery state"]
concurrency = ["concurrency:owner locks retain coherent state across competing threads and asynchronous timer or signal delivery"]
teardown = ["teardown:release imported data and temporary references; timer deletion cancels outstanding ownership"]

[[cell]]
number = 226
name = "timer_delete"
status = "implemented"
contract = "linux-timer_delete"
handler = "kernel/src/syscall/time.rs:sys_timer_delete"
conditional = "explicit-none"
tests = ["tests/guest/tools/signal-wait-boundary.c:test_unaligned_posix_timer_usercopy"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest matrix for this entry remains pending; helper tests do not establish full runtime coverage"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-clock_settime"
flags = ["flag:clock id and timer flags; absolute/relative deadlines; timex adjustment mode bits"]
structs = ["struct:native timeval timezone timespec itimerspec itimerval sigevent timex or tms as required"]
multiplexer_commands = ["mux:clock id selects realtime monotonic CPU or timer clock domain"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific scalar and copy-in validation before state admission; snapshot outputs use explicit usercopy errors"]
usercopy = ["usercopy:owned native records and bounded byte outputs; scalar-only operations avoid user access"]
state = ["state:read time or accounting; retain process timer identity; setters update time/timer ownership and delivery state"]
concurrency = ["concurrency:owner locks retain coherent state across competing threads and asynchronous timer or signal delivery"]
teardown = ["teardown:release imported data and temporary references; timer deletion cancels outstanding ownership"]

[[cell]]
number = 227
name = "clock_settime"
status = "partial"
contract = "linux-clock_settime"
handler = "kernel/src/syscall/time.rs:sys_clock_settime"
conditional = "explicit-none"
tests = []
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest matrix for this entry remains pending; helper tests do not establish full runtime coverage"]
limitations = ["Wall-time setter lacks Linux set-to-time range and wall-to-monotonic validation"]

[[contract]]
id = "linux-clock_gettime"
flags = ["flag:clock id and timer flags; absolute/relative deadlines; timex adjustment mode bits"]
structs = ["struct:native timeval timezone timespec itimerspec itimerval sigevent timex or tms as required"]
multiplexer_commands = ["mux:clock id selects realtime monotonic CPU or timer clock domain"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific scalar and copy-in validation before state admission; snapshot outputs use explicit usercopy errors"]
usercopy = ["usercopy:owned native records and bounded byte outputs; scalar-only operations avoid user access"]
state = ["state:read time or accounting; retain process timer identity; setters update time/timer ownership and delivery state"]
concurrency = ["concurrency:owner locks retain coherent state across competing threads and asynchronous timer or signal delivery"]
teardown = ["teardown:release imported data and temporary references; timer deletion cancels outstanding ownership"]

[[cell]]
number = 228
name = "clock_gettime"
status = "partial"
contract = "linux-clock_gettime"
handler = "kernel/src/syscall/time.rs:sys_clock_gettime"
conditional = "explicit-none"
tests = ["kernel/src/syscall/time.rs:clock_domain_accepts_linux_encoded_cpu_clock_ids"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest matrix for this entry remains pending; helper tests do not establish full runtime coverage"]
limitations = ["CLOCK_MONOTONIC_RAW omits the Linux time namespace monotonic offset; ALARM clocks do not check RTC availability"]

[[contract]]
id = "linux-clock_getres"
flags = ["flag:clock id and timer flags; absolute/relative deadlines; timex adjustment mode bits"]
structs = ["struct:native timeval timezone timespec itimerspec itimerval sigevent timex or tms as required"]
multiplexer_commands = ["mux:clock id selects realtime monotonic CPU or timer clock domain"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific scalar and copy-in validation before state admission; snapshot outputs use explicit usercopy errors"]
usercopy = ["usercopy:owned native records and bounded byte outputs; scalar-only operations avoid user access"]
state = ["state:read time or accounting; retain process timer identity; setters update time/timer ownership and delivery state"]
concurrency = ["concurrency:owner locks retain coherent state across competing threads and asynchronous timer or signal delivery"]
teardown = ["teardown:release imported data and temporary references; timer deletion cancels outstanding ownership"]

[[cell]]
number = 229
name = "clock_getres"
status = "partial"
contract = "linux-clock_getres"
handler = "kernel/src/syscall/time.rs:sys_clock_getres"
conditional = "explicit-none"
tests = ["kernel/src/syscall/time.rs:clock_resolution_requires_supported_clock"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest matrix for this entry remains pending; helper tests do not establish full runtime coverage"]
limitations = ["Encoded CPU PROF/VIRT clocks return one nanosecond instead of the accounting tick resolution; ALARM clocks omit RTC availability checks"]

[[contract]]
id = "linux-clock_adjtime"
flags = ["flag:clock id and timer flags; absolute/relative deadlines; timex adjustment mode bits"]
structs = ["struct:native timeval timezone timespec itimerspec itimerval sigevent timex or tms as required"]
multiplexer_commands = ["mux:clock id selects realtime monotonic CPU or timer clock domain"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific scalar and copy-in validation before state admission; snapshot outputs use explicit usercopy errors"]
usercopy = ["usercopy:owned native records and bounded byte outputs; scalar-only operations avoid user access"]
state = ["state:read time or accounting; retain process timer identity; setters update time/timer ownership and delivery state"]
concurrency = ["concurrency:owner locks retain coherent state across competing threads and asynchronous timer or signal delivery"]
teardown = ["teardown:release imported data and temporary references; timer deletion cancels outstanding ownership"]

[[cell]]
number = 305
name = "clock_adjtime"
status = "partial"
contract = "linux-clock_adjtime"
handler = "kernel/src/syscall/time.rs:sys_clock_adjtime"
conditional = "explicit-none"
tests = ["kernel/src/syscall/time.rs:clock_adjtime_copies_timex_before_classifying_clock"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest matrix for this entry remains pending; helper tests do not establish full runtime coverage"]
limitations = ["ADJ_TAI and ADJ_SETOFFSET are rejected; offset frequency and tick changes only update metadata rather than timekeeping; error/timeconstant clamps differ"]

[[contract]]
id = "linux-syslog"
flags = ["flag:syslog action and signed buffer length"]
structs = ["struct:byte buffer and ring read/clear cursor"]
multiplexer_commands = ["mux:syslog action selects read clear or console policy"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific scalar and copy-in validation before state admission; snapshot outputs use explicit usercopy errors"]
usercopy = ["usercopy:owned native records and bounded byte outputs; scalar-only operations avoid user access"]
state = ["state:serialized kernel log snapshot and read/clear cursor"]
concurrency = ["concurrency:owner locks retain coherent state across competing threads and asynchronous timer or signal delivery"]
teardown = ["teardown:release imported data and temporary references; timer deletion cancels outstanding ownership"]

[[cell]]
number = 103
name = "syslog"
status = "partial"
contract = "linux-syslog"
handler = "kernel/src/syscall/sys.rs:sys_syslog"
conditional = "explicit-none"
tests = ["kernel/src/syscall/sys.rs:syslog_zero_length_copy_returns_without_user_memory_access"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest matrix for this entry remains pending; helper tests do not establish full runtime coverage"]
limitations = ["READ READ_ALL READ_CLEAR with NULL output and zero length returns zero instead of Linux EINVAL"]

[[contract]]
id = "linux-restart_syscall"
flags = ["flag:implicit per-thread restart block"]
structs = ["struct:saved syscall arguments and user register context"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific scalar and copy-in validation before state admission; snapshot outputs use explicit usercopy errors"]
usercopy = ["usercopy:owned native records and bounded byte outputs; scalar-only operations avoid user access"]
state = ["state:consume the current thread restart block and re-enter its operation"]
concurrency = ["concurrency:owner locks retain coherent state across competing threads and asynchronous timer or signal delivery"]
teardown = ["teardown:release imported data and temporary references; timer deletion cancels outstanding ownership"]

[[cell]]
number = 219
name = "restart_syscall"
status = "partial"
contract = "linux-restart_syscall"
handler = "kernel/src/syscall/sys.rs:sys_restart_syscall"
conditional = "explicit-none"
tests = ["tests/guest/portable/signal-boundary-differential.c:main"]
validation_gaps = ["New raw signal/restart boundary assertions passed host Linux; matched Linux 7.2.3 and TheKernel guest execution remains pending"]
limitations = ["Only FutexWait restart blocks are represented; nanosleep and poll restart-block lifecycles remain unsupported"]

[[contract]]
id = "linux-getrandom"
flags = ["flag:GRND_RANDOM GRND_NONBLOCK GRND_INSECURE"]
structs = ["struct:length-delimited random output bytes"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:operation-specific scalar and copy-in validation before state admission; snapshot outputs use explicit usercopy errors"]
usercopy = ["usercopy:owned native records and bounded byte outputs; scalar-only operations avoid user access"]
state = ["state:secure or explicitly insecure random stream with partial output accounting"]
concurrency = ["concurrency:owner locks retain coherent state across competing threads and asynchronous timer or signal delivery"]
teardown = ["teardown:release imported data and temporary references; timer deletion cancels outstanding ownership"]

[[cell]]
number = 318
name = "getrandom"
status = "partial"
contract = "linux-getrandom"
handler = "kernel/src/syscall/sys.rs:sys_getrandom"
conditional = "explicit-none"
tests = []
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest matrix for this entry remains pending; helper tests do not establish full runtime coverage"]
limitations = ["When secure entropy is not ready the blocking flags-zero call returns EAGAIN rather than waiting; GRND_NONBLOCK does not select behavior"]

[[contract]]
id = "linux-getpid"
flags = ["explicit-none"]
structs = ["struct:namespace-visible pid/tid and deferred clear_child_tid address"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["explicit-none"]
usercopy = ["explicit-none"]
state = ["state:return caller-visible thread-group ID"]
concurrency = ["concurrency:namespace and task identity snapshots remain valid through parent or target exit"]
teardown = ["teardown:identity queries release snapshots; clear-child-tid is consumed during thread exit"]

[[cell]]
number = 39
name = "getpid"
status = "implemented"
contract = "linux-getpid"
handler = "kernel/src/syscall/task/thread.rs:sys_getpid"
conditional = "explicit-none"
tests = []
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, namespace and failure-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-getppid"
flags = ["explicit-none"]
structs = ["struct:namespace-visible pid/tid and deferred clear_child_tid address"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["explicit-none"]
usercopy = ["explicit-none"]
state = ["state:return caller-visible parent ID or zero for an invisible parent"]
concurrency = ["concurrency:namespace and task identity snapshots remain valid through parent or target exit"]
teardown = ["teardown:identity queries release snapshots; clear-child-tid is consumed during thread exit"]

[[cell]]
number = 110
name = "getppid"
status = "implemented"
contract = "linux-getppid"
handler = "kernel/src/syscall/task/thread.rs:sys_getppid"
conditional = "explicit-none"
tests = ["kernel/src/syscall/task/thread.rs:getppid_parent_identity_obeys_pid_namespace_visibility"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, namespace and failure-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-gettid"
flags = ["explicit-none"]
structs = ["struct:namespace-visible pid/tid and deferred clear_child_tid address"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["explicit-none"]
usercopy = ["explicit-none"]
state = ["state:return caller-visible task ID"]
concurrency = ["concurrency:namespace and task identity snapshots remain valid through parent or target exit"]
teardown = ["teardown:identity queries release snapshots; clear-child-tid is consumed during thread exit"]

[[cell]]
number = 186
name = "gettid"
status = "implemented"
contract = "linux-gettid"
handler = "kernel/src/syscall/task/thread.rs:sys_gettid"
conditional = "explicit-none"
tests = []
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, namespace and failure-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-set_tid_address"
flags = ["explicit-none"]
structs = ["struct:namespace-visible pid/tid and deferred clear_child_tid address"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["explicit-none"]
usercopy = ["explicit-none"]
state = ["state:retain raw clear-child-tid address without probing it and return visible TID"]
concurrency = ["concurrency:namespace and task identity snapshots remain valid through parent or target exit"]
teardown = ["teardown:identity queries release snapshots; clear-child-tid is consumed during thread exit"]

[[cell]]
number = 218
name = "set_tid_address"
status = "implemented"
contract = "linux-set_tid_address"
handler = "kernel/src/syscall/task/thread.rs:sys_set_tid_address"
conditional = "explicit-none"
tests = []
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, namespace and failure-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-rt_sigaction"
flags = ["flag:native signal number; signal-mask operation or signal-action/alternate-stack flags where applicable"]
structs = ["struct:native 64-bit signal set, sigaction, siginfo, stack_t and x86_64 signal frame as required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific sigset size, snapshot, signal and target validation; output faults preserve already committed action or alternate stack"]
usercopy = ["usercopy:owned signal records and exact native signal-set copies; ordered frame restore and output-prefix errors"]
state = ["state:retain signal target authority; manipulate mask action queue or alternate stack; wait/return paths preserve caught-signal and restart state"]
concurrency = ["concurrency:signal manager and target authority coordinate delivery with mask changes, exit and queue ownership"]
teardown = ["teardown:consume delivered queue entries, restore masks/context, and release frame or queued-signal reservations"]

[[cell]]
number = 13
name = "rt_sigaction"
status = "implemented"
contract = "linux-rt_sigaction"
handler = "kernel/src/syscall/signal.rs:sys_rt_sigaction"
conditional = "explicit-none"
tests = ["tests/guest/portable/signal-boundary-differential.c:main"]
validation_gaps = ["Current paired boundary cases pass, but the broader signal entry, namespace and lifecycle matrix remains unverified"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-rt_sigprocmask"
flags = ["flag:native signal number; signal-mask operation or signal-action/alternate-stack flags where applicable"]
structs = ["struct:native 64-bit signal set, sigaction, siginfo, stack_t and x86_64 signal frame as required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific sigset size, snapshot, signal and target validation; output faults preserve already committed action or alternate stack"]
usercopy = ["usercopy:owned signal records and exact native signal-set copies; ordered frame restore and output-prefix errors"]
state = ["state:retain signal target authority; manipulate mask action queue or alternate stack; wait/return paths preserve caught-signal and restart state"]
concurrency = ["concurrency:signal manager and target authority coordinate delivery with mask changes, exit and queue ownership"]
teardown = ["teardown:consume delivered queue entries, restore masks/context, and release frame or queued-signal reservations"]

[[cell]]
number = 14
name = "rt_sigprocmask"
status = "implemented"
contract = "linux-rt_sigprocmask"
handler = "kernel/src/syscall/signal.rs:sys_rt_sigprocmask"
conditional = "explicit-none"
tests = ["tests/guest/portable/signal-mask-alias.c:main"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, namespace and failure-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-rt_sigreturn"
flags = ["flag:native signal number; signal-mask operation or signal-action/alternate-stack flags where applicable"]
structs = ["struct:native 64-bit signal set, sigaction, siginfo, stack_t and x86_64 signal frame as required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific sigset size, snapshot, signal and target validation; output faults preserve already committed action or alternate stack"]
usercopy = ["usercopy:owned signal records and exact native signal-set copies; ordered frame restore and output-prefix errors"]
state = ["state:retain signal target authority; manipulate mask action queue or alternate stack; wait/return paths preserve caught-signal and restart state"]
concurrency = ["concurrency:signal manager and target authority coordinate delivery with mask changes, exit and queue ownership"]
teardown = ["teardown:consume delivered queue entries, restore masks/context, and release frame or queued-signal reservations"]

[[cell]]
number = 15
name = "rt_sigreturn"
status = "implemented"
contract = "linux-rt_sigreturn"
handler = "kernel/src/syscall/signal.rs:sys_rt_sigreturn"
conditional = "explicit-none"
tests = ["tests/guest/tools/signal-fp-smoke.c:main"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, namespace and failure-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-pause"
flags = ["flag:native signal number; signal-mask operation or signal-action/alternate-stack flags where applicable"]
structs = ["struct:native 64-bit signal set, sigaction, siginfo, stack_t and x86_64 signal frame as required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific sigset size, snapshot, signal and target validation; output faults preserve already committed action or alternate stack"]
usercopy = ["usercopy:owned signal records and exact native signal-set copies; ordered frame restore and output-prefix errors"]
state = ["state:retain signal target authority; manipulate mask action queue or alternate stack; wait/return paths preserve caught-signal and restart state"]
concurrency = ["concurrency:signal manager and target authority coordinate delivery with mask changes, exit and queue ownership"]
teardown = ["teardown:consume delivered queue entries, restore masks/context, and release frame or queued-signal reservations"]

[[cell]]
number = 34
name = "pause"
status = "implemented"
contract = "linux-pause"
handler = "kernel/src/syscall/signal.rs:sys_pause"
conditional = "explicit-none"
tests = ["tests/guest/tools/pause-smoke.c:main"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, namespace and failure-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-kill"
flags = ["flag:native signal number; signal-mask operation or signal-action/alternate-stack flags where applicable"]
structs = ["struct:native 64-bit signal set, sigaction, siginfo, stack_t and x86_64 signal frame as required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific sigset size, snapshot, signal and target validation; output faults preserve already committed action or alternate stack"]
usercopy = ["usercopy:owned signal records and exact native signal-set copies; ordered frame restore and output-prefix errors"]
state = ["state:retain signal target authority; manipulate mask action queue or alternate stack; wait/return paths preserve caught-signal and restart state"]
concurrency = ["concurrency:signal manager and target authority coordinate delivery with mask changes, exit and queue ownership"]
teardown = ["teardown:consume delivered queue entries, restore masks/context, and release frame or queued-signal reservations"]

[[cell]]
number = 62
name = "kill"
status = "implemented"
contract = "linux-kill"
handler = "kernel/src/syscall/signal.rs:sys_kill"
conditional = "explicit-none"
tests = ["kernel/src/syscall/signal.rs:process_group_signal_reducer_keeps_the_first_success_sticky", "kernel/src/syscall/signal.rs:broadcast_signal_reducer_matches_linux_historical_eperm_rule"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, namespace and failure-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-rt_sigpending"
flags = ["flag:native signal number; signal-mask operation or signal-action/alternate-stack flags where applicable"]
structs = ["struct:native 64-bit signal set, sigaction, siginfo, stack_t and x86_64 signal frame as required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific sigset size, snapshot, signal and target validation; output faults preserve already committed action or alternate stack"]
usercopy = ["usercopy:owned signal records and exact native signal-set copies; ordered frame restore and output-prefix errors"]
state = ["state:retain signal target authority; manipulate mask action queue or alternate stack; wait/return paths preserve caught-signal and restart state"]
concurrency = ["concurrency:signal manager and target authority coordinate delivery with mask changes, exit and queue ownership"]
teardown = ["teardown:consume delivered queue entries, restore masks/context, and release frame or queued-signal reservations"]

[[cell]]
number = 127
name = "rt_sigpending"
status = "implemented"
contract = "linux-rt_sigpending"
handler = "kernel/src/syscall/signal.rs:sys_rt_sigpending"
conditional = "explicit-none"
tests = ["kernel/src/syscall/signal.rs:sigpending_only_reports_blocked_pending_signals"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, namespace and failure-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-rt_sigtimedwait"
flags = ["flag:native signal number; signal-mask operation or signal-action/alternate-stack flags where applicable"]
structs = ["struct:native 64-bit signal set, sigaction, siginfo, stack_t and x86_64 signal frame as required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific sigset size, snapshot, signal and target validation; output faults preserve already committed action or alternate stack"]
usercopy = ["usercopy:owned signal records and exact native signal-set copies; ordered frame restore and output-prefix errors"]
state = ["state:retain signal target authority; manipulate mask action queue or alternate stack; wait/return paths preserve caught-signal and restart state"]
concurrency = ["concurrency:signal manager and target authority coordinate delivery with mask changes, exit and queue ownership"]
teardown = ["teardown:consume delivered queue entries, restore masks/context, and release frame or queued-signal reservations"]

[[cell]]
number = 128
name = "rt_sigtimedwait"
status = "implemented"
contract = "linux-rt_sigtimedwait"
handler = "kernel/src/syscall/signal.rs:sys_rt_sigtimedwait"
conditional = "explicit-none"
tests = ["tests/guest/tools/signal-wait-boundary.c:main"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, namespace and failure-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-rt_sigqueueinfo"
flags = ["flag:native signal number; signal-mask operation or signal-action/alternate-stack flags where applicable"]
structs = ["struct:native 64-bit signal set, sigaction, siginfo, stack_t and x86_64 signal frame as required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific sigset size, snapshot, signal and target validation; output faults preserve already committed action or alternate stack"]
usercopy = ["usercopy:owned signal records and exact native signal-set copies; ordered frame restore and output-prefix errors"]
state = ["state:retain signal target authority; manipulate mask action queue or alternate stack; wait/return paths preserve caught-signal and restart state"]
concurrency = ["concurrency:signal manager and target authority coordinate delivery with mask changes, exit and queue ownership"]
teardown = ["teardown:consume delivered queue entries, restore masks/context, and release frame or queued-signal reservations"]

[[cell]]
number = 129
name = "rt_sigqueueinfo"
status = "implemented"
contract = "linux-rt_sigqueueinfo"
handler = "kernel/src/syscall/signal.rs:sys_rt_sigqueueinfo"
conditional = "explicit-none"
tests = ["kernel/src/syscall/signal.rs:realtime_queue_policy_matches_linux_siginfo_classification"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, namespace and failure-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-rt_sigsuspend"
flags = ["flag:native signal number; signal-mask operation or signal-action/alternate-stack flags where applicable"]
structs = ["struct:native 64-bit signal set, sigaction, siginfo, stack_t and x86_64 signal frame as required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific sigset size, snapshot, signal and target validation; output faults preserve already committed action or alternate stack"]
usercopy = ["usercopy:owned signal records and exact native signal-set copies; ordered frame restore and output-prefix errors"]
state = ["state:retain signal target authority; manipulate mask action queue or alternate stack; wait/return paths preserve caught-signal and restart state"]
concurrency = ["concurrency:signal manager and target authority coordinate delivery with mask changes, exit and queue ownership"]
teardown = ["teardown:consume delivered queue entries, restore masks/context, and release frame or queued-signal reservations"]

[[cell]]
number = 130
name = "rt_sigsuspend"
status = "implemented"
contract = "linux-rt_sigsuspend"
handler = "kernel/src/syscall/signal.rs:sys_rt_sigsuspend"
conditional = "explicit-none"
tests = ["kernel/src/syscall/signal.rs:signal_set_size_rules_match_each_linux_syscall_contract"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, namespace and failure-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-sigaltstack"
flags = ["flag:native signal number; signal-mask operation or signal-action/alternate-stack flags where applicable"]
structs = ["struct:native 64-bit signal set, sigaction, siginfo, stack_t and x86_64 signal frame as required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific sigset size, snapshot, signal and target validation; output faults preserve already committed action or alternate stack"]
usercopy = ["usercopy:owned signal records and exact native signal-set copies; ordered frame restore and output-prefix errors"]
state = ["state:retain signal target authority; manipulate mask action queue or alternate stack; wait/return paths preserve caught-signal and restart state"]
concurrency = ["concurrency:signal manager and target authority coordinate delivery with mask changes, exit and queue ownership"]
teardown = ["teardown:consume delivered queue entries, restore masks/context, and release frame or queued-signal reservations"]

[[cell]]
number = 131
name = "sigaltstack"
status = "implemented"
contract = "linux-sigaltstack"
handler = "kernel/src/syscall/signal.rs:sys_sigaltstack"
conditional = "explicit-none"
tests = ["tests/guest/portable/signal-boundary-differential.c:main"]
validation_gaps = ["Current paired boundary cases pass, but the broader signal entry, namespace and lifecycle matrix remains unverified"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-tkill"
flags = ["flag:native signal number; signal-mask operation or signal-action/alternate-stack flags where applicable"]
structs = ["struct:native 64-bit signal set, sigaction, siginfo, stack_t and x86_64 signal frame as required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific sigset size, snapshot, signal and target validation; output faults preserve already committed action or alternate stack"]
usercopy = ["usercopy:owned signal records and exact native signal-set copies; ordered frame restore and output-prefix errors"]
state = ["state:retain signal target authority; manipulate mask action queue or alternate stack; wait/return paths preserve caught-signal and restart state"]
concurrency = ["concurrency:signal manager and target authority coordinate delivery with mask changes, exit and queue ownership"]
teardown = ["teardown:consume delivered queue entries, restore masks/context, and release frame or queued-signal reservations"]

[[cell]]
number = 200
name = "tkill"
status = "implemented"
contract = "linux-tkill"
handler = "kernel/src/syscall/signal.rs:sys_tkill"
conditional = "explicit-none"
tests = ["kernel/src/syscall/signal.rs:signal_numbers_are_range_checked_before_narrowing"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, namespace and failure-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-tgkill"
flags = ["flag:native signal number; signal-mask operation or signal-action/alternate-stack flags where applicable"]
structs = ["struct:native 64-bit signal set, sigaction, siginfo, stack_t and x86_64 signal frame as required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific sigset size, snapshot, signal and target validation; output faults preserve already committed action or alternate stack"]
usercopy = ["usercopy:owned signal records and exact native signal-set copies; ordered frame restore and output-prefix errors"]
state = ["state:retain signal target authority; manipulate mask action queue or alternate stack; wait/return paths preserve caught-signal and restart state"]
concurrency = ["concurrency:signal manager and target authority coordinate delivery with mask changes, exit and queue ownership"]
teardown = ["teardown:consume delivered queue entries, restore masks/context, and release frame or queued-signal reservations"]

[[cell]]
number = 234
name = "tgkill"
status = "implemented"
contract = "linux-tgkill"
handler = "kernel/src/syscall/signal.rs:sys_tgkill"
conditional = "explicit-none"
tests = ["kernel/src/syscall/signal.rs:signal_numbers_are_range_checked_before_narrowing"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, namespace and failure-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-rt_tgsigqueueinfo"
flags = ["flag:native signal number; signal-mask operation or signal-action/alternate-stack flags where applicable"]
structs = ["struct:native 64-bit signal set, sigaction, siginfo, stack_t and x86_64 signal frame as required"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific sigset size, snapshot, signal and target validation; output faults preserve already committed action or alternate stack"]
usercopy = ["usercopy:owned signal records and exact native signal-set copies; ordered frame restore and output-prefix errors"]
state = ["state:retain signal target authority; manipulate mask action queue or alternate stack; wait/return paths preserve caught-signal and restart state"]
concurrency = ["concurrency:signal manager and target authority coordinate delivery with mask changes, exit and queue ownership"]
teardown = ["teardown:consume delivered queue entries, restore masks/context, and release frame or queued-signal reservations"]

[[cell]]
number = 297
name = "rt_tgsigqueueinfo"
status = "implemented"
contract = "linux-rt_tgsigqueueinfo"
handler = "kernel/src/syscall/signal.rs:sys_rt_tgsigqueueinfo"
conditional = "explicit-none"
tests = ["tests/guest/portable/signal-boundary-differential.c:main"]
validation_gaps = ["explicit-none"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-mremap"
flags = ["flag:MREMAP_MAYMOVE FIXED DONTUNMAP; zero old_size shared clone"]
structs = ["struct:native address ranges, pollfd/epoll_event, timespec or robust-list pointer/size according to entry"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate entry flags and geometry; retain target and authority; commit owner state or copy readiness output with explicit failure semantics"]
usercopy = ["usercopy:owned request/output snapshots where required; mapping operations use scalar addresses without importing mapped contents"]
state = ["state:apply requested memory mapping protection, locking, remapping or advisory operation under address-space ownership"]
concurrency = ["concurrency:retain task/address-space/OFD owner through operation; release locks before blocking population or usercopy"]
teardown = ["teardown:release poll registrations or temporary mapping capabilities; persistent state remains owned by the process"]

[[cell]]
number = 25
name = "mremap"
status = "implemented"
contract = "linux-mremap"
handler = "kernel/src/syscall/mm/mmap.rs:sys_mremap"
conditional = "explicit-none"
tests = ["kernel/src/mm/remap.rs:mremap_locked_growth_uses_linux_pre_replacement_accounting"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, boundaries and persistent-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-msync"
flags = ["flag:MS_ASYNC MS_SYNC MS_INVALIDATE"]
structs = ["struct:native address ranges, pollfd/epoll_event, timespec or robust-list pointer/size according to entry"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate entry flags and geometry; retain target and authority; commit owner state or copy readiness output with explicit failure semantics"]
usercopy = ["usercopy:owned request/output snapshots where required; mapping operations use scalar addresses without importing mapped contents"]
state = ["state:apply requested memory mapping protection, locking, remapping or advisory operation under address-space ownership"]
concurrency = ["concurrency:retain task/address-space/OFD owner through operation; release locks before blocking population or usercopy"]
teardown = ["teardown:release poll registrations or temporary mapping capabilities; persistent state remains owned by the process"]

[[cell]]
number = 26
name = "msync"
status = "partial"
contract = "linux-msync"
handler = "kernel/src/syscall/mm/mmap.rs:sys_msync"
conditional = "explicit-none"
tests = ["kernel/src/syscall/mm/mmap.rs:msync_flushes_shared_file_ranges_after_holes_before_reporting_enomem"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, boundaries and persistent-state matrix remains pending"]
limitations = ["Whole-range locked-VMA preflight can reject before flushing an earlier shared file range; Linux applies MS_SYNC/INVALIDATE effects in VMA order"]

[[contract]]
id = "linux-mlock"
flags = ["flag:page-rounded address range"]
structs = ["struct:native address ranges, pollfd/epoll_event, timespec or robust-list pointer/size according to entry"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate entry flags and geometry; retain target and authority; commit owner state or copy readiness output with explicit failure semantics"]
usercopy = ["usercopy:owned request/output snapshots where required; mapping operations use scalar addresses without importing mapped contents"]
state = ["state:apply requested memory mapping protection, locking, remapping or advisory operation under address-space ownership"]
concurrency = ["concurrency:retain task/address-space/OFD owner through operation; release locks before blocking population or usercopy"]
teardown = ["teardown:release poll registrations or temporary mapping capabilities; persistent state remains owned by the process"]

[[cell]]
number = 149
name = "mlock"
status = "implemented"
contract = "linux-mlock"
handler = "kernel/src/syscall/mm/mmap.rs:sys_mlock"
conditional = "explicit-none"
tests = ["tests/guest/portable/mm-contracts-differential.c:main"]
validation_gaps = ["New lock-prefix and population-error differential cases await current paired Linux7.2.3 and TheKernel execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-mlock2"
flags = ["flag:MLOCK_ONFAULT"]
structs = ["struct:native address ranges, pollfd/epoll_event, timespec or robust-list pointer/size according to entry"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate entry flags and geometry; retain target and authority; commit owner state or copy readiness output with explicit failure semantics"]
usercopy = ["usercopy:owned request/output snapshots where required; mapping operations use scalar addresses without importing mapped contents"]
state = ["state:apply requested memory mapping protection, locking, remapping or advisory operation under address-space ownership"]
concurrency = ["concurrency:retain task/address-space/OFD owner through operation; release locks before blocking population or usercopy"]
teardown = ["teardown:release poll registrations or temporary mapping capabilities; persistent state remains owned by the process"]

[[cell]]
number = 325
name = "mlock2"
status = "implemented"
contract = "linux-mlock2"
handler = "kernel/src/syscall/mm/mmap.rs:sys_mlock2"
conditional = "explicit-none"
tests = ["tests/guest/portable/mm-contracts-differential.c:main"]
validation_gaps = ["New lock-prefix and population-error differential cases await current paired Linux7.2.3 and TheKernel execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-munlock"
flags = ["flag:page-rounded address range"]
structs = ["struct:native address ranges, pollfd/epoll_event, timespec or robust-list pointer/size according to entry"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate entry flags and geometry; retain target and authority; commit owner state or copy readiness output with explicit failure semantics"]
usercopy = ["usercopy:owned request/output snapshots where required; mapping operations use scalar addresses without importing mapped contents"]
state = ["state:apply requested memory mapping protection, locking, remapping or advisory operation under address-space ownership"]
concurrency = ["concurrency:retain task/address-space/OFD owner through operation; release locks before blocking population or usercopy"]
teardown = ["teardown:release poll registrations or temporary mapping capabilities; persistent state remains owned by the process"]

[[cell]]
number = 150
name = "munlock"
status = "implemented"
contract = "linux-munlock"
handler = "kernel/src/syscall/mm/mmap.rs:sys_munlock"
conditional = "explicit-none"
tests = ["tests/guest/portable/mm-contracts-differential.c:main"]
validation_gaps = ["New lock-prefix and population-error differential cases await current paired Linux7.2.3 and TheKernel execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-mlockall"
flags = ["flag:MCL_CURRENT MCL_FUTURE MCL_ONFAULT"]
structs = ["struct:native address ranges, pollfd/epoll_event, timespec or robust-list pointer/size according to entry"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate entry flags and geometry; retain target and authority; commit owner state or copy readiness output with explicit failure semantics"]
usercopy = ["usercopy:owned request/output snapshots where required; mapping operations use scalar addresses without importing mapped contents"]
state = ["state:apply requested memory mapping protection, locking, remapping or advisory operation under address-space ownership"]
concurrency = ["concurrency:retain task/address-space/OFD owner through operation; release locks before blocking population or usercopy"]
teardown = ["teardown:release poll registrations or temporary mapping capabilities; persistent state remains owned by the process"]

[[cell]]
number = 151
name = "mlockall"
status = "implemented"
contract = "linux-mlockall"
handler = "kernel/src/syscall/mm/mmap.rs:sys_mlockall"
conditional = "explicit-none"
tests = ["tests/guest/portable/mm-contracts-differential.c:main"]
validation_gaps = ["New lock-prefix and population-error differential cases await current paired Linux7.2.3 and TheKernel execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-munlockall"
flags = ["flag:clear current and future locking"]
structs = ["struct:native address ranges, pollfd/epoll_event, timespec or robust-list pointer/size according to entry"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate entry flags and geometry; retain target and authority; commit owner state or copy readiness output with explicit failure semantics"]
usercopy = ["usercopy:owned request/output snapshots where required; mapping operations use scalar addresses without importing mapped contents"]
state = ["state:apply requested memory mapping protection, locking, remapping or advisory operation under address-space ownership"]
concurrency = ["concurrency:retain task/address-space/OFD owner through operation; release locks before blocking population or usercopy"]
teardown = ["teardown:release poll registrations or temporary mapping capabilities; persistent state remains owned by the process"]

[[cell]]
number = 152
name = "munlockall"
status = "implemented"
contract = "linux-munlockall"
handler = "kernel/src/syscall/mm/mmap.rs:sys_munlockall"
conditional = "explicit-none"
tests = []
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, boundaries and persistent-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-remap_file_pages"
flags = ["flag:zero prot and valid shared-file mapping geometry"]
structs = ["struct:native address ranges, pollfd/epoll_event, timespec or robust-list pointer/size according to entry"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate entry flags and geometry; retain target and authority; commit owner state or copy readiness output with explicit failure semantics"]
usercopy = ["usercopy:owned request/output snapshots where required; mapping operations use scalar addresses without importing mapped contents"]
state = ["state:apply requested memory mapping protection, locking, remapping or advisory operation under address-space ownership"]
concurrency = ["concurrency:retain task/address-space/OFD owner through operation; release locks before blocking population or usercopy"]
teardown = ["teardown:release poll registrations or temporary mapping capabilities; persistent state remains owned by the process"]

[[cell]]
number = 216
name = "remap_file_pages"
status = "implemented"
contract = "linux-remap_file_pages"
handler = "kernel/src/syscall/mm/mmap.rs:sys_remap_file_pages"
conditional = "explicit-none"
tests = []
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, boundaries and persistent-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-set_robust_list"
flags = ["flag:exact native robust_list_head size"]
structs = ["struct:native address ranges, pollfd/epoll_event, timespec or robust-list pointer/size according to entry"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate entry flags and geometry; retain target and authority; commit owner state or copy readiness output with explicit failure semantics"]
usercopy = ["usercopy:owned request/output snapshots where required; mapping operations use scalar addresses without importing mapped contents"]
state = ["state:register or inspect per-thread robust-list ownership for exit cleanup"]
concurrency = ["concurrency:retain task/address-space/OFD owner through operation; release locks before blocking population or usercopy"]
teardown = ["teardown:release poll registrations or temporary mapping capabilities; persistent state remains owned by the process"]

[[cell]]
number = 273
name = "set_robust_list"
status = "implemented"
contract = "linux-set_robust_list"
handler = "kernel/src/syscall/sync/futex.rs:sys_set_robust_list"
conditional = "explicit-none"
tests = []
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, boundaries and persistent-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-get_robust_list"
flags = ["flag:namespace-visible TID and ptrace read-real permission"]
structs = ["struct:native address ranges, pollfd/epoll_event, timespec or robust-list pointer/size according to entry"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate entry flags and geometry; retain target and authority; commit owner state or copy readiness output with explicit failure semantics"]
usercopy = ["usercopy:owned request/output snapshots where required; mapping operations use scalar addresses without importing mapped contents"]
state = ["state:register or inspect per-thread robust-list ownership for exit cleanup"]
concurrency = ["concurrency:retain task/address-space/OFD owner through operation; release locks before blocking population or usercopy"]
teardown = ["teardown:release poll registrations or temporary mapping capabilities; persistent state remains owned by the process"]

[[cell]]
number = 274
name = "get_robust_list"
status = "implemented"
contract = "linux-get_robust_list"
handler = "kernel/src/syscall/sync/futex.rs:sys_get_robust_list"
conditional = "explicit-none"
tests = ["kernel/src/syscall/sync/futex.rs:credential_caller_robust_list_uses_read_real_only_for_other_tid"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, boundaries and persistent-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-poll"
flags = ["flag:pollfd requested events and signed millisecond timeout"]
structs = ["struct:native address ranges, pollfd/epoll_event, timespec or robust-list pointer/size according to entry"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate entry flags and geometry; retain target and authority; commit owner state or copy readiness output with explicit failure semantics"]
usercopy = ["usercopy:owned request/output snapshots where required; mapping operations use scalar addresses without importing mapped contents"]
state = ["state:retain OFDs and return requested readiness while preserving mask and timeout semantics"]
concurrency = ["concurrency:retain task/address-space/OFD owner through operation; release locks before blocking population or usercopy"]
teardown = ["teardown:release poll registrations or temporary mapping capabilities; persistent state remains owned by the process"]

[[cell]]
number = 7
name = "poll"
status = "implemented"
contract = "linux-poll"
handler = "kernel/src/syscall/io_mpx/poll.rs:sys_poll"
conditional = "explicit-none"
tests = ["kernel/src/syscall/io_mpx/poll.rs:every_negative_fd_is_ignored_and_cleared", "kernel/src/syscall/io_mpx/poll.rs:invalid_and_ready_descriptors_are_counted_together"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, boundaries and persistent-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-ppoll"
flags = ["flag:pollfd events and temporary signal mask with timespec timeout"]
structs = ["struct:native address ranges, pollfd/epoll_event, timespec or robust-list pointer/size according to entry"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate entry flags and geometry; retain target and authority; commit owner state or copy readiness output with explicit failure semantics"]
usercopy = ["usercopy:owned request/output snapshots where required; mapping operations use scalar addresses without importing mapped contents"]
state = ["state:retain OFDs and return requested readiness while preserving mask and timeout semantics"]
concurrency = ["concurrency:retain task/address-space/OFD owner through operation; release locks before blocking population or usercopy"]
teardown = ["teardown:release poll registrations or temporary mapping capabilities; persistent state remains owned by the process"]

[[cell]]
number = 271
name = "ppoll"
status = "implemented"
contract = "linux-ppoll"
handler = "kernel/src/syscall/io_mpx/poll.rs:sys_ppoll"
conditional = "explicit-none"
tests = ["kernel/src/syscall/io_mpx/poll.rs:unknown_input_event_bits_are_ignored"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, boundaries and persistent-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-pkey_mprotect"
flags = ["flag:protection flags and allocated protection-key number"]
structs = ["struct:native address ranges, pollfd/epoll_event, timespec or robust-list pointer/size according to entry"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate entry flags and geometry; retain target and authority; commit owner state or copy readiness output with explicit failure semantics"]
usercopy = ["usercopy:owned request/output snapshots where required; mapping operations use scalar addresses without importing mapped contents"]
state = ["state:apply requested memory mapping protection, locking, remapping or advisory operation under address-space ownership"]
concurrency = ["concurrency:retain task/address-space/OFD owner through operation; release locks before blocking population or usercopy"]
teardown = ["teardown:release poll registrations or temporary mapping capabilities; persistent state remains owned by the process"]

[[cell]]
number = 329
name = "pkey_mprotect"
status = "implemented"
contract = "linux-pkey_mprotect"
handler = "kernel/src/syscall/mm/mmap.rs:sys_pkey_mprotect"
conditional = "explicit-none"
tests = ["tests/guest/tools/cpu-smoke.c:pku_state"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, boundaries and persistent-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-pkey_free"
flags = ["flag:allocated protection-key number"]
structs = ["struct:native address ranges, pollfd/epoll_event, timespec or robust-list pointer/size according to entry"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate entry flags and geometry; retain target and authority; commit owner state or copy readiness output with explicit failure semantics"]
usercopy = ["usercopy:owned request/output snapshots where required; mapping operations use scalar addresses without importing mapped contents"]
state = ["state:apply requested memory mapping protection, locking, remapping or advisory operation under address-space ownership"]
concurrency = ["concurrency:retain task/address-space/OFD owner through operation; release locks before blocking population or usercopy"]
teardown = ["teardown:release poll registrations or temporary mapping capabilities; persistent state remains owned by the process"]

[[cell]]
number = 331
name = "pkey_free"
status = "implemented"
contract = "linux-pkey_free"
handler = "kernel/src/syscall/mm/mmap.rs:sys_pkey_free"
conditional = "explicit-none"
tests = ["tests/guest/tools/cpu-smoke.c:pku_state"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, boundaries and persistent-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-process_madvise"
flags = ["flag:pidfd iovec advice and zero flags"]
structs = ["struct:native address ranges, pollfd/epoll_event, timespec or robust-list pointer/size according to entry"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate entry flags and geometry; retain target and authority; commit owner state or copy readiness output with explicit failure semantics"]
usercopy = ["usercopy:owned request/output snapshots where required; mapping operations use scalar addresses without importing mapped contents"]
state = ["state:apply requested memory mapping protection, locking, remapping or advisory operation under address-space ownership"]
concurrency = ["concurrency:retain task/address-space/OFD owner through operation; release locks before blocking population or usercopy"]
teardown = ["teardown:release poll registrations or temporary mapping capabilities; persistent state remains owned by the process"]

[[cell]]
number = 440
name = "process_madvise"
status = "partial"
contract = "linux-process_madvise"
handler = "kernel/src/syscall/mm/process_vm.rs:sys_process_madvise"
conditional = "explicit-none"
tests = []
validation_gaps = ["New self-advice differential case awaits paired execution; Linux oracle disables KSM, THP and memory-failure advice and requires explicit configuration alignment for these commands"]
limitations = ["Same-mm advice inherits madvise implementation limitations, including nominal KSM and anonymous reclamation behavior"]

[[contract]]
id = "linux-map_shadow_stack"
flags = ["flag:SHADOW_STACK_SET_TOKEN; CET hardware admission and aligned geometry"]
structs = ["struct:native address ranges, pollfd/epoll_event, timespec or robust-list pointer/size according to entry"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate entry flags and geometry; retain target and authority; commit owner state or copy readiness output with explicit failure semantics"]
usercopy = ["usercopy:owned request/output snapshots where required; mapping operations use scalar addresses without importing mapped contents"]
state = ["state:apply requested memory mapping protection, locking, remapping or advisory operation under address-space ownership"]
concurrency = ["concurrency:retain task/address-space/OFD owner through operation; release locks before blocking population or usercopy"]
teardown = ["teardown:release poll registrations or temporary mapping capabilities; persistent state remains owned by the process"]

[[cell]]
number = 453
name = "map_shadow_stack"
status = "implemented"
contract = "linux-map_shadow_stack"
handler = "kernel/src/syscall/mm/mmap.rs:sys_map_shadow_stack"
conditional = "explicit-none"
tests = []
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, boundaries and persistent-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-epoll_create"
flags = ["flag:positive size argument; create without CLOEXEC"]
structs = ["struct:native address ranges, pollfd/epoll_event, timespec or robust-list pointer/size according to entry"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate entry flags and geometry; retain target and authority; commit owner state or copy readiness output with explicit failure semantics"]
usercopy = ["usercopy:owned request/output snapshots where required; mapping operations use scalar addresses without importing mapped contents"]
state = ["state:retain OFDs and return requested readiness while preserving mask and timeout semantics"]
concurrency = ["concurrency:retain task/address-space/OFD owner through operation; release locks before blocking population or usercopy"]
teardown = ["teardown:release poll registrations or temporary mapping capabilities; persistent state remains owned by the process"]

[[cell]]
number = 213
name = "epoll_create"
status = "implemented"
contract = "linux-epoll_create"
handler = "kernel/src/syscall/dispatch.rs:compat_epoll_create"
conditional = "explicit-none"
tests = ["tests/guest/portable/epoll-smoke.c:main"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, boundaries and persistent-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-epoll_create1"
flags = ["flag:EPOLL_CLOEXEC"]
structs = ["struct:native address ranges, pollfd/epoll_event, timespec or robust-list pointer/size according to entry"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate entry flags and geometry; retain target and authority; commit owner state or copy readiness output with explicit failure semantics"]
usercopy = ["usercopy:owned request/output snapshots where required; mapping operations use scalar addresses without importing mapped contents"]
state = ["state:retain OFDs and return requested readiness while preserving mask and timeout semantics"]
concurrency = ["concurrency:retain task/address-space/OFD owner through operation; release locks before blocking population or usercopy"]
teardown = ["teardown:release poll registrations or temporary mapping capabilities; persistent state remains owned by the process"]

[[cell]]
number = 291
name = "epoll_create1"
status = "implemented"
contract = "linux-epoll_create1"
handler = "kernel/src/syscall/io_mpx/epoll.rs:sys_epoll_create1"
conditional = "explicit-none"
tests = ["tests/guest/portable/epoll-smoke.c:main"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, boundaries and persistent-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-epoll_wait"
flags = ["flag:positive maxevents and signed millisecond timeout"]
structs = ["struct:native address ranges, pollfd/epoll_event, timespec or robust-list pointer/size according to entry"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate entry flags and geometry; retain target and authority; commit owner state or copy readiness output with explicit failure semantics"]
usercopy = ["usercopy:owned request/output snapshots where required; mapping operations use scalar addresses without importing mapped contents"]
state = ["state:retain OFDs and return requested readiness while preserving mask and timeout semantics"]
concurrency = ["concurrency:retain task/address-space/OFD owner through operation; release locks before blocking population or usercopy"]
teardown = ["teardown:release poll registrations or temporary mapping capabilities; persistent state remains owned by the process"]

[[cell]]
number = 232
name = "epoll_wait"
status = "implemented"
contract = "linux-epoll_wait"
handler = "kernel/src/syscall/io_mpx/epoll.rs:sys_epoll_wait"
conditional = "explicit-none"
tests = ["tests/guest/portable/epoll-smoke.c:main"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, boundaries and persistent-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-epoll_pwait"
flags = ["flag:temporary signal mask and millisecond timeout"]
structs = ["struct:native address ranges, pollfd/epoll_event, timespec or robust-list pointer/size according to entry"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate entry flags and geometry; retain target and authority; commit owner state or copy readiness output with explicit failure semantics"]
usercopy = ["usercopy:owned request/output snapshots where required; mapping operations use scalar addresses without importing mapped contents"]
state = ["state:retain OFDs and return requested readiness while preserving mask and timeout semantics"]
concurrency = ["concurrency:retain task/address-space/OFD owner through operation; release locks before blocking population or usercopy"]
teardown = ["teardown:release poll registrations or temporary mapping capabilities; persistent state remains owned by the process"]

[[cell]]
number = 281
name = "epoll_pwait"
status = "implemented"
contract = "linux-epoll_pwait"
handler = "kernel/src/syscall/io_mpx/epoll.rs:sys_epoll_pwait"
conditional = "explicit-none"
tests = ["tests/guest/portable/epoll-smoke.c:main"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, boundaries and persistent-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-epoll_pwait2"
flags = ["flag:temporary signal mask and timespec timeout"]
structs = ["struct:native address ranges, pollfd/epoll_event, timespec or robust-list pointer/size according to entry"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate entry flags and geometry; retain target and authority; commit owner state or copy readiness output with explicit failure semantics"]
usercopy = ["usercopy:owned request/output snapshots where required; mapping operations use scalar addresses without importing mapped contents"]
state = ["state:retain OFDs and return requested readiness while preserving mask and timeout semantics"]
concurrency = ["concurrency:retain task/address-space/OFD owner through operation; release locks before blocking population or usercopy"]
teardown = ["teardown:release poll registrations or temporary mapping capabilities; persistent state remains owned by the process"]

[[cell]]
number = 441
name = "epoll_pwait2"
status = "implemented"
contract = "linux-epoll_pwait2"
handler = "kernel/src/syscall/io_mpx/epoll.rs:sys_epoll_pwait2"
conditional = "explicit-none"
tests = ["tests/guest/portable/epoll-smoke.c:main"]
validation_gaps = ["Matched Linux 7.2.3 and TheKernel guest entry, boundaries and persistent-state matrix remains pending"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-clone"
flags = ["flag:legacy clone sharing, exit signal, TLS and parent/child TID flags"]
structs = ["struct:x86_64 clone register arguments"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate ABI arguments and user memory before committing child publication or replacement image"]
usercopy = ["usercopy:copy supplied task ABI pointers with checked user-memory access"]
state = ["state:publish child with selected shared or copied VM, files, FS, signal and credentials"]
concurrency = ["concurrency:task and address-space ownership gates serialize publication and image replacement"]
teardown = ["teardown:failed preparation releases staged resources; successful exit or exec releases prior ownership"]

[[cell]]
number = 56
name = "clone"
status = "implemented"
contract = "linux-clone"
handler = "kernel/src/syscall/task/clone.rs:sys_clone"
conditional = "explicit-none"
tests = ["kernel/src/syscall/task/clone.rs:clone_validate_allows_detached_without_pidfd"]
validation_gaps = ["Focused implementation tests do not establish complete raw task lifecycle behavior on both Linux7.2.3 and TheKernel guests"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-fork"
flags = ["explicit-none"]
structs = ["explicit-none"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate ABI arguments and user memory before committing child publication or replacement image"]
usercopy = ["usercopy:copy supplied task ABI pointers with checked user-memory access"]
state = ["state:copy process VM with COW and copy descriptor/FS ownership before child publication"]
concurrency = ["concurrency:task and address-space ownership gates serialize publication and image replacement"]
teardown = ["teardown:failed preparation releases staged resources; successful exit or exec releases prior ownership"]

[[cell]]
number = 57
name = "fork"
status = "implemented"
contract = "linux-fork"
handler = "kernel/src/syscall/task/clone.rs:sys_fork"
conditional = "explicit-none"
tests = ["kernel/src/syscall/task/clone.rs:clone_validate_allows_detached_without_pidfd"]
validation_gaps = ["Focused implementation tests do not establish complete raw task lifecycle behavior on both Linux7.2.3 and TheKernel guests"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-vfork"
flags = ["explicit-none"]
structs = ["explicit-none"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate ABI arguments and user memory before committing child publication or replacement image"]
usercopy = ["usercopy:copy supplied task ABI pointers with checked user-memory access"]
state = ["state:share VM and suspend parent until child exec or exit completes"]
concurrency = ["concurrency:task and address-space ownership gates serialize publication and image replacement"]
teardown = ["teardown:failed preparation releases staged resources; successful exit or exec releases prior ownership"]

[[cell]]
number = 58
name = "vfork"
status = "implemented"
contract = "linux-vfork"
handler = "kernel/src/syscall/task/clone.rs:sys_vfork"
conditional = "explicit-none"
tests = ["kernel/src/syscall/task/clone.rs:clone_validate_allows_detached_without_pidfd"]
validation_gaps = ["Focused implementation tests do not establish complete raw task lifecycle behavior on both Linux7.2.3 and TheKernel guests"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-execve"
flags = ["explicit-none"]
structs = ["struct:NULL-terminated argv and envp pointer vectors"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate ABI arguments and user memory before committing child publication or replacement image"]
usercopy = ["usercopy:copy supplied task ABI pointers with checked user-memory access"]
state = ["state:replace image, reset registers and TLS, close CLOEXEC descriptors and apply exec credential transition"]
concurrency = ["concurrency:task and address-space ownership gates serialize publication and image replacement"]
teardown = ["teardown:failed preparation releases staged resources; successful exit or exec releases prior ownership"]

[[cell]]
number = 59
name = "execve"
status = "implemented"
contract = "linux-execve"
handler = "kernel/src/syscall/task/execve.rs:sys_execve"
conditional = "explicit-none"
tests = ["kernel/src/syscall/task/execve.rs:exec_replaces_old_image_registers_and_tls"]
validation_gaps = ["Focused implementation tests do not establish complete raw task lifecycle behavior on both Linux7.2.3 and TheKernel guests"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-execveat"
flags = ["flag:AT_EMPTY_PATH and AT_SYMLINK_NOFOLLOW"]
structs = ["struct:NULL-terminated argv and envp pointer vectors"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:validate ABI arguments and user memory before committing child publication or replacement image"]
usercopy = ["usercopy:copy supplied task ABI pointers with checked user-memory access"]
state = ["state:resolve image through dirfd or empty-path descriptor and commit the replacement image"]
concurrency = ["concurrency:task and address-space ownership gates serialize publication and image replacement"]
teardown = ["teardown:failed preparation releases staged resources; successful exit or exec releases prior ownership"]

[[cell]]
number = 322
name = "execveat"
status = "implemented"
contract = "linux-execveat"
handler = "kernel/src/syscall/task/execve.rs:sys_execveat"
conditional = "explicit-none"
tests = ["kernel/src/syscall/task/execve.rs:exec_replaces_old_image_registers_and_tls"]
validation_gaps = ["Focused implementation tests do not establish complete raw task lifecycle behavior on both Linux7.2.3 and TheKernel guests"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-open"
flags = ["flag:O_* and openat2 RESOLVE_* flags"]
structs = ["struct:native x86_64 open argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve pathname/open_how then install an owned open-file description with access, creation, resolution and CLOEXEC semantics"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 2
name = "open"
status = "implemented"
contract = "linux-open"
handler = "kernel/src/syscall/fs/fd_ops.rs:sys_open"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/fd_ops.rs:creat_is_the_x86_64_openat_alias"]
validation_gaps = ["Existing source/helper coverage does not establish the full open raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-openat"
flags = ["flag:O_* and openat2 RESOLVE_* flags"]
structs = ["struct:native x86_64 openat argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve pathname/open_how then install an owned open-file description with access, creation, resolution and CLOEXEC semantics"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 257
name = "openat"
status = "implemented"
contract = "linux-openat"
handler = "kernel/src/syscall/fs/fd_ops.rs:sys_openat"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/fd_ops.rs:creat_is_the_x86_64_openat_alias"]
validation_gaps = ["Existing source/helper coverage does not establish the full openat raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-openat2"
flags = ["flag:O_* and openat2 RESOLVE_* flags"]
structs = ["struct:native x86_64 openat2 argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve pathname/open_how then install an owned open-file description with access, creation, resolution and CLOEXEC semantics"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 437
name = "openat2"
status = "implemented"
contract = "linux-openat2"
handler = "kernel/src/syscall/fs/fd_ops.rs:sys_openat2"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/fd_ops.rs:creat_is_the_x86_64_openat_alias"]
validation_gaps = ["Existing source/helper coverage does not establish the full openat2 raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-name_to_handle_at"
flags = ["flag:AT_EMPTY_PATH/AT_SYMLINK_FOLLOW and file-open flags"]
structs = ["struct:native x86_64 name_to_handle_at argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:encode export handle sizing and mount identity; authorize and decode stable export handle before opening"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 303
name = "name_to_handle_at"
status = "implemented"
contract = "linux-name_to_handle_at"
handler = "kernel/src/syscall/fs/fd_ops.rs:sys_name_to_handle_at"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/fd_ops.rs:export_handle_decode_uses_complete_dwords_only"]
validation_gaps = ["Existing source/helper coverage does not establish the full name_to_handle_at raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-open_by_handle_at"
flags = ["flag:AT_EMPTY_PATH/AT_SYMLINK_FOLLOW and file-open flags"]
structs = ["struct:native x86_64 open_by_handle_at argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:encode export handle sizing and mount identity; authorize and decode stable export handle before opening"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 304
name = "open_by_handle_at"
status = "implemented"
contract = "linux-open_by_handle_at"
handler = "kernel/src/syscall/fs/fd_ops.rs:sys_open_by_handle_at"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/fd_ops.rs:export_handle_decode_uses_complete_dwords_only"]
validation_gaps = ["Existing source/helper coverage does not establish the full open_by_handle_at raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-close"
flags = ["flag:close_range UNSHARE/CLOEXEC and dup3 CLOEXEC"]
structs = ["struct:native x86_64 close argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:descriptor-table removal or duplication retains shared open-file-description lifetime and independent descriptor flags"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 3
name = "close"
status = "implemented"
contract = "linux-close"
handler = "kernel/src/syscall/fs/fd_ops.rs:sys_close"
conditional = "explicit-none"
tests = ["kernel/src/file/fd_table.rs:last_descriptor_close_is_counted_across_dup_and_fork"]
validation_gaps = ["Existing source/helper coverage does not establish the full close raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-close_range"
flags = ["flag:close_range UNSHARE/CLOEXEC and dup3 CLOEXEC"]
structs = ["struct:native x86_64 close_range argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:descriptor-table removal or duplication retains shared open-file-description lifetime and independent descriptor flags"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 436
name = "close_range"
status = "implemented"
contract = "linux-close_range"
handler = "kernel/src/syscall/fs/fd_ops.rs:sys_close_range"
conditional = "explicit-none"
tests = ["kernel/src/file/fd_table.rs:last_descriptor_close_is_counted_across_dup_and_fork"]
validation_gaps = ["Existing source/helper coverage does not establish the full close_range raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-dup"
flags = ["flag:close_range UNSHARE/CLOEXEC and dup3 CLOEXEC"]
structs = ["struct:native x86_64 dup argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:descriptor-table removal or duplication retains shared open-file-description lifetime and independent descriptor flags"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 32
name = "dup"
status = "implemented"
contract = "linux-dup"
handler = "kernel/src/syscall/fs/fd_ops.rs:sys_dup"
conditional = "explicit-none"
tests = ["kernel/src/file/fd_table.rs:last_descriptor_close_is_counted_across_dup_and_fork"]
validation_gaps = ["Existing source/helper coverage does not establish the full dup raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-dup2"
flags = ["flag:close_range UNSHARE/CLOEXEC and dup3 CLOEXEC"]
structs = ["struct:native x86_64 dup2 argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:descriptor-table removal or duplication retains shared open-file-description lifetime and independent descriptor flags"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 33
name = "dup2"
status = "implemented"
contract = "linux-dup2"
handler = "kernel/src/syscall/fs/fd_ops.rs:sys_dup2"
conditional = "explicit-none"
tests = ["kernel/src/file/fd_table.rs:last_descriptor_close_is_counted_across_dup_and_fork"]
validation_gaps = ["Existing source/helper coverage does not establish the full dup2 raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-dup3"
flags = ["flag:close_range UNSHARE/CLOEXEC and dup3 CLOEXEC"]
structs = ["struct:native x86_64 dup3 argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:descriptor-table removal or duplication retains shared open-file-description lifetime and independent descriptor flags"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 292
name = "dup3"
status = "implemented"
contract = "linux-dup3"
handler = "kernel/src/syscall/fs/fd_ops.rs:sys_dup3"
conditional = "explicit-none"
tests = ["kernel/src/file/fd_table.rs:last_descriptor_close_is_counted_across_dup_and_fork"]
validation_gaps = ["Existing source/helper coverage does not establish the full dup3 raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-fcntl"
flags = ["flag:F_* command-specific flags"]
structs = ["struct:native x86_64 fcntl argument/result layout"]
multiplexer_commands = ["mux:F_* command-specific flags"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:dispatch descriptor/status flags, duplication and record-lock commands through authoritative open-file descriptions"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 72
name = "fcntl"
status = "partial"
contract = "linux-fcntl"
handler = "kernel/src/syscall/fs/fd_ops.rs:sys_fcntl"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/fd_ops.rs:noatime_requires_owner_match_or_namespace_relative_fowner"]
validation_gaps = ["Existing source/helper coverage does not establish the full fcntl raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["F_GETPIPE_SZ/F_SETPIPE_SZ handle anonymous pipes but not FIFO; nonpipe error differs from Linux EBADF; F_SETFL omits mutable O_NOATIME/O_DIRECT including packet-mode pipe transitions"]

[[contract]]
id = "linux-sysfs"
flags = ["flag:mode/type bits and native x86_64 directory-record format"]
structs = ["struct:native x86_64 sysfs argument/result layout"]
multiplexer_commands = ["mux:mode/type bits and native x86_64 directory-record format"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve directory identity and permissions; create typed named nodes or serialize native directory records and update cursor"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 139
name = "sysfs"
status = "implemented"
contract = "linux-sysfs"
handler = "kernel/src/syscall/fs/ctl.rs:sys_sysfs"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:named_create_preserves_trailing_and_special_terminal_syntax"]
validation_gaps = ["Existing source/helper coverage does not establish the full sysfs raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-chdir"
flags = ["flag:mode/type bits and native x86_64 directory-record format"]
structs = ["struct:native x86_64 chdir argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve directory identity and permissions; create typed named nodes or serialize native directory records and update cursor"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 80
name = "chdir"
status = "implemented"
contract = "linux-chdir"
handler = "kernel/src/syscall/fs/ctl.rs:sys_chdir"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:named_create_preserves_trailing_and_special_terminal_syntax"]
validation_gaps = ["Existing source/helper coverage does not establish the full chdir raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-fchdir"
flags = ["flag:mode/type bits and native x86_64 directory-record format"]
structs = ["struct:native x86_64 fchdir argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve directory identity and permissions; create typed named nodes or serialize native directory records and update cursor"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 81
name = "fchdir"
status = "implemented"
contract = "linux-fchdir"
handler = "kernel/src/syscall/fs/ctl.rs:sys_fchdir"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:named_create_preserves_trailing_and_special_terminal_syntax"]
validation_gaps = ["Existing source/helper coverage does not establish the full fchdir raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-chroot"
flags = ["flag:mode/type bits and native x86_64 directory-record format"]
structs = ["struct:native x86_64 chroot argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve directory identity and permissions; create typed named nodes or serialize native directory records and update cursor"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 161
name = "chroot"
status = "implemented"
contract = "linux-chroot"
handler = "kernel/src/syscall/fs/ctl.rs:sys_chroot"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:named_create_preserves_trailing_and_special_terminal_syntax"]
validation_gaps = ["Existing source/helper coverage does not establish the full chroot raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-mkdir"
flags = ["flag:mode/type bits and native x86_64 directory-record format"]
structs = ["struct:native x86_64 mkdir argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve directory identity and permissions; create typed named nodes or serialize native directory records and update cursor"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 83
name = "mkdir"
status = "implemented"
contract = "linux-mkdir"
handler = "kernel/src/syscall/fs/ctl.rs:sys_mkdir"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:named_create_preserves_trailing_and_special_terminal_syntax"]
validation_gaps = ["Existing source/helper coverage does not establish the full mkdir raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-mkdirat"
flags = ["flag:mode/type bits and native x86_64 directory-record format"]
structs = ["struct:native x86_64 mkdirat argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve directory identity and permissions; create typed named nodes or serialize native directory records and update cursor"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 258
name = "mkdirat"
status = "implemented"
contract = "linux-mkdirat"
handler = "kernel/src/syscall/fs/ctl.rs:sys_mkdirat"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:named_create_preserves_trailing_and_special_terminal_syntax"]
validation_gaps = ["Existing source/helper coverage does not establish the full mkdirat raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-mknodat"
flags = ["flag:mode/type bits and native x86_64 directory-record format"]
structs = ["struct:native x86_64 mknodat argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve directory identity and permissions; create typed named nodes or serialize native directory records and update cursor"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 259
name = "mknodat"
status = "implemented"
contract = "linux-mknodat"
handler = "kernel/src/syscall/fs/ctl.rs:sys_mknodat"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:named_create_preserves_trailing_and_special_terminal_syntax"]
validation_gaps = ["Existing source/helper coverage does not establish the full mknodat raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-getdents"
flags = ["flag:mode/type bits and native x86_64 directory-record format"]
structs = ["struct:native x86_64 getdents argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve directory identity and permissions; create typed named nodes or serialize native directory records and update cursor"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 78
name = "getdents"
status = "implemented"
contract = "linux-getdents"
handler = "kernel/src/syscall/fs/ctl.rs:sys_getdents"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:named_create_preserves_trailing_and_special_terminal_syntax"]
validation_gaps = ["Existing source/helper coverage does not establish the full getdents raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-getdents64"
flags = ["flag:mode/type bits and native x86_64 directory-record format"]
structs = ["struct:native x86_64 getdents64 argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve directory identity and permissions; create typed named nodes or serialize native directory records and update cursor"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 217
name = "getdents64"
status = "implemented"
contract = "linux-getdents64"
handler = "kernel/src/syscall/fs/ctl.rs:sys_getdents64"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:named_create_preserves_trailing_and_special_terminal_syntax"]
validation_gaps = ["Existing source/helper coverage does not establish the full getdents64 raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-link"
flags = ["flag:linkat EMPTY_PATH/SYMLINK_FOLLOW and unlinkat REMOVEDIR"]
structs = ["struct:native x86_64 link argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve hard-link source and terminal path syntax; atomically mutate namespace with ownership and directory constraints"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 86
name = "link"
status = "implemented"
contract = "linux-link"
handler = "kernel/src/syscall/fs/ctl.rs:sys_link"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:unlink_target_resolution_never_retargets_trailing_or_dot_syntax"]
validation_gaps = ["Existing source/helper coverage does not establish the full link raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-linkat"
flags = ["flag:linkat EMPTY_PATH/SYMLINK_FOLLOW and unlinkat REMOVEDIR"]
structs = ["struct:native x86_64 linkat argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve hard-link source and terminal path syntax; atomically mutate namespace with ownership and directory constraints"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 265
name = "linkat"
status = "implemented"
contract = "linux-linkat"
handler = "kernel/src/syscall/fs/ctl.rs:sys_linkat"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:unlink_target_resolution_never_retargets_trailing_or_dot_syntax"]
validation_gaps = ["Existing source/helper coverage does not establish the full linkat raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-unlink"
flags = ["flag:linkat EMPTY_PATH/SYMLINK_FOLLOW and unlinkat REMOVEDIR"]
structs = ["struct:native x86_64 unlink argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve hard-link source and terminal path syntax; atomically mutate namespace with ownership and directory constraints"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 87
name = "unlink"
status = "implemented"
contract = "linux-unlink"
handler = "kernel/src/syscall/fs/ctl.rs:sys_unlink"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:unlink_target_resolution_never_retargets_trailing_or_dot_syntax"]
validation_gaps = ["Existing source/helper coverage does not establish the full unlink raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-unlinkat"
flags = ["flag:linkat EMPTY_PATH/SYMLINK_FOLLOW and unlinkat REMOVEDIR"]
structs = ["struct:native x86_64 unlinkat argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve hard-link source and terminal path syntax; atomically mutate namespace with ownership and directory constraints"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 263
name = "unlinkat"
status = "implemented"
contract = "linux-unlinkat"
handler = "kernel/src/syscall/fs/ctl.rs:sys_unlinkat"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:unlink_target_resolution_never_retargets_trailing_or_dot_syntax"]
validation_gaps = ["Existing source/helper coverage does not establish the full unlinkat raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-rmdir"
flags = ["flag:linkat EMPTY_PATH/SYMLINK_FOLLOW and unlinkat REMOVEDIR"]
structs = ["struct:native x86_64 rmdir argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve hard-link source and terminal path syntax; atomically mutate namespace with ownership and directory constraints"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 84
name = "rmdir"
status = "implemented"
contract = "linux-rmdir"
handler = "kernel/src/syscall/fs/ctl.rs:sys_rmdir"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:unlink_target_resolution_never_retargets_trailing_or_dot_syntax"]
validation_gaps = ["Existing source/helper coverage does not establish the full rmdir raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-symlink"
flags = ["flag:dirfd and empty-path link lookup"]
structs = ["struct:native x86_64 symlink argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:preserve symlink target bytes and return bounded non-NUL-terminated link contents with fd/path origin rules"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 88
name = "symlink"
status = "implemented"
contract = "linux-symlink"
handler = "kernel/src/syscall/fs/ctl.rs:sys_symlink"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:symlink_target_uses_linux_empty_and_path_max_rules_without_name_max"]
validation_gaps = ["Existing source/helper coverage does not establish the full symlink raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-symlinkat"
flags = ["flag:dirfd and empty-path link lookup"]
structs = ["struct:native x86_64 symlinkat argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:preserve symlink target bytes and return bounded non-NUL-terminated link contents with fd/path origin rules"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 266
name = "symlinkat"
status = "implemented"
contract = "linux-symlinkat"
handler = "kernel/src/syscall/fs/ctl.rs:sys_symlinkat"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:symlink_target_uses_linux_empty_and_path_max_rules_without_name_max"]
validation_gaps = ["Existing source/helper coverage does not establish the full symlinkat raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-readlink"
flags = ["flag:dirfd and empty-path link lookup"]
structs = ["struct:native x86_64 readlink argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:preserve symlink target bytes and return bounded non-NUL-terminated link contents with fd/path origin rules"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 89
name = "readlink"
status = "implemented"
contract = "linux-readlink"
handler = "kernel/src/syscall/fs/ctl.rs:sys_readlink"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:symlink_target_uses_linux_empty_and_path_max_rules_without_name_max"]
validation_gaps = ["Existing source/helper coverage does not establish the full readlink raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-readlinkat"
flags = ["flag:dirfd and empty-path link lookup"]
structs = ["struct:native x86_64 readlinkat argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:preserve symlink target bytes and return bounded non-NUL-terminated link contents with fd/path origin rules"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 267
name = "readlinkat"
status = "implemented"
contract = "linux-readlinkat"
handler = "kernel/src/syscall/fs/ctl.rs:sys_readlinkat"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:symlink_target_uses_linux_empty_and_path_max_rules_without_name_max"]
validation_gaps = ["Existing source/helper coverage does not establish the full readlinkat raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-chown"
flags = ["flag:ownership unchanged sentinels, mode bits and per-entry AT_* flags"]
structs = ["struct:native x86_64 chown argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize metadata changes using snapshot taken within writer gate, preserving idmap and fd/path origin"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 92
name = "chown"
status = "implemented"
contract = "linux-chown"
handler = "kernel/src/syscall/fs/ctl.rs:sys_chown"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:metadata_syscalls_reject_invalid_flags_before_faulting_the_path_pointer"]
validation_gaps = ["Existing source/helper coverage does not establish the full chown raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-lchown"
flags = ["flag:ownership unchanged sentinels, mode bits and per-entry AT_* flags"]
structs = ["struct:native x86_64 lchown argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize metadata changes using snapshot taken within writer gate, preserving idmap and fd/path origin"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 94
name = "lchown"
status = "implemented"
contract = "linux-lchown"
handler = "kernel/src/syscall/fs/ctl.rs:sys_lchown"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:metadata_syscalls_reject_invalid_flags_before_faulting_the_path_pointer"]
validation_gaps = ["Existing source/helper coverage does not establish the full lchown raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-fchown"
flags = ["flag:ownership unchanged sentinels, mode bits and per-entry AT_* flags"]
structs = ["struct:native x86_64 fchown argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize metadata changes using snapshot taken within writer gate, preserving idmap and fd/path origin"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 93
name = "fchown"
status = "implemented"
contract = "linux-fchown"
handler = "kernel/src/syscall/fs/ctl.rs:sys_fchown"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:metadata_syscalls_reject_invalid_flags_before_faulting_the_path_pointer"]
validation_gaps = ["Existing source/helper coverage does not establish the full fchown raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-fchownat"
flags = ["flag:ownership unchanged sentinels, mode bits and per-entry AT_* flags"]
structs = ["struct:native x86_64 fchownat argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize metadata changes using snapshot taken within writer gate, preserving idmap and fd/path origin"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 260
name = "fchownat"
status = "implemented"
contract = "linux-fchownat"
handler = "kernel/src/syscall/fs/ctl.rs:sys_fchownat"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:metadata_syscalls_reject_invalid_flags_before_faulting_the_path_pointer"]
validation_gaps = ["Existing source/helper coverage does not establish the full fchownat raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-chmod"
flags = ["flag:ownership unchanged sentinels, mode bits and per-entry AT_* flags"]
structs = ["struct:native x86_64 chmod argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize metadata changes using snapshot taken within writer gate, preserving idmap and fd/path origin"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 90
name = "chmod"
status = "implemented"
contract = "linux-chmod"
handler = "kernel/src/syscall/fs/ctl.rs:sys_chmod"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:metadata_syscalls_reject_invalid_flags_before_faulting_the_path_pointer"]
validation_gaps = ["Existing source/helper coverage does not establish the full chmod raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-fchmod"
flags = ["flag:ownership unchanged sentinels, mode bits and per-entry AT_* flags"]
structs = ["struct:native x86_64 fchmod argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize metadata changes using snapshot taken within writer gate, preserving idmap and fd/path origin"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 91
name = "fchmod"
status = "implemented"
contract = "linux-fchmod"
handler = "kernel/src/syscall/fs/ctl.rs:sys_fchmod"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:metadata_syscalls_reject_invalid_flags_before_faulting_the_path_pointer"]
validation_gaps = ["Existing source/helper coverage does not establish the full fchmod raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-fchmodat"
flags = ["flag:ownership unchanged sentinels, mode bits and per-entry AT_* flags"]
structs = ["struct:native x86_64 fchmodat argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize metadata changes using snapshot taken within writer gate, preserving idmap and fd/path origin"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 268
name = "fchmodat"
status = "implemented"
contract = "linux-fchmodat"
handler = "kernel/src/syscall/fs/ctl.rs:sys_fchmodat"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:metadata_syscalls_reject_invalid_flags_before_faulting_the_path_pointer"]
validation_gaps = ["Existing source/helper coverage does not establish the full fchmodat raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-fchmodat2"
flags = ["flag:ownership unchanged sentinels, mode bits and per-entry AT_* flags"]
structs = ["struct:native x86_64 fchmodat2 argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize metadata changes using snapshot taken within writer gate, preserving idmap and fd/path origin"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 452
name = "fchmodat2"
status = "implemented"
contract = "linux-fchmodat2"
handler = "kernel/src/syscall/fs/ctl.rs:sys_fchmodat"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:metadata_syscalls_reject_invalid_flags_before_faulting_the_path_pointer"]
validation_gaps = ["Existing source/helper coverage does not establish the full fchmodat2 raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-utime"
flags = ["flag:NULL current-time update and native timestamp layouts"]
structs = ["struct:native x86_64 utime argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:decode second/microsecond timestamps, authorize explicit versus current-time updates, and commit inode metadata"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 132
name = "utime"
status = "implemented"
contract = "linux-utime"
handler = "kernel/src/syscall/fs/ctl.rs:sys_utime"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:metadata_syscalls_reject_invalid_flags_before_faulting_the_path_pointer"]
validation_gaps = ["Existing source/helper coverage does not establish the full utime raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-utimes"
flags = ["flag:NULL current-time update and native timestamp layouts"]
structs = ["struct:native x86_64 utimes argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:decode second/microsecond timestamps, authorize explicit versus current-time updates, and commit inode metadata"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 235
name = "utimes"
status = "implemented"
contract = "linux-utimes"
handler = "kernel/src/syscall/fs/ctl.rs:sys_utimes"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:metadata_syscalls_reject_invalid_flags_before_faulting_the_path_pointer"]
validation_gaps = ["Existing source/helper coverage does not establish the full utimes raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-futimesat"
flags = ["flag:NULL current-time update and native timestamp layouts"]
structs = ["struct:native x86_64 futimesat argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:decode second/microsecond timestamps, authorize explicit versus current-time updates, and commit inode metadata"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 261
name = "futimesat"
status = "implemented"
contract = "linux-futimesat"
handler = "kernel/src/syscall/fs/ctl.rs:sys_futimesat"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:metadata_syscalls_reject_invalid_flags_before_faulting_the_path_pointer"]
validation_gaps = ["Existing source/helper coverage does not establish the full futimesat raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-rename"
flags = ["flag:RENAME_NOREPLACE/EXCHANGE/WHITEOUT"]
structs = ["struct:native x86_64 rename argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:atomically rename/exchange namespace entries while enforcing terminal syntax and source/target directory conditions"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 82
name = "rename"
status = "implemented"
contract = "linux-rename"
handler = "kernel/src/syscall/fs/ctl.rs:sys_rename"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:renameat_preserves_special_components_and_noreplace_errno"]
validation_gaps = ["Existing source/helper coverage does not establish the full rename raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-renameat"
flags = ["flag:RENAME_NOREPLACE/EXCHANGE/WHITEOUT"]
structs = ["struct:native x86_64 renameat argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:atomically rename/exchange namespace entries while enforcing terminal syntax and source/target directory conditions"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 264
name = "renameat"
status = "implemented"
contract = "linux-renameat"
handler = "kernel/src/syscall/fs/ctl.rs:sys_renameat"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:renameat_preserves_special_components_and_noreplace_errno"]
validation_gaps = ["Existing source/helper coverage does not establish the full renameat raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-renameat2"
flags = ["flag:RENAME_NOREPLACE/EXCHANGE/WHITEOUT"]
structs = ["struct:native x86_64 renameat2 argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:atomically rename/exchange namespace entries while enforcing terminal syntax and source/target directory conditions"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 316
name = "renameat2"
status = "implemented"
contract = "linux-renameat2"
handler = "kernel/src/syscall/fs/ctl.rs:sys_renameat2"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/ctl.rs:renameat_preserves_special_components_and_noreplace_errno"]
validation_gaps = ["Existing source/helper coverage does not establish the full renameat2 raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-sync"
flags = ["flag:command-specific arguments"]
structs = ["struct:native x86_64 sync argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:flush filesystem providers, control terminal lifecycle, resolve current-root-relative path or authorize reboot commands"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 162
name = "sync"
status = "implemented"
contract = "linux-sync"
handler = "kernel/src/syscall/fs/ctl.rs:sys_sync"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full sync raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-vhangup"
flags = ["flag:command-specific arguments"]
structs = ["struct:native x86_64 vhangup argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:flush filesystem providers, control terminal lifecycle, resolve current-root-relative path or authorize reboot commands"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 153
name = "vhangup"
status = "implemented"
contract = "linux-vhangup"
handler = "kernel/src/syscall/fs/ctl.rs:sys_vhangup"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full vhangup raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-syncfs"
flags = ["flag:command-specific arguments"]
structs = ["struct:native x86_64 syncfs argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:flush filesystem providers, control terminal lifecycle, resolve current-root-relative path or authorize reboot commands"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 306
name = "syncfs"
status = "partial"
contract = "linux-syncfs"
handler = "kernel/src/syscall/fs/ctl.rs:sys_syncfs"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full syncfs raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Filesystem lookup excludes pipe, FIFO, socket and pidfd descriptors and returns EINVAL instead of accepting their mounted filesystem as Linux does"]

[[contract]]
id = "linux-getcwd"
flags = ["flag:command-specific arguments"]
structs = ["struct:native x86_64 getcwd argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:flush filesystem providers, control terminal lifecycle, resolve current-root-relative path or authorize reboot commands"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 79
name = "getcwd"
status = "partial"
contract = "linux-getcwd"
handler = "kernel/src/syscall/fs/ctl.rs:sys_getcwd"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full getcwd raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Current directory outside the process root returns ENOENT rather than Linux raw getcwd's (unreachable) prefix"]

[[contract]]
id = "linux-reboot"
flags = ["flag:command-specific arguments"]
structs = ["struct:native x86_64 reboot argument/result layout"]
multiplexer_commands = ["mux:command-specific arguments"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:flush filesystem providers, control terminal lifecycle, resolve current-root-relative path or authorize reboot commands"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 169
name = "reboot"
status = "partial"
contract = "linux-reboot"
handler = "kernel/src/syscall/fs/ctl.rs:sys_reboot"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full reboot raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["CAD_ON/CAD_OFF return EOPNOTSUPP; RESTART2 ignores its user argument rather than copying and forwarding the restart command"]

[[contract]]
id = "linux-read"
flags = ["flag:entry-specific offsets, iovecs, seek origins, splice and range-sync flags"]
structs = ["struct:native x86_64 read argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:admit descriptor capabilities and signed offsets, preserve partial transfer progress, serialize shared cursor and provider mutations, and perform requested provider sync/advice"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 0
name = "read"
status = "implemented"
contract = "linux-read"
handler = "kernel/src/syscall/fs/io.rs:sys_read"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/io.rs:stream_cursor_and_explicit_io_capabilities_are_independent"]
validation_gaps = ["Existing source/helper coverage does not establish the full read raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-write"
flags = ["flag:entry-specific offsets, iovecs, seek origins, splice and range-sync flags"]
structs = ["struct:native x86_64 write argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:admit descriptor capabilities and signed offsets, preserve partial transfer progress, serialize shared cursor and provider mutations, and perform requested provider sync/advice"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 1
name = "write"
status = "implemented"
contract = "linux-write"
handler = "kernel/src/syscall/fs/io.rs:sys_write"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/io.rs:stream_cursor_and_explicit_io_capabilities_are_independent"]
validation_gaps = ["Existing source/helper coverage does not establish the full write raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-readv"
flags = ["flag:entry-specific offsets, iovecs, seek origins, splice and range-sync flags"]
structs = ["struct:native x86_64 readv argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:admit descriptor capabilities and signed offsets, preserve partial transfer progress, serialize shared cursor and provider mutations, and perform requested provider sync/advice"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 19
name = "readv"
status = "implemented"
contract = "linux-readv"
handler = "kernel/src/syscall/fs/io.rs:sys_readv"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/io.rs:stream_cursor_and_explicit_io_capabilities_are_independent"]
validation_gaps = ["Existing source/helper coverage does not establish the full readv raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-writev"
flags = ["flag:entry-specific offsets, iovecs, seek origins, splice and range-sync flags"]
structs = ["struct:native x86_64 writev argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:admit descriptor capabilities and signed offsets, preserve partial transfer progress, serialize shared cursor and provider mutations, and perform requested provider sync/advice"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 20
name = "writev"
status = "implemented"
contract = "linux-writev"
handler = "kernel/src/syscall/fs/io.rs:sys_writev"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/io.rs:stream_cursor_and_explicit_io_capabilities_are_independent"]
validation_gaps = ["Existing source/helper coverage does not establish the full writev raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-lseek"
flags = ["flag:entry-specific offsets, iovecs, seek origins, splice and range-sync flags"]
structs = ["struct:native x86_64 lseek argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:admit descriptor capabilities and signed offsets, preserve partial transfer progress, serialize shared cursor and provider mutations, and perform requested provider sync/advice"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 8
name = "lseek"
status = "implemented"
contract = "linux-lseek"
handler = "kernel/src/syscall/fs/io.rs:sys_lseek"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/io.rs:stream_cursor_and_explicit_io_capabilities_are_independent"]
validation_gaps = ["Existing source/helper coverage does not establish the full lseek raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-pread64"
flags = ["flag:entry-specific offsets, iovecs, seek origins, splice and range-sync flags"]
structs = ["struct:native x86_64 pread64 argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:admit descriptor capabilities and signed offsets, preserve partial transfer progress, serialize shared cursor and provider mutations, and perform requested provider sync/advice"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 17
name = "pread64"
status = "implemented"
contract = "linux-pread64"
handler = "kernel/src/syscall/fs/io.rs:sys_pread64"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/io.rs:stream_cursor_and_explicit_io_capabilities_are_independent"]
validation_gaps = ["Existing source/helper coverage does not establish the full pread64 raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-pwrite64"
flags = ["flag:entry-specific offsets, iovecs, seek origins, splice and range-sync flags"]
structs = ["struct:native x86_64 pwrite64 argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:admit descriptor capabilities and signed offsets, preserve partial transfer progress, serialize shared cursor and provider mutations, and perform requested provider sync/advice"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 18
name = "pwrite64"
status = "implemented"
contract = "linux-pwrite64"
handler = "kernel/src/syscall/fs/io.rs:sys_pwrite64"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/io.rs:stream_cursor_and_explicit_io_capabilities_are_independent"]
validation_gaps = ["Existing source/helper coverage does not establish the full pwrite64 raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-preadv"
flags = ["flag:entry-specific offsets, iovecs, seek origins, splice and range-sync flags"]
structs = ["struct:native x86_64 preadv argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:admit descriptor capabilities and signed offsets, preserve partial transfer progress, serialize shared cursor and provider mutations, and perform requested provider sync/advice"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 295
name = "preadv"
status = "implemented"
contract = "linux-preadv"
handler = "kernel/src/syscall/fs/io.rs:sys_preadv"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/io.rs:stream_cursor_and_explicit_io_capabilities_are_independent"]
validation_gaps = ["Existing source/helper coverage does not establish the full preadv raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-pwritev"
flags = ["flag:entry-specific offsets, iovecs, seek origins, splice and range-sync flags"]
structs = ["struct:native x86_64 pwritev argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:admit descriptor capabilities and signed offsets, preserve partial transfer progress, serialize shared cursor and provider mutations, and perform requested provider sync/advice"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 296
name = "pwritev"
status = "implemented"
contract = "linux-pwritev"
handler = "kernel/src/syscall/fs/io.rs:sys_pwritev"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/io.rs:stream_cursor_and_explicit_io_capabilities_are_independent"]
validation_gaps = ["Existing source/helper coverage does not establish the full pwritev raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-sendfile"
flags = ["flag:entry-specific offsets, iovecs, seek origins, splice and range-sync flags"]
structs = ["struct:native x86_64 sendfile argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:admit descriptor capabilities and signed offsets, preserve partial transfer progress, serialize shared cursor and provider mutations, and perform requested provider sync/advice"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 40
name = "sendfile"
status = "implemented"
contract = "linux-sendfile"
handler = "kernel/src/syscall/fs/io.rs:sys_sendfile"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/io.rs:stream_cursor_and_explicit_io_capabilities_are_independent"]
validation_gaps = ["Existing source/helper coverage does not establish the full sendfile raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-splice"
flags = ["flag:entry-specific offsets, iovecs, seek origins, splice and range-sync flags"]
structs = ["struct:native x86_64 splice argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:admit descriptor capabilities and signed offsets, preserve partial transfer progress, serialize shared cursor and provider mutations, and perform requested provider sync/advice"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 275
name = "splice"
status = "implemented"
contract = "linux-splice"
handler = "kernel/src/syscall/fs/io.rs:sys_splice"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/io.rs:stream_cursor_and_explicit_io_capabilities_are_independent"]
validation_gaps = ["Existing source/helper coverage does not establish the full splice raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-copy_file_range"
flags = ["flag:entry-specific offsets, iovecs, seek origins, splice and range-sync flags"]
structs = ["struct:native x86_64 copy_file_range argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:admit descriptor capabilities and signed offsets, preserve partial transfer progress, serialize shared cursor and provider mutations, and perform requested provider sync/advice"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 326
name = "copy_file_range"
status = "implemented"
contract = "linux-copy_file_range"
handler = "kernel/src/syscall/fs/io.rs:sys_copy_file_range"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/io.rs:stream_cursor_and_explicit_io_capabilities_are_independent"]
validation_gaps = ["Existing source/helper coverage does not establish the full copy_file_range raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-fsync"
flags = ["flag:entry-specific offsets, iovecs, seek origins, splice and range-sync flags"]
structs = ["struct:native x86_64 fsync argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:admit descriptor capabilities and signed offsets, preserve partial transfer progress, serialize shared cursor and provider mutations, and perform requested provider sync/advice"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 74
name = "fsync"
status = "implemented"
contract = "linux-fsync"
handler = "kernel/src/syscall/fs/io.rs:sys_fsync"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/io.rs:stream_cursor_and_explicit_io_capabilities_are_independent"]
validation_gaps = ["Existing source/helper coverage does not establish the full fsync raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-fdatasync"
flags = ["flag:entry-specific offsets, iovecs, seek origins, splice and range-sync flags"]
structs = ["struct:native x86_64 fdatasync argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:admit descriptor capabilities and signed offsets, preserve partial transfer progress, serialize shared cursor and provider mutations, and perform requested provider sync/advice"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 75
name = "fdatasync"
status = "implemented"
contract = "linux-fdatasync"
handler = "kernel/src/syscall/fs/io.rs:sys_fdatasync"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/io.rs:stream_cursor_and_explicit_io_capabilities_are_independent"]
validation_gaps = ["Existing source/helper coverage does not establish the full fdatasync raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-truncate"
flags = ["flag:entry-specific offsets, iovecs, seek origins, splice and range-sync flags"]
structs = ["struct:native x86_64 truncate argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:admit descriptor capabilities and signed offsets, preserve partial transfer progress, serialize shared cursor and provider mutations, and perform requested provider sync/advice"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 76
name = "truncate"
status = "implemented"
contract = "linux-truncate"
handler = "kernel/src/syscall/fs/io.rs:sys_truncate"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/io.rs:stream_cursor_and_explicit_io_capabilities_are_independent"]
validation_gaps = ["Existing source/helper coverage does not establish the full truncate raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-ftruncate"
flags = ["flag:entry-specific offsets, iovecs, seek origins, splice and range-sync flags"]
structs = ["struct:native x86_64 ftruncate argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:admit descriptor capabilities and signed offsets, preserve partial transfer progress, serialize shared cursor and provider mutations, and perform requested provider sync/advice"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 77
name = "ftruncate"
status = "implemented"
contract = "linux-ftruncate"
handler = "kernel/src/syscall/fs/io.rs:sys_ftruncate"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/io.rs:stream_cursor_and_explicit_io_capabilities_are_independent"]
validation_gaps = ["Existing source/helper coverage does not establish the full ftruncate raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-fadvise64"
flags = ["flag:entry-specific offsets, iovecs, seek origins, splice and range-sync flags"]
structs = ["struct:native x86_64 fadvise64 argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:admit descriptor capabilities and signed offsets, preserve partial transfer progress, serialize shared cursor and provider mutations, and perform requested provider sync/advice"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 221
name = "fadvise64"
status = "implemented"
contract = "linux-fadvise64"
handler = "kernel/src/syscall/fs/io.rs:sys_fadvise64"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/io.rs:stream_cursor_and_explicit_io_capabilities_are_independent"]
validation_gaps = ["Existing source/helper coverage does not establish the full fadvise64 raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-sync_file_range"
flags = ["flag:entry-specific offsets, iovecs, seek origins, splice and range-sync flags"]
structs = ["struct:native x86_64 sync_file_range argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:admit descriptor capabilities and signed offsets, preserve partial transfer progress, serialize shared cursor and provider mutations, and perform requested provider sync/advice"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 277
name = "sync_file_range"
status = "implemented"
contract = "linux-sync_file_range"
handler = "kernel/src/syscall/fs/io.rs:sys_sync_file_range"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/io.rs:stream_cursor_and_explicit_io_capabilities_are_independent"]
validation_gaps = ["Existing source/helper coverage does not establish the full sync_file_range raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-preadv2"
flags = ["flag:RWF_* or SPLICE_F_*"]
structs = ["struct:native x86_64 preadv2 argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:transfer vectored or pipe-buffer data with caller memory checks and partial-progress semantics"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 327
name = "preadv2"
status = "partial"
contract = "linux-preadv2"
handler = "kernel/src/syscall/fs/io.rs:sys_preadv2"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full preadv2 raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["RWF_HIPRI is rejected although Linux accepts it; RWF_ATOMIC remains unsupported subject to provider capability"]

[[contract]]
id = "linux-pwritev2"
flags = ["flag:RWF_* or SPLICE_F_*"]
structs = ["struct:native x86_64 pwritev2 argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:transfer vectored or pipe-buffer data with caller memory checks and partial-progress semantics"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 328
name = "pwritev2"
status = "partial"
contract = "linux-pwritev2"
handler = "kernel/src/syscall/fs/io.rs:sys_pwritev2"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full pwritev2 raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["RWF_HIPRI is rejected although Linux accepts it; RWF_ATOMIC remains unsupported subject to provider capability"]

[[contract]]
id = "linux-tee"
flags = ["flag:RWF_* or SPLICE_F_*"]
structs = ["struct:native x86_64 tee argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:transfer vectored or pipe-buffer data with caller memory checks and partial-progress semantics"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 276
name = "tee"
status = "partial"
contract = "linux-tee"
handler = "kernel/src/syscall/fs/io.rs:sys_tee"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full tee raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Only anonymous Pipe open-file descriptions are accepted; named FIFO pipe-buffer transfer is missing"]

[[contract]]
id = "linux-vmsplice"
flags = ["flag:RWF_* or SPLICE_F_*"]
structs = ["struct:native x86_64 vmsplice argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:transfer vectored or pipe-buffer data with caller memory checks and partial-progress semantics"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 278
name = "vmsplice"
status = "partial"
contract = "linux-vmsplice"
handler = "kernel/src/syscall/fs/io.rs:sys_vmsplice"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full vmsplice raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Only anonymous Pipe open-file descriptions are accepted; named FIFO pipe-buffer transfer is missing"]

[[contract]]
id = "linux-io_setup"
flags = ["flag:native IOCB opcode/RESFD/IOPRIO and timespec/sigmask layouts"]
structs = ["struct:native x86_64 io_setup argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage native AIO context ownership, submit decoded IOCBs and deliver completion-ring events with signal-mask handling"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 206
name = "io_setup"
status = "implemented"
contract = "linux-io_setup"
handler = "kernel/src/syscall/fs/aio.rs:sys_io_setup"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/aio.rs:context_ids_wrap_skip_live_contexts_and_never_use_zero"]
validation_gaps = ["Existing source/helper coverage does not establish the full io_setup raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-io_submit"
flags = ["flag:native IOCB opcode/RESFD/IOPRIO and timespec/sigmask layouts"]
structs = ["struct:native x86_64 io_submit argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage native AIO context ownership, submit decoded IOCBs and deliver completion-ring events with signal-mask handling"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 209
name = "io_submit"
status = "implemented"
contract = "linux-io_submit"
handler = "kernel/src/syscall/fs/aio.rs:sys_io_submit"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/aio.rs:context_ids_wrap_skip_live_contexts_and_never_use_zero"]
validation_gaps = ["Existing source/helper coverage does not establish the full io_submit raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-io_getevents"
flags = ["flag:native IOCB opcode/RESFD/IOPRIO and timespec/sigmask layouts"]
structs = ["struct:native x86_64 io_getevents argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage native AIO context ownership, submit decoded IOCBs and deliver completion-ring events with signal-mask handling"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 208
name = "io_getevents"
status = "implemented"
contract = "linux-io_getevents"
handler = "kernel/src/syscall/fs/aio.rs:sys_io_getevents"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/aio.rs:context_ids_wrap_skip_live_contexts_and_never_use_zero"]
validation_gaps = ["Existing source/helper coverage does not establish the full io_getevents raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-io_pgetevents"
flags = ["flag:native IOCB opcode/RESFD/IOPRIO and timespec/sigmask layouts"]
structs = ["struct:native x86_64 io_pgetevents argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage native AIO context ownership, submit decoded IOCBs and deliver completion-ring events with signal-mask handling"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 333
name = "io_pgetevents"
status = "implemented"
contract = "linux-io_pgetevents"
handler = "kernel/src/syscall/fs/aio.rs:sys_io_pgetevents"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/aio.rs:context_ids_wrap_skip_live_contexts_and_never_use_zero"]
validation_gaps = ["Existing source/helper coverage does not establish the full io_pgetevents raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-io_destroy"
flags = ["flag:native IOCB opcode/RESFD/IOPRIO and timespec/sigmask layouts"]
structs = ["struct:native x86_64 io_destroy argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage native AIO context ownership, submit decoded IOCBs and deliver completion-ring events with signal-mask handling"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 207
name = "io_destroy"
status = "partial"
contract = "linux-io_destroy"
handler = "kernel/src/syscall/fs/aio.rs:sys_io_destroy"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/aio.rs:context_ids_wrap_skip_live_contexts_and_never_use_zero"]
validation_gaps = ["Existing source/helper coverage does not establish the full io_destroy raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Retires requests without waiting for all in-flight provider operations, unlike Linux kill_ioctx completion wait"]

[[contract]]
id = "linux-io_cancel"
flags = ["flag:native IOCB opcode/RESFD/IOPRIO and timespec/sigmask layouts"]
structs = ["struct:native x86_64 io_cancel argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage native AIO context ownership, submit decoded IOCBs and deliver completion-ring events with signal-mask handling"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 210
name = "io_cancel"
status = "partial"
contract = "linux-io_cancel"
handler = "kernel/src/syscall/fs/aio.rs:sys_io_cancel"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/aio.rs:context_ids_wrap_skip_live_contexts_and_never_use_zero"]
validation_gaps = ["Existing source/helper coverage does not establish the full io_cancel raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Copies the full IOCB rather than only aio_key, writes deprecated result and returns zero rather than Linux completion-ring delivery with EINPROGRESS"]

[[contract]]
id = "linux-pipe"
flags = ["flag:O_CLOEXEC/O_NONBLOCK/O_DIRECT and notification pipe flags"]
structs = ["struct:native x86_64 pipe argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:allocate paired pipe endpoints and release them with descriptor lifetime"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 22
name = "pipe"
status = "implemented"
contract = "linux-pipe"
handler = "kernel/src/syscall/fs/pipe.rs:sys_pipe2"
conditional = "explicit-none"
tests = ["kernel/src/file/pipe.rs:one_write_uses_one_nonblocking_snapshot"]
validation_gaps = ["Existing source/helper coverage does not establish the full pipe raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-pipe2"
flags = ["flag:O_CLOEXEC/O_NONBLOCK/O_DIRECT and notification pipe flags"]
structs = ["struct:native x86_64 pipe2 argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:allocate paired pipe endpoints and release them with descriptor lifetime"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 293
name = "pipe2"
status = "partial"
contract = "linux-pipe2"
handler = "kernel/src/syscall/fs/pipe.rs:sys_pipe2"
conditional = "explicit-none"
tests = ["kernel/src/file/pipe.rs:one_write_uses_one_nonblocking_snapshot"]
validation_gaps = ["Existing source/helper coverage does not establish the full pipe2 raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["O_DIRECT does not implement packet-mode buffering; O_NOTIFICATION_PIPE returns EINVAL instead of ENOPKG for the selected Linux CONFIG_WATCH_QUEUE=n build"]

[[contract]]
id = "linux-pidfd_open"
flags = ["flag:pidfd thread/nonblock and signal scope flags"]
structs = ["struct:native x86_64 pidfd_open argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:retain stable task identity across PID reuse and authorize descriptor duplication or signal delivery"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 434
name = "pidfd_open"
status = "implemented"
contract = "linux-pidfd_open"
handler = "kernel/src/syscall/fs/pidfd.rs:sys_pidfd_open"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/pidfd.rs:process_pidfd_identity_rejects_reap_and_pid_reuse"]
validation_gaps = ["Existing source/helper coverage does not establish the full pidfd_open raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-pidfd_getfd"
flags = ["flag:pidfd thread/nonblock and signal scope flags"]
structs = ["struct:native x86_64 pidfd_getfd argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:retain stable task identity across PID reuse and authorize descriptor duplication or signal delivery"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 438
name = "pidfd_getfd"
status = "implemented"
contract = "linux-pidfd_getfd"
handler = "kernel/src/syscall/fs/pidfd.rs:sys_pidfd_getfd"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/pidfd.rs:process_pidfd_identity_rejects_reap_and_pid_reuse"]
validation_gaps = ["Existing source/helper coverage does not establish the full pidfd_getfd raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-pidfd_send_signal"
flags = ["flag:pidfd thread/nonblock and signal scope flags"]
structs = ["struct:native x86_64 pidfd_send_signal argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:retain stable task identity across PID reuse and authorize descriptor duplication or signal delivery"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 424
name = "pidfd_send_signal"
status = "partial"
contract = "linux-pidfd_send_signal"
handler = "kernel/src/syscall/fs/pidfd.rs:sys_pidfd_send_signal"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/pidfd.rs:process_pidfd_identity_rejects_reap_and_pid_reuse"]
validation_gaps = ["Existing source/helper coverage does not establish the full pidfd_send_signal raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Only zero flags are accepted; Linux PIDFD_SIGNAL_THREAD/THREAD_GROUP/PROCESS_GROUP and PIDFD_SELF_THREAD/PIDFD_SELF_THREAD_GROUP identifiers are missing"]

[[contract]]
id = "linux-cachestat"
flags = ["flag:cachestat range and flags"]
structs = ["struct:native x86_64 cachestat argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:validate regular-file cache query admission and return range residency/dirty/writeback/eviction counters"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 451
name = "cachestat"
status = "implemented"
contract = "linux-cachestat"
handler = "kernel/src/syscall/fs/cachestat.rs:sys_cachestat"
conditional = "explicit-none"
tests = ["kernel/src/syscall/fs/cachestat.rs:cachestat_model_preserves_range_type_and_admission_boundaries"]
validation_gaps = ["Existing source/helper coverage does not establish the full cachestat raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-mknod"
flags = ["flag:mode and device encoding"]
structs = ["struct:native x86_64 mknod argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:decode low32 device number and create a typed named node relative to AT_FDCWD"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 133
name = "mknod"
status = "implemented"
contract = "linux-mknod"
handler = "kernel/src/syscall/dispatch.rs:compat_mknod"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full mknod raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-inotify_init"
flags = ["flag:IN_* watch flags and init NONBLOCK/CLOEXEC"]
structs = ["struct:native x86_64 inotify_init argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage inotify instance and watch identity, update masks, enqueue filesystem events and IN_IGNORED on removal"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 253
name = "inotify_init"
status = "implemented"
contract = "linux-inotify_init"
handler = "kernel/src/syscall/dispatch.rs:compat_inotify_init"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full inotify_init raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-inotify_add_watch"
flags = ["flag:IN_* watch flags and init NONBLOCK/CLOEXEC"]
structs = ["struct:native x86_64 inotify_add_watch argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage inotify instance and watch identity, update masks, enqueue filesystem events and IN_IGNORED on removal"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 254
name = "inotify_add_watch"
status = "implemented"
contract = "linux-inotify_add_watch"
handler = "kernel/src/syscall/fs/inotify.rs:sys_inotify_add_watch"
conditional = "explicit-none"
tests = ["tests/guest/portable/fs-boundary-differential.c:main"]
validation_gaps = ["Existing source/helper coverage does not establish the full inotify_add_watch raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-inotify_rm_watch"
flags = ["flag:IN_* watch flags and init NONBLOCK/CLOEXEC"]
structs = ["struct:native x86_64 inotify_rm_watch argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage inotify instance and watch identity, update masks, enqueue filesystem events and IN_IGNORED on removal"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 255
name = "inotify_rm_watch"
status = "implemented"
contract = "linux-inotify_rm_watch"
handler = "kernel/src/syscall/fs/inotify.rs:sys_inotify_rm_watch"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full inotify_rm_watch raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-inotify_init1"
flags = ["flag:IN_* watch flags and init NONBLOCK/CLOEXEC"]
structs = ["struct:native x86_64 inotify_init1 argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage inotify instance and watch identity, update masks, enqueue filesystem events and IN_IGNORED on removal"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 294
name = "inotify_init1"
status = "implemented"
contract = "linux-inotify_init1"
handler = "kernel/src/syscall/fs/inotify.rs:sys_inotify_init1"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full inotify_init1 raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-signalfd"
flags = ["flag:native sigset size and NONBLOCK/CLOEXEC"]
structs = ["struct:native x86_64 signalfd argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:snapshot signal mask, exclude uncatchable signals and bind readable signal records to an owned descriptor"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 282
name = "signalfd"
status = "implemented"
contract = "linux-signalfd"
handler = "kernel/src/syscall/dispatch.rs:compat_signalfd"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full signalfd raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-signalfd4"
flags = ["flag:native sigset size and NONBLOCK/CLOEXEC"]
structs = ["struct:native x86_64 signalfd4 argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:snapshot signal mask, exclude uncatchable signals and bind readable signal records to an owned descriptor"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 289
name = "signalfd4"
status = "implemented"
contract = "linux-signalfd4"
handler = "kernel/src/syscall/fs/signalfd.rs:sys_signalfd4"
conditional = "explicit-none"
tests = ["tests/guest/portable/fs-boundary-differential.c:main"]
validation_gaps = ["Existing source/helper coverage does not establish the full signalfd4 raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-timerfd_create"
flags = ["flag:TFD_* and clock IDs"]
structs = ["struct:native x86_64 timerfd_create argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage timer descriptor expiration count, absolute/relative arming, cancellation and remaining-time output"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 283
name = "timerfd_create"
status = "partial"
contract = "linux-timerfd_create"
handler = "kernel/src/syscall/fs/timerfd.rs:sys_timerfd_create"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full timerfd_create raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["CLOCK_REALTIME_ALARM/CLOCK_BOOTTIME_ALARM and their CAP_WAKE_ALARM admission are missing"]

[[contract]]
id = "linux-timerfd_settime"
flags = ["flag:TFD_* and clock IDs"]
structs = ["struct:native x86_64 timerfd_settime argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage timer descriptor expiration count, absolute/relative arming, cancellation and remaining-time output"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 286
name = "timerfd_settime"
status = "implemented"
contract = "linux-timerfd_settime"
handler = "kernel/src/syscall/fs/timerfd.rs:sys_timerfd_settime"
conditional = "explicit-none"
tests = ["tests/guest/portable/fs-boundary-differential.c:main"]
validation_gaps = ["Existing source/helper coverage does not establish the full timerfd_settime raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-timerfd_gettime"
flags = ["flag:TFD_* and clock IDs"]
structs = ["struct:native x86_64 timerfd_gettime argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage timer descriptor expiration count, absolute/relative arming, cancellation and remaining-time output"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 287
name = "timerfd_gettime"
status = "implemented"
contract = "linux-timerfd_gettime"
handler = "kernel/src/syscall/fs/timerfd.rs:sys_timerfd_gettime"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full timerfd_gettime raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-fanotify_init"
flags = ["flag:FAN_* flags and event metadata"]
structs = ["struct:native x86_64 fanotify_init argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage fanotify notification groups, marks and permission-response lifecycle"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 300
name = "fanotify_init"
status = "partial"
contract = "linux-fanotify_init"
handler = "kernel/src/syscall/fs/fanotify.rs:sys_fanotify_init"
conditional = "explicit-none"
tests = []
validation_gaps = ["Selected Linux oracle CONFIG_FANOTIFY=n returns ENOSYS while TheKernel implements this facility; a feature-enabled oracle is required to compare the implemented subset, and the missing Linux UAPI flags remain implementation limitations"]
limitations = ["FAN_REPORT_FD_ERROR and FAN_REPORT_MNT are not implemented"]

[[contract]]
id = "linux-fanotify_mark"
flags = ["flag:FAN_* flags and event metadata"]
structs = ["struct:native x86_64 fanotify_mark argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage fanotify notification groups, marks and permission-response lifecycle"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 301
name = "fanotify_mark"
status = "partial"
contract = "linux-fanotify_mark"
handler = "kernel/src/syscall/fs/fanotify.rs:sys_fanotify_mark"
conditional = "explicit-none"
tests = []
validation_gaps = ["Selected Linux oracle CONFIG_FANOTIFY=n returns ENOSYS while TheKernel implements this facility; a feature-enabled oracle is required to compare the implemented subset, and the missing Linux UAPI flags remain implementation limitations"]
limitations = ["FAN_MARK_MNTNS and associated mount-notification event support are missing"]

[[contract]]
id = "linux-quotactl"
flags = ["flag:Q_* and Q_X* command families"]
structs = ["struct:native x86_64 quotactl argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize quota commands and read/update per-filesystem quota state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 179
name = "quotactl"
status = "partial"
contract = "linux-quotactl"
handler = "kernel/src/syscall/fs/quota.rs:sys_quotactl"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full quotactl raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Path lookup accepts ordinary files rather than requiring a block device; Q_XGETQSTAT/Q_XGETQSTATV/Q_XGETQUOTA/Q_XGETNEXTQUOTA are missing for ext4; Q_SETINFO does not enforce Linux dqi_valid and DQF_SETINFO_MASK validation"]

[[contract]]
id = "linux-quotactl_fd"
flags = ["flag:Q_* and Q_X* command families"]
structs = ["struct:native x86_64 quotactl_fd argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize quota commands and read/update per-filesystem quota state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 443
name = "quotactl_fd"
status = "partial"
contract = "linux-quotactl_fd"
handler = "kernel/src/syscall/fs/quota.rs:sys_quotactl_fd"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full quotactl_fd raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Mount fds are rejected because fd location lookup only accepts ordinary file/directory; Q_XGETQSTAT/Q_XGETQSTATV/Q_XGETQUOTA/Q_XGETNEXTQUOTA are missing for ext4; Q_SETINFO does not enforce Linux dqi_valid and DQF_SETINFO_MASK validation"]

[[contract]]
id = "linux-mount"
flags = ["flag:entry-specific MS_*, OPEN_TREE_*, FSMOUNT_*, MOVE_MOUNT_* and MOUNT_ATTR_* flags"]
structs = ["struct:native x86_64 mount argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve stable mount identity, enforce namespace and capability authority, and query or mutate mounted filesystem ownership"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 165
name = "mount"
status = "partial"
contract = "linux-mount"
handler = "kernel/src/syscall/fs/mount.rs:sys_mount"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full mount raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["MS_SYNCHRONOUS, MS_DIRSYNC, MS_LAZYTIME and MS_I_VERSION are rejected rather than applied to the ext4 superblock"]

[[contract]]
id = "linux-pivot_root"
flags = ["flag:entry-specific MS_*, OPEN_TREE_*, FSMOUNT_*, MOVE_MOUNT_* and MOUNT_ATTR_* flags"]
structs = ["struct:native x86_64 pivot_root argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve stable mount identity, enforce namespace and capability authority, and query or mutate mounted filesystem ownership"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 155
name = "pivot_root"
status = "partial"
contract = "linux-pivot_root"
handler = "kernel/src/syscall/fs/mount.rs:sys_pivot_root"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full pivot_root raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Checks mount capability before copying/resolving new_root and put_old; Linux path lookup may report EFAULT before EPERM"]

[[contract]]
id = "linux-open_tree"
flags = ["flag:entry-specific MS_*, OPEN_TREE_*, FSMOUNT_*, MOVE_MOUNT_* and MOUNT_ATTR_* flags"]
structs = ["struct:native x86_64 open_tree argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve stable mount identity, enforce namespace and capability authority, and query or mutate mounted filesystem ownership"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 428
name = "open_tree"
status = "partial"
contract = "linux-open_tree"
handler = "kernel/src/syscall/fs/mount.rs:sys_open_tree"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full open_tree raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["OPEN_TREE_NAMESPACE is not accepted"]

[[contract]]
id = "linux-move_mount"
flags = ["flag:entry-specific MS_*, OPEN_TREE_*, FSMOUNT_*, MOVE_MOUNT_* and MOUNT_ATTR_* flags"]
structs = ["struct:native x86_64 move_mount argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve stable mount identity, enforce namespace and capability authority, and query or mutate mounted filesystem ownership"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 429
name = "move_mount"
status = "implemented"
contract = "linux-move_mount"
handler = "kernel/src/syscall/fs/mount.rs:sys_move_mount"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full move_mount raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-fsopen"
flags = ["flag:entry-specific MS_*, OPEN_TREE_*, FSMOUNT_*, MOVE_MOUNT_* and MOUNT_ATTR_* flags"]
structs = ["struct:native x86_64 fsopen argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve stable mount identity, enforce namespace and capability authority, and query or mutate mounted filesystem ownership"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 430
name = "fsopen"
status = "implemented"
contract = "linux-fsopen"
handler = "kernel/src/syscall/fs/mount.rs:sys_fsopen"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full fsopen raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-fsconfig"
flags = ["flag:entry-specific MS_*, OPEN_TREE_*, FSMOUNT_*, MOVE_MOUNT_* and MOUNT_ATTR_* flags"]
structs = ["struct:native x86_64 fsconfig argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve stable mount identity, enforce namespace and capability authority, and query or mutate mounted filesystem ownership"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 431
name = "fsconfig"
status = "partial"
contract = "linux-fsconfig"
handler = "kernel/src/syscall/fs/mount.rs:sys_fsconfig"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full fsconfig raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["SET_BINARY and non-overlay SET_PATH parameters are stored without provider consumption; CREATE_EXCL is treated as CREATE"]

[[contract]]
id = "linux-fsmount"
flags = ["flag:entry-specific MS_*, OPEN_TREE_*, FSMOUNT_*, MOVE_MOUNT_* and MOUNT_ATTR_* flags"]
structs = ["struct:native x86_64 fsmount argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve stable mount identity, enforce namespace and capability authority, and query or mutate mounted filesystem ownership"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 432
name = "fsmount"
status = "partial"
contract = "linux-fsmount"
handler = "kernel/src/syscall/fs/mount.rs:sys_fsmount"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full fsmount raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["FSMOUNT_NAMESPACE is not accepted; provider parameter consumption also inherits fsconfig limitations"]

[[contract]]
id = "linux-fspick"
flags = ["flag:entry-specific MS_*, OPEN_TREE_*, FSMOUNT_*, MOVE_MOUNT_* and MOUNT_ATTR_* flags"]
structs = ["struct:native x86_64 fspick argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve stable mount identity, enforce namespace and capability authority, and query or mutate mounted filesystem ownership"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 433
name = "fspick"
status = "partial"
contract = "linux-fspick"
handler = "kernel/src/syscall/fs/mount.rs:sys_fspick"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full fspick raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Accepts an internal directory rather than requiring the mount root as Linux does"]

[[contract]]
id = "linux-statmount"
flags = ["flag:entry-specific MS_*, OPEN_TREE_*, FSMOUNT_*, MOVE_MOUNT_* and MOUNT_ATTR_* flags"]
structs = ["struct:native x86_64 statmount argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve stable mount identity, enforce namespace and capability authority, and query or mutate mounted filesystem ownership"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 457
name = "statmount"
status = "partial"
contract = "linux-statmount"
handler = "kernel/src/syscall/fs/mount.rs:sys_statmount"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full statmount raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["STATMOUNT_BY_FD and mnt_fd/mnt_ns_fd request semantics are missing"]

[[contract]]
id = "linux-listmount"
flags = ["flag:entry-specific MS_*, OPEN_TREE_*, FSMOUNT_*, MOVE_MOUNT_* and MOUNT_ATTR_* flags"]
structs = ["struct:native x86_64 listmount argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve stable mount identity, enforce namespace and capability authority, and query or mutate mounted filesystem ownership"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 458
name = "listmount"
status = "implemented"
contract = "linux-listmount"
handler = "kernel/src/syscall/fs/mount.rs:sys_listmount"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full listmount raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-mount_setattr"
flags = ["flag:entry-specific MS_*, OPEN_TREE_*, FSMOUNT_*, MOVE_MOUNT_* and MOUNT_ATTR_* flags"]
structs = ["struct:native x86_64 mount_setattr argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve stable mount identity, enforce namespace and capability authority, and query or mutate mounted filesystem ownership"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 442
name = "mount_setattr"
status = "implemented"
contract = "linux-mount_setattr"
handler = "kernel/src/syscall/fs/mount.rs:sys_mount_setattr"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full mount_setattr raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-sched_yield"
flags = ["flag:entry-specific scheduler policy, priority, clock, affinity or I/O permission arguments"]
structs = ["struct:native x86_64 sched_yield argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve task identity and credentials, validate native scheduling or I/O privilege arguments, and apply/query task-owned state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 24
name = "sched_yield"
status = "implemented"
contract = "linux-sched_yield"
handler = "kernel/src/syscall/task/schedule.rs:sys_sched_yield"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full sched_yield raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-nanosleep"
flags = ["flag:entry-specific scheduler policy, priority, clock, affinity or I/O permission arguments"]
structs = ["struct:native x86_64 nanosleep argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve task identity and credentials, validate native scheduling or I/O privilege arguments, and apply/query task-owned state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 35
name = "nanosleep"
status = "implemented"
contract = "linux-nanosleep"
handler = "kernel/src/syscall/task/schedule.rs:sys_nanosleep"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full nanosleep raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-sched_setaffinity"
flags = ["flag:entry-specific scheduler policy, priority, clock, affinity or I/O permission arguments"]
structs = ["struct:native x86_64 sched_setaffinity argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve task identity and credentials, validate native scheduling or I/O privilege arguments, and apply/query task-owned state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 203
name = "sched_setaffinity"
status = "implemented"
contract = "linux-sched_setaffinity"
handler = "kernel/src/syscall/task/schedule.rs:sys_sched_setaffinity"
conditional = "explicit-none"
tests = ["tests/guest/portable/scheduler-basic-differential.c:main"]
validation_gaps = ["Existing source/helper coverage does not establish the full sched_setaffinity raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-sched_getaffinity"
flags = ["flag:entry-specific scheduler policy, priority, clock, affinity or I/O permission arguments"]
structs = ["struct:native x86_64 sched_getaffinity argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve task identity and credentials, validate native scheduling or I/O privilege arguments, and apply/query task-owned state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 204
name = "sched_getaffinity"
status = "implemented"
contract = "linux-sched_getaffinity"
handler = "kernel/src/syscall/task/schedule.rs:sys_sched_getaffinity"
conditional = "explicit-none"
tests = ["tests/guest/portable/scheduler-basic-differential.c:main"]
validation_gaps = ["Existing source/helper coverage does not establish the full sched_getaffinity raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-getcpu"
flags = ["flag:entry-specific scheduler policy, priority, clock, affinity or I/O permission arguments"]
structs = ["struct:native x86_64 getcpu argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve task identity and credentials, validate native scheduling or I/O privilege arguments, and apply/query task-owned state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 309
name = "getcpu"
status = "implemented"
contract = "linux-getcpu"
handler = "kernel/src/syscall/task/schedule.rs:sys_getcpu"
conditional = "explicit-none"
tests = ["tests/guest/portable/scheduler-basic-differential.c:main"]
validation_gaps = ["Existing source/helper coverage does not establish the full getcpu raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-sched_setparam"
flags = ["flag:entry-specific scheduler policy, priority, clock, affinity or I/O permission arguments"]
structs = ["struct:native x86_64 sched_setparam argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve task identity and credentials, validate native scheduling or I/O privilege arguments, and apply/query task-owned state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 142
name = "sched_setparam"
status = "implemented"
contract = "linux-sched_setparam"
handler = "kernel/src/syscall/task/schedule.rs:sys_sched_setparam"
conditional = "explicit-none"
tests = ["tests/guest/portable/scheduler-basic-differential.c:main"]
validation_gaps = ["Existing source/helper coverage does not establish the full sched_setparam raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-sched_setscheduler"
flags = ["flag:entry-specific scheduler policy, priority, clock, affinity or I/O permission arguments"]
structs = ["struct:native x86_64 sched_setscheduler argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve task identity and credentials, validate native scheduling or I/O privilege arguments, and apply/query task-owned state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 144
name = "sched_setscheduler"
status = "implemented"
contract = "linux-sched_setscheduler"
handler = "kernel/src/syscall/task/schedule.rs:sys_sched_setscheduler"
conditional = "explicit-none"
tests = ["tests/guest/portable/scheduler-basic-differential.c:main"]
validation_gaps = ["Existing source/helper coverage does not establish the full sched_setscheduler raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-sched_getparam"
flags = ["flag:entry-specific scheduler policy, priority, clock, affinity or I/O permission arguments"]
structs = ["struct:native x86_64 sched_getparam argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve task identity and credentials, validate native scheduling or I/O privilege arguments, and apply/query task-owned state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 143
name = "sched_getparam"
status = "partial"
contract = "linux-sched_getparam"
handler = "kernel/src/syscall/task/schedule.rs:sys_sched_getparam"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full sched_getparam raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Task lookup excludes unreaped zombie scheduler state and returns ESRCH instead of exposing the retained scheduler snapshot"]

[[contract]]
id = "linux-sched_getscheduler"
flags = ["flag:entry-specific scheduler policy, priority, clock, affinity or I/O permission arguments"]
structs = ["struct:native x86_64 sched_getscheduler argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve task identity and credentials, validate native scheduling or I/O privilege arguments, and apply/query task-owned state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 145
name = "sched_getscheduler"
status = "partial"
contract = "linux-sched_getscheduler"
handler = "kernel/src/syscall/task/schedule.rs:sys_sched_getscheduler"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full sched_getscheduler raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Task lookup excludes unreaped zombie scheduler state and returns ESRCH instead of exposing the retained scheduler snapshot"]

[[contract]]
id = "linux-sched_rr_get_interval"
flags = ["flag:entry-specific scheduler policy, priority, clock, affinity or I/O permission arguments"]
structs = ["struct:native x86_64 sched_rr_get_interval argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve task identity and credentials, validate native scheduling or I/O privilege arguments, and apply/query task-owned state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 148
name = "sched_rr_get_interval"
status = "partial"
contract = "linux-sched_rr_get_interval"
handler = "kernel/src/syscall/task/schedule.rs:sys_sched_rr_get_interval"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full sched_rr_get_interval raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Unreaped zombie queries return ESRCH; default SCHED_RR interval is 50ms rather than Linux default 100ms"]

[[contract]]
id = "linux-sched_get_priority_max"
flags = ["flag:entry-specific scheduler policy, priority, clock, affinity or I/O permission arguments"]
structs = ["struct:native x86_64 sched_get_priority_max argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve task identity and credentials, validate native scheduling or I/O privilege arguments, and apply/query task-owned state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 146
name = "sched_get_priority_max"
status = "implemented"
contract = "linux-sched_get_priority_max"
handler = "kernel/src/syscall/task/schedule.rs:sys_sched_get_priority_max"
conditional = "explicit-none"
tests = ["tests/guest/portable/scheduler-basic-differential.c:main"]
validation_gaps = ["Existing source/helper coverage does not establish the full sched_get_priority_max raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-sched_get_priority_min"
flags = ["flag:entry-specific scheduler policy, priority, clock, affinity or I/O permission arguments"]
structs = ["struct:native x86_64 sched_get_priority_min argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve task identity and credentials, validate native scheduling or I/O privilege arguments, and apply/query task-owned state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 147
name = "sched_get_priority_min"
status = "implemented"
contract = "linux-sched_get_priority_min"
handler = "kernel/src/syscall/task/schedule.rs:sys_sched_get_priority_min"
conditional = "explicit-none"
tests = ["tests/guest/portable/scheduler-basic-differential.c:main"]
validation_gaps = ["Existing source/helper coverage does not establish the full sched_get_priority_min raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-getpriority"
flags = ["flag:entry-specific scheduler policy, priority, clock, affinity or I/O permission arguments"]
structs = ["struct:native x86_64 getpriority argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve task identity and credentials, validate native scheduling or I/O privilege arguments, and apply/query task-owned state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 140
name = "getpriority"
status = "implemented"
contract = "linux-getpriority"
handler = "kernel/src/syscall/task/schedule.rs:sys_getpriority"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full getpriority raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-setpriority"
flags = ["flag:entry-specific scheduler policy, priority, clock, affinity or I/O permission arguments"]
structs = ["struct:native x86_64 setpriority argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve task identity and credentials, validate native scheduling or I/O privilege arguments, and apply/query task-owned state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 141
name = "setpriority"
status = "partial"
contract = "linux-setpriority"
handler = "kernel/src/syscall/task/schedule.rs:sys_setpriority"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full setpriority raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Process, process-group and user selection omit unreaped zombies; Linux can authorize and update their retained priority"]

[[contract]]
id = "linux-clock_nanosleep"
flags = ["flag:entry-specific scheduler policy, priority, clock, affinity or I/O permission arguments"]
structs = ["struct:native x86_64 clock_nanosleep argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve task identity and credentials, validate native scheduling or I/O privilege arguments, and apply/query task-owned state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 230
name = "clock_nanosleep"
status = "partial"
contract = "linux-clock_nanosleep"
handler = "kernel/src/syscall/task/schedule.rs:sys_clock_nanosleep"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full clock_nanosleep raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["CLOCK_TAI is missing; THREAD_CPUTIME_ID/RAW/COARSE unsupported sleep clocks have incorrect admission or errno; REALTIME_ALARM/BOOTTIME_ALARM and CAP_WAKE_ALARM admission are missing"]

[[contract]]
id = "linux-ioprio_set"
flags = ["flag:entry-specific scheduler policy, priority, clock, affinity or I/O permission arguments"]
structs = ["struct:native x86_64 ioprio_set argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve task identity and credentials, validate native scheduling or I/O privilege arguments, and apply/query task-owned state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 251
name = "ioprio_set"
status = "partial"
contract = "linux-ioprio_set"
handler = "kernel/src/syscall/task/schedule.rs:sys_ioprio_set"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full ioprio_set raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Task selection excludes PF_EXITING before zombie publication; Linux can still locate and update that task in this lifecycle window"]

[[contract]]
id = "linux-ioprio_get"
flags = ["flag:entry-specific scheduler policy, priority, clock, affinity or I/O permission arguments"]
structs = ["struct:native x86_64 ioprio_get argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve task identity and credentials, validate native scheduling or I/O privilege arguments, and apply/query task-owned state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 252
name = "ioprio_get"
status = "partial"
contract = "linux-ioprio_get"
handler = "kernel/src/syscall/task/schedule.rs:sys_ioprio_get"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full ioprio_get raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Task selection excludes PF_EXITING before zombie publication; Linux can still locate and read that task in this lifecycle window"]

[[contract]]
id = "linux-sched_setattr"
flags = ["flag:entry-specific scheduler policy, priority, clock, affinity or I/O permission arguments"]
structs = ["struct:native x86_64 sched_setattr argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve task identity and credentials, validate native scheduling or I/O privilege arguments, and apply/query task-owned state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 314
name = "sched_setattr"
status = "partial"
contract = "linux-sched_setattr"
handler = "kernel/src/syscall/task/schedule.rs:sys_sched_setattr"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full sched_setattr raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["SCHED_FLAG_RECLAIM and SCHED_FLAG_DL_OVERRUN are rejected rather than implemented for deadline scheduling"]

[[contract]]
id = "linux-sched_getattr"
flags = ["flag:entry-specific scheduler policy, priority, clock, affinity or I/O permission arguments"]
structs = ["struct:native x86_64 sched_getattr argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve task identity and credentials, validate native scheduling or I/O privilege arguments, and apply/query task-owned state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 315
name = "sched_getattr"
status = "partial"
contract = "linux-sched_getattr"
handler = "kernel/src/syscall/task/schedule.rs:sys_sched_getattr"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full sched_getattr raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Deadline RECLAIM/DL_OVERRUN state cannot be represented because the setter rejects these Linux flags"]

[[contract]]
id = "linux-ioperm"
flags = ["flag:entry-specific scheduler policy, priority, clock, affinity or I/O permission arguments"]
structs = ["struct:native x86_64 ioperm argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve task identity and credentials, validate native scheduling or I/O privilege arguments, and apply/query task-owned state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 173
name = "ioperm"
status = "implemented"
contract = "linux-ioperm"
handler = "kernel/src/syscall/task/ioport.rs:sys_ioperm"
conditional = "explicit-none"
tests = ["kernel/src/syscall/task/ioport.rs:ioperm_range_matches_linux_unsigned_long_checks"]
validation_gaps = ["Existing source/helper coverage does not establish the full ioperm raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-iopl"
flags = ["flag:entry-specific scheduler policy, priority, clock, affinity or I/O permission arguments"]
structs = ["struct:native x86_64 iopl argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:resolve task identity and credentials, validate native scheduling or I/O privilege arguments, and apply/query task-owned state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 172
name = "iopl"
status = "partial"
contract = "linux-iopl"
handler = "kernel/src/syscall/task/ioport.rs:sys_iopl"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full iopl raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Every request requires CAP_SYS_RAWIO, including unchanged or lowered level; Linux only checks privilege when raising IOPL"]

[[contract]]
id = "linux-setpgid"
flags = ["flag:pid and pgid native signed identifiers"]
structs = ["struct:native x86_64 setpgid argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:query or mutate process group/session membership while enforcing parentage, session leader and controlling-terminal constraints"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 109
name = "setpgid"
status = "partial"
contract = "linux-setpgid"
handler = "kernel/src/syscall/task/job.rs:sys_setpgid"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full setpgid raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Does not reject changing a child process group after that child execs; Linux requires EACCES"]

[[contract]]
id = "linux-getpgrp"
flags = ["flag:pid and pgid native signed identifiers"]
structs = ["struct:native x86_64 getpgrp argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:query or mutate process group/session membership while enforcing parentage, session leader and controlling-terminal constraints"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 111
name = "getpgrp"
status = "implemented"
contract = "linux-getpgrp"
handler = "kernel/src/syscall/dispatch.rs:compat_getpgrp"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full getpgrp raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-setsid"
flags = ["flag:pid and pgid native signed identifiers"]
structs = ["struct:native x86_64 setsid argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:query or mutate process group/session membership while enforcing parentage, session leader and controlling-terminal constraints"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 112
name = "setsid"
status = "implemented"
contract = "linux-setsid"
handler = "kernel/src/syscall/task/job.rs:sys_setsid"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full setsid raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-getpgid"
flags = ["flag:pid and pgid native signed identifiers"]
structs = ["struct:native x86_64 getpgid argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:query or mutate process group/session membership while enforcing parentage, session leader and controlling-terminal constraints"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 121
name = "getpgid"
status = "implemented"
contract = "linux-getpgid"
handler = "kernel/src/syscall/task/job.rs:sys_getpgid"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full getpgid raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-getsid"
flags = ["flag:pid and pgid native signed identifiers"]
structs = ["struct:native x86_64 getsid argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:query or mutate process group/session membership while enforcing parentage, session leader and controlling-terminal constraints"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 124
name = "getsid"
status = "implemented"
contract = "linux-getsid"
handler = "kernel/src/syscall/task/job.rs:sys_getsid"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full getsid raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-setreuid"
flags = ["flag:unchanged ID sentinels and capability ABI versions"]
structs = ["struct:native x86_64 setreuid argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:snapshot and update real/effective/saved credentials or capability sets under namespace-relative authority"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 113
name = "setreuid"
status = "implemented"
contract = "linux-setreuid"
handler = "kernel/src/syscall/task/ctl.rs:sys_setreuid"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full setreuid raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-setregid"
flags = ["flag:unchanged ID sentinels and capability ABI versions"]
structs = ["struct:native x86_64 setregid argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:snapshot and update real/effective/saved credentials or capability sets under namespace-relative authority"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 114
name = "setregid"
status = "implemented"
contract = "linux-setregid"
handler = "kernel/src/syscall/task/ctl.rs:sys_setregid"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full setregid raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-setresuid"
flags = ["flag:unchanged ID sentinels and capability ABI versions"]
structs = ["struct:native x86_64 setresuid argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:snapshot and update real/effective/saved credentials or capability sets under namespace-relative authority"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 117
name = "setresuid"
status = "implemented"
contract = "linux-setresuid"
handler = "kernel/src/syscall/task/ctl.rs:sys_setresuid"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full setresuid raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-setresgid"
flags = ["flag:unchanged ID sentinels and capability ABI versions"]
structs = ["struct:native x86_64 setresgid argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:snapshot and update real/effective/saved credentials or capability sets under namespace-relative authority"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 119
name = "setresgid"
status = "implemented"
contract = "linux-setresgid"
handler = "kernel/src/syscall/task/ctl.rs:sys_setresgid"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full setresgid raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-capget"
flags = ["flag:unchanged ID sentinels and capability ABI versions"]
structs = ["struct:native x86_64 capget argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:snapshot and update real/effective/saved credentials or capability sets under namespace-relative authority"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 125
name = "capget"
status = "implemented"
contract = "linux-capget"
handler = "kernel/src/syscall/task/ctl.rs:sys_capget"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full capget raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-capset"
flags = ["flag:unchanged ID sentinels and capability ABI versions"]
structs = ["struct:native x86_64 capset argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:snapshot and update real/effective/saved credentials or capability sets under namespace-relative authority"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 126
name = "capset"
status = "partial"
contract = "linux-capset"
handler = "kernel/src/syscall/task/ctl.rs:sys_capset"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full capset raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["A non-current nonexistent header pid is looked up and yields ESRCH; Linux capset rejects a non-current pid with EPERM before lookup"]

[[contract]]
id = "linux-mbind"
flags = ["flag:MPOL_* policies, nodes masks and move/migrate flags"]
structs = ["struct:native x86_64 mbind argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage memory-policy ranges and report or migrate pages within the actual single-node topology"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 237
name = "mbind"
status = "partial"
contract = "linux-mbind"
handler = "kernel/src/syscall/task/ctl.rs:sys_mbind"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full mbind raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Unaligned address is rounded down rather than rejected; zero length bypasses mode/flag validation; mutually exclusive STATIC_NODES/RELATIVE_NODES and weighted interleave admission are incomplete"]

[[contract]]
id = "linux-set_mempolicy"
flags = ["flag:MPOL_* policies, nodes masks and move/migrate flags"]
structs = ["struct:native x86_64 set_mempolicy argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage memory-policy ranges and report or migrate pages within the actual single-node topology"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 238
name = "set_mempolicy"
status = "partial"
contract = "linux-set_mempolicy"
handler = "kernel/src/syscall/task/ctl.rs:sys_set_mempolicy"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full set_mempolicy raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["STATIC_NODES|RELATIVE_NODES is accepted instead of EINVAL; NUMA_BALANCING excludes PREFERRED_MANY; MPOL_WEIGHTED_INTERLEAVE is missing"]

[[contract]]
id = "linux-get_mempolicy"
flags = ["flag:MPOL_* policies, nodes masks and move/migrate flags"]
structs = ["struct:native x86_64 get_mempolicy argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage memory-policy ranges and report or migrate pages within the actual single-node topology"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 239
name = "get_mempolicy"
status = "partial"
contract = "linux-get_mempolicy"
handler = "kernel/src/syscall/task/ctl.rs:sys_get_mempolicy"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full get_mempolicy raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["MPOL_F_NODE returns zero for non-interleave policy rather than EINVAL; non-NULL nodemask with maxnode below nr_node_ids is not rejected"]

[[contract]]
id = "linux-migrate_pages"
flags = ["flag:MPOL_* policies, nodes masks and move/migrate flags"]
structs = ["struct:native x86_64 migrate_pages argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage memory-policy ranges and report or migrate pages within the actual single-node topology"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 256
name = "migrate_pages"
status = "implemented"
contract = "linux-migrate_pages"
handler = "kernel/src/syscall/task/ctl.rs:sys_migrate_pages"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full migrate_pages raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-move_pages"
flags = ["flag:MPOL_* policies, nodes masks and move/migrate flags"]
structs = ["struct:native x86_64 move_pages argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage memory-policy ranges and report or migrate pages within the actual single-node topology"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 279
name = "move_pages"
status = "partial"
contract = "linux-move_pages"
handler = "kernel/src/syscall/task/ctl.rs:sys_move_pages"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full move_pages raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Zero count bypasses target pid and access checks; Linux resolves and authorizes the target before returning zero"]

[[contract]]
id = "linux-set_mempolicy_home_node"
flags = ["flag:MPOL_* policies, nodes masks and move/migrate flags"]
structs = ["struct:native x86_64 set_mempolicy_home_node argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage memory-policy ranges and report or migrate pages within the actual single-node topology"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 450
name = "set_mempolicy_home_node"
status = "implemented"
contract = "linux-set_mempolicy_home_node"
handler = "kernel/src/syscall/task/ctl.rs:sys_set_mempolicy_home_node"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full set_mempolicy_home_node raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-unshare"
flags = ["flag:CLONE_* sharing and namespace flags"]
structs = ["struct:native x86_64 unshare argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:prepare independent namespace and resource owners before committing task membership"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 272
name = "unshare"
status = "partial"
contract = "linux-unshare"
handler = "kernel/src/syscall/task/ctl.rs:sys_unshare"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full unshare raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Single-thread CLONE_THREAD/CLONE_SIGHAND/CLONE_VM are rejected although Linux permits their unshare no-op or implied state transition"]

[[contract]]
id = "linux-setns"
flags = ["flag:namespace type and pidfd multi-namespace flags"]
structs = ["struct:native x86_64 setns argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize namespace descriptor and atomically install namespace membership and dependent FS state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 308
name = "setns"
status = "partial"
contract = "linux-setns"
handler = "kernel/src/syscall/task/ctl.rs:sys_setns"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full setns raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Mount namespace entry lacks CAP_SYS_CHROOT and shared fs-users checks; Linux rejects these cases with EPERM and EINVAL respectively"]

[[contract]]
id = "linux-kcmp"
flags = ["flag:KCMP_* resource selector"]
structs = ["struct:native x86_64 kcmp argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize cross-process comparison and compare retained kernel resource identity"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 312
name = "kcmp"
status = "implemented"
contract = "linux-kcmp"
handler = "kernel/src/syscall/task/ctl.rs:sys_kcmp"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full kcmp raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-lsm_get_self_attr"
flags = ["flag:LSM_FLAG_SINGLE, attribute IDs and context sizing"]
structs = ["struct:native x86_64 lsm_get_self_attr argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:query registered security module IDs or dispatch current-task LSM attributes"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 459
name = "lsm_get_self_attr"
status = "partial"
contract = "linux-lsm_get_self_attr"
handler = "kernel/src/syscall/landlock.rs:sys_lsm_get_self_attr"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full lsm_get_self_attr raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Unknown but structurally valid single LSM ID returns EINVAL rather than Linux EOPNOTSUPP"]

[[contract]]
id = "linux-lsm_set_self_attr"
flags = ["flag:LSM_FLAG_SINGLE, attribute IDs and context sizing"]
structs = ["struct:native x86_64 lsm_set_self_attr argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:query registered security module IDs or dispatch current-task LSM attributes"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 460
name = "lsm_set_self_attr"
status = "partial"
contract = "linux-lsm_set_self_attr"
handler = "kernel/src/syscall/landlock.rs:sys_lsm_set_self_attr"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full lsm_set_self_attr raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Unknown but structurally valid single LSM ID returns EINVAL rather than Linux EOPNOTSUPP"]

[[contract]]
id = "linux-lsm_list_modules"
flags = ["flag:LSM_FLAG_SINGLE, attribute IDs and context sizing"]
structs = ["struct:native x86_64 lsm_list_modules argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:query registered security module IDs or dispatch current-task LSM attributes"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 461
name = "lsm_list_modules"
status = "implemented"
contract = "linux-lsm_list_modules"
handler = "kernel/src/syscall/landlock.rs:sys_lsm_list_modules"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full lsm_list_modules raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-ptrace"
flags = ["flag:PTRACE_* requests and NT_* regset selectors"]
structs = ["struct:native x86_64 ptrace argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize stable tracee identity, manage trace-stop and resume state, and transfer supported register or memory records"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 101
name = "ptrace"
status = "partial"
contract = "linux-ptrace"
handler = "kernel/src/syscall/task/ptrace.rs:sys_ptrace"
conditional = "explicit-none"
tests = ["kernel/src/task/access.rs:process_access_ptrace_enforces_dumpability_image_owner_and_commoncap"]
validation_gaps = ["Existing source/helper coverage does not establish the full ptrace raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["PTRACE_PEEKUSER, PTRACE_POKEUSER and PTRACE_LISTEN return fixed EIO; regsets expose SHSTK only and omit NT_PRSTATUS, floating-point and XSTATE records"]

[[contract]]
id = "linux-acct"
flags = ["flag:native acct command, flags and ABI argument layouts"]
structs = ["struct:native x86_64 acct argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize acct and manage its retained kernel object, provider and task lifecycle"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 163
name = "acct"
status = "partial"
contract = "linux-acct"
handler = "kernel/src/syscall/task/acct.rs:sys_acct"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full acct raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Accounting records hardcode flags, tty, memory, I/O, faults and swaps to zero; credentials are captured when accounting is enabled instead of at task exit; accounting owner is global rather than per PID namespace"]

[[contract]]
id = "linux-swapon"
flags = ["flag:native swapon command, flags and ABI argument layouts"]
structs = ["struct:native x86_64 swapon argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize swapon and manage its retained kernel object, provider and task lifecycle"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 167
name = "swapon"
status = "partial"
contract = "linux-swapon"
handler = "kernel/src/syscall/mm/swap.rs:sys_swapon"
conditional = "explicit-none"
tests = ["kernel/src/mm/swap.rs:mutation_gate_linearizes_writers_mappings_and_activation"]
validation_gaps = ["Existing source/helper coverage does not establish the full swapon raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Only regular files are admitted rather than regular files and block devices; header validation omits version, last_page, badpage bounds and logical area sizing"]

[[contract]]
id = "linux-swapoff"
flags = ["flag:native swapoff command, flags and ABI argument layouts"]
structs = ["struct:native x86_64 swapoff argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize swapoff and manage its retained kernel object, provider and task lifecycle"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 168
name = "swapoff"
status = "partial"
contract = "linux-swapoff"
handler = "kernel/src/syscall/mm/swap.rs:sys_swapoff"
conditional = "explicit-none"
tests = ["kernel/src/mm/swap.rs:mutation_gate_linearizes_writers_mappings_and_activation"]
validation_gaps = ["Existing source/helper coverage does not establish the full swapoff raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Block-device swap areas are rejected by the regular-file-only path admission"]

[[contract]]
id = "linux-init_module"
flags = ["flag:native init_module command, flags and ABI argument layouts"]
structs = ["struct:native x86_64 init_module argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize init_module and manage its retained kernel object, provider and task lifecycle"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 175
name = "init_module"
status = "partial"
contract = "linux-init_module"
handler = "kernel/src/syscall/task/module.rs:sys_init_module"
conditional = "explicit-none"
tests = ["kernel/src/syscall/task/module.rs:module_init_return_matches_linux_zero_negative_and_positive_rules"]
validation_gaps = ["Selected Linux oracle CONFIG_MODULES=n returns ENOSYS; TheKernel has a loader, so matching feature configuration and module fixtures are required before runtime comparison"]
limitations = ["Parameter strings are copied before the module image rather than after image validation; MODULE_RELEASE remains 6.12.103 and vermagic validation only compares its first token"]

[[contract]]
id = "linux-finit_module"
flags = ["flag:native finit_module command, flags and ABI argument layouts"]
structs = ["struct:native x86_64 finit_module argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize finit_module and manage its retained kernel object, provider and task lifecycle"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 313
name = "finit_module"
status = "partial"
contract = "linux-finit_module"
handler = "kernel/src/syscall/task/module.rs:sys_finit_module"
conditional = "explicit-none"
tests = ["kernel/src/syscall/task/module.rs:module_init_return_matches_linux_zero_negative_and_positive_rules"]
validation_gaps = ["Selected Linux oracle CONFIG_MODULES=n returns ENOSYS; TheKernel has a loader, so matching feature configuration and module fixtures are required before runtime comparison"]
limitations = ["Parameter string copying precedes invalid-fd rejection; module identity uses stale 6.12.103 release and incomplete vermagic matching"]

[[contract]]
id = "linux-delete_module"
flags = ["flag:native delete_module command, flags and ABI argument layouts"]
structs = ["struct:native x86_64 delete_module argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize delete_module and manage its retained kernel object, provider and task lifecycle"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 176
name = "delete_module"
status = "partial"
contract = "linux-delete_module"
handler = "kernel/src/syscall/task/module.rs:sys_delete_module"
conditional = "explicit-none"
tests = ["kernel/src/syscall/task/module.rs:module_init_return_matches_linux_zero_negative_and_positive_rules"]
validation_gaps = ["Selected Linux oracle CONFIG_MODULES=n returns ENOSYS; TheKernel has a loader, so matching feature configuration and module fixtures are required before runtime comparison"]
limitations = ["O_TRUNC always enables force-unload rather than respecting CONFIG_MODULE_FORCE_UNLOAD admission"]

[[contract]]
id = "linux-kexec_load"
flags = ["flag:native kexec_load command, flags and ABI argument layouts"]
structs = ["struct:native x86_64 kexec_load argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize kexec_load and manage its retained kernel object, provider and task lifecycle"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 246
name = "kexec_load"
status = "partial"
contract = "linux-kexec_load"
handler = "kernel/src/syscall/task/kexec.rs:sys_kexec_load"
conditional = "explicit-none"
tests = ["kernel/src/syscall/task/kexec.rs:bzimage_requires_64_bit_boot_contract_and_cmdline_capacity"]
validation_gaps = ["Existing source/helper coverage does not establish the full kexec_load raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["KEXEC_UPDATE_ELFCOREHDR and KEXEC_CRASH_HOTPLUG_SUPPORT are unconditionally rejected although the selected Linux CRASH_HOTPLUG build admits them"]

[[contract]]
id = "linux-kexec_file_load"
flags = ["flag:native kexec_file_load command, flags and ABI argument layouts"]
structs = ["struct:native x86_64 kexec_file_load argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize kexec_file_load and manage its retained kernel object, provider and task lifecycle"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 320
name = "kexec_file_load"
status = "implemented"
contract = "linux-kexec_file_load"
handler = "kernel/src/syscall/task/kexec.rs:sys_kexec_file_load"
conditional = "explicit-none"
tests = ["kernel/src/syscall/task/kexec.rs:bzimage_requires_64_bit_boot_contract_and_cmdline_capacity"]
validation_gaps = ["Selected Linux oracle CONFIG_KEXEC_FILE=n returns ENOSYS while TheKernel implements file loading; a matching enabled oracle and actual image handoff tests are required"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-add_key"
flags = ["flag:native add_key command, flags and ABI argument layouts"]
structs = ["struct:native x86_64 add_key argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize add_key and manage its retained kernel object, provider and task lifecycle"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 248
name = "add_key"
status = "partial"
contract = "linux-add_key"
handler = "kernel/src/syscall/task/keys.rs:sys_add_key"
conditional = "explicit-none"
tests = ["kernel/src/syscall/task/keys.rs:add_key_adapter_rejects_private_keyring_prefix_before_type_lookup"]
validation_gaps = ["Current oracle CONFIG_BIG_KEYS=n differs from the implemented big_key feature; no complete current paired key-type and errno matrix has executed"]
limitations = ["Asymmetric key type enabled by the Linux oracle is missing; NULL description is unconditionally copied and returns EFAULT rather than undergoing key-type preparation"]

[[contract]]
id = "linux-request_key"
flags = ["flag:native request_key command, flags and ABI argument layouts"]
structs = ["struct:native x86_64 request_key argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize request_key and manage its retained kernel object, provider and task lifecycle"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 249
name = "request_key"
status = "partial"
contract = "linux-request_key"
handler = "kernel/src/syscall/task/keys.rs:sys_request_key"
conditional = "explicit-none"
tests = []
validation_gaps = ["Existing source/helper coverage does not establish the full request_key raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["NULL callout starts construction/upcall instead of immediate ENOKEY; helper argv and session authorization key contract differ from Linux /sbin/request-key ABI"]

[[contract]]
id = "linux-keyctl"
flags = ["flag:native keyctl command, flags and ABI argument layouts"]
structs = ["struct:native x86_64 keyctl argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize keyctl and manage its retained kernel object, provider and task lifecycle"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 250
name = "keyctl"
status = "partial"
contract = "linux-keyctl"
handler = "kernel/src/syscall/task/keys.rs:sys_keyctl"
conditional = "explicit-none"
tests = ["kernel/src/syscall/task/keys.rs:keyctl_capabilities_requires_a_non_null_output_for_nonzero_size"]
validation_gaps = ["Capability bytes and persistent/big/DH features differ from oracle configuration; active asymmetric functionality is missing and the complete keyctl guest matrix remains unexecuted"]
limitations = ["Keyring READ lacks serial-word output-length alignment; typed restriction accepts unsupported user/logon restrictions; SESSION_TO_PARENT and asymmetric PKEY commands are missing; instantiate payload is limited to 4096 rather than the larger Linux type limit"]

[[contract]]
id = "linux-perf_event_open"
flags = ["flag:native perf_event_open command, flags and ABI argument layouts"]
structs = ["struct:native x86_64 perf_event_open argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize perf_event_open and manage its retained kernel object, provider and task lifecycle"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 298
name = "perf_event_open"
status = "partial"
contract = "linux-perf_event_open"
handler = "kernel/src/syscall/task/perf.rs:sys_perf_event_open"
conditional = "explicit-none"
tests = ["kernel/src/syscall/task/perf.rs:perf_clock_selection_accepts_only_the_monotonic_backend_clock"]
validation_gaps = ["Observer use exercises a subset of software/tracepoint events; complete attribute, sampling, lifecycle and accounting differential verification remains missing"]
limitations = ["Task sigtrap, namespace records, exclude_idle and PERF_SAMPLE_CGROUP are unsupported; perf ring VMA partial splits are not rejected as Linux requires, and repeated same-ring mmap charge behavior is not fully aligned"]

[[contract]]
id = "linux-umount2"
flags = ["flag:MNT_FORCE/DETACH/EXPIRE and UMOUNT_NOFOLLOW"]
structs = ["struct:native x86_64 umount2 argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize and detach mount ownership or perform expiry/remount lifecycle"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 166
name = "umount2"
status = "partial"
contract = "linux-umount2"
handler = "kernel/src/syscall/fs/mount.rs:sys_umount2"
conditional = "explicit-none"
tests = ["kernel/src/mounts.rs:first_expire_probe_records_the_epoch_and_retries"]
validation_gaps = ["Existing source/helper coverage does not establish the full umount2 raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["MNT_FORCE unconditionally returns EOPNOTSUPP; root mount unconditionally returns EBUSY instead of Linux read-only remount or detach behavior"]

[[contract]]
id = "linux-flock"
flags = ["flag:LOCK_SH/EX/NB/UN and obsolete LOCK_MAND"]
structs = ["struct:native x86_64 flock argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:apply open-file-description-owned advisory locks and release them on final close"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 73
name = "flock"
status = "implemented"
contract = "linux-flock"
handler = "kernel/src/syscall/fs/fd_ops.rs:sys_flock"
conditional = "explicit-none"
tests = ["tests/guest/portable/fs-boundary-differential.c:main"]
validation_gaps = ["Existing source/helper coverage does not establish the full flock raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-utimensat"
flags = ["flag:UTIME_NOW/OMIT and AT_* lookup flags"]
structs = ["struct:native x86_64 utimensat argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:snapshot timestamps, short-circuit dual OMIT, then authorize and commit inode timestamp changes"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 280
name = "utimensat"
status = "implemented"
contract = "linux-utimensat"
handler = "kernel/src/syscall/fs/ctl.rs:sys_utimensat"
conditional = "explicit-none"
tests = ["tests/guest/portable/fs-boundary-differential.c:main"]
validation_gaps = ["Existing source/helper coverage does not establish the full utimensat raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-fallocate"
flags = ["flag:FALLOC_FL_* operation modes"]
structs = ["struct:native x86_64 fallocate argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:authorize file allocation operation and mutate extents while preserving data and size semantics"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 285
name = "fallocate"
status = "partial"
contract = "linux-fallocate"
handler = "kernel/src/syscall/fs/io.rs:sys_fallocate"
conditional = "explicit-none"
tests = ["tests/guest/portable/fs-boundary-differential.c:main"]
validation_gaps = ["Existing source/helper coverage does not establish the full fallocate raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Ext4 UNSHARE_RANGE returns success although Linux ext4 rejects this mode; WRITE_ZEROES is not implemented where the backing device enables Linux support"]

[[contract]]
id = "linux-readahead"
flags = ["flag:signed offset and byte count"]
structs = ["struct:native x86_64 readahead argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:admit readable file provider and request anticipatory page-cache population"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 187
name = "readahead"
status = "partial"
contract = "linux-readahead"
handler = "kernel/src/syscall/fs/io.rs:sys_readahead"
conditional = "explicit-none"
tests = ["tests/guest/portable/fs-boundary-differential.c:main"]
validation_gaps = ["Existing source/helper coverage does not establish the full readahead raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Readable block-device open-file descriptions are classified as Other and rejected rather than following Linux block-device fadvise/readahead"]

[[contract]]
id = "linux-wait4"
flags = ["flag:wait selection and W* option bits"]
structs = ["struct:native x86_64 wait4 argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:select child exit/stop/continue events and return status and resource accounting"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 61
name = "wait4"
status = "partial"
contract = "linux-wait4"
handler = "kernel/src/syscall/task/wait.rs:sys_waitpid"
conditional = "explicit-none"
tests = ["tests/guest/portable/vfork-smoke.c:wait_success"]
validation_gaps = ["Existing source/helper coverage does not establish the full wait4 raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Reaping occurs after copyout rather than before it, preserving a zombie when Linux has consumed it on EFAULT; global stop/continue preference differs from per-child Linux event selection"]

[[contract]]
id = "linux-waitid"
flags = ["flag:P_* selectors, WNOWAIT and wait event flags"]
structs = ["struct:native x86_64 waitid argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:select child or pidfd events and return siginfo and resource accounting"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 247
name = "waitid"
status = "partial"
contract = "linux-waitid"
handler = "kernel/src/syscall/task/wait.rs:sys_waitid"
conditional = "explicit-none"
tests = ["tests/guest/tools/ioprio-smoke.c:test_zombie_ioprio"]
validation_gaps = ["Existing source/helper coverage does not establish the full waitid raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Copyout writes siginfo before rusage and reaps afterward, unlike Linux ordering; P_PIDFD excludes non-parent ptrace tracees"]

[[contract]]
id = "linux-modify_ldt"
flags = ["flag:function 0/1/2/0x11 and user_desc segment flags"]
structs = ["struct:native x86_64 modify_ldt argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:read or update mm-owned LDT descriptors and preserve table lifetime across sharing and fork"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 154
name = "modify_ldt"
status = "partial"
contract = "linux-modify_ldt"
handler = "kernel/src/syscall/task/thread.rs:sys_modify_ldt"
conditional = "explicit-none"
tests = ["kernel/src/mm/ldt.rs:new_and_old_clear_rules_differ"]
validation_gaps = ["Existing source/helper coverage does not establish the full modify_ldt raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["16-bit descriptors are unconditionally rejected although the Linux CONFIG_X86_16BIT oracle accepts them"]

[[contract]]
id = "linux-prctl"
flags = ["flag:PR_* command-specific arguments"]
structs = ["struct:native x86_64 prctl argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:query and mutate process controls, credentials, task names and memory/task policy"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 157
name = "prctl"
status = "partial"
contract = "linux-prctl"
handler = "kernel/src/syscall/task/ctl.rs:sys_prctl"
conditional = "explicit-none"
tests = ["tests/guest/portable/mm-contracts-differential.c:main"]
validation_gaps = ["Existing source/helper coverage does not establish the full prctl raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["PR_SET_NAME reads beyond the Linux 15-byte bound and rejects non-UTF8 names; IO_FLUSHER clear/get omit CAP_SYS_RESOURCE; TIMING, TSC, task perf enable/disable, speculation control, syscall user dispatch, AUXV, timer restore and futex hash controls are missing; disabled RSEQ_SLICE_EXTENSION returns EINVAL rather than ENOTSUPP"]

[[contract]]
id = "linux-arch_prctl"
flags = ["flag:ARCH_* commands"]
structs = ["struct:native x86_64 arch_prctl argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:manage x86 thread FS/GS bases and exposed processor extension state"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 158
name = "arch_prctl"
status = "partial"
contract = "linux-arch_prctl"
handler = "kernel/src/syscall/task/thread.rs:sys_arch_prctl"
conditional = "explicit-none"
tests = ["tests/guest/tools/cpu-smoke.c:cet_status"]
validation_gaps = ["Existing source/helper coverage does not establish the full arch_prctl raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["ARCH_SET_FS/GS accept addresses at or above TASK_SIZE_MAX instead of EPERM; XCOMP support/permission query and request commands are missing"]

[[contract]]
id = "linux-rseq"
flags = ["flag:register 0/2, unregister 1, signature and 32-byte legacy or >=33-byte extended size/alignment"]
structs = ["struct:native x86_64 rseq argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:register a per-thread restartable sequence area, update CPU identifiers and abort critical sections on scheduling/signal transitions"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 334
name = "rseq"
status = "implemented"
contract = "linux-rseq"
handler = "kernel/src/syscall/sync/rseq.rs:sys_rseq"
conditional = "explicit-none"
tests = ["tests/guest/tools/rseq-smoke.c:test_registration"]
validation_gaps = ["Existing source/helper coverage does not establish the full rseq raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["explicit-none"]

[[contract]]
id = "linux-clone3"
flags = ["flag:clone_args sizes, CLONE_* and set_tid/exit_signal fields"]
structs = ["struct:native x86_64 clone3 argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:copy versioned clone_args and publish a child with selected sharing, identity and stack ownership"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 435
name = "clone3"
status = "partial"
contract = "linux-clone3"
handler = "kernel/src/syscall/task/clone3.rs:sys_clone3"
conditional = "explicit-none"
tests = ["kernel/src/syscall/task/clone3.rs:clone3_copy_checks_extension_before_faulting_common_prefix"]
validation_gaps = ["Existing source/helper coverage does not establish the full clone3 raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["CLONE_AUTOREAP, CLONE_NNP, CLONE_PIDFD_AUTOKILL and CLONE_EMPTY_MNTNS flags and their lifecycle semantics are missing"]

[[contract]]
id = "linux-process_mrelease"
flags = ["flag:pidfd and zero flags"]
structs = ["struct:native x86_64 process_mrelease argument/result layout"]
multiplexer_commands = ["explicit-none"]
provider_ioctls = ["explicit-none"]
errno_order = ["errno:entry-specific flag, descriptor, pathname and provider admission; known ordering deviations are recorded in limitations"]
usercopy = ["usercopy:checked input and output accesses with the syscall transfer or metadata commit rules"]
state = ["state:retain target mm and reap eligible dying-process private memory without destroying mapping ownership"]
concurrency = ["concurrency:retain descriptor/provider identity and serialize namespace, cursor or context mutation as applicable"]
teardown = ["teardown:release temporary references; persistent file and context resources follow final-owner teardown"]

[[cell]]
number = 448
name = "process_mrelease"
status = "partial"
contract = "linux-process_mrelease"
handler = "kernel/src/syscall/mm/process_vm.rs:sys_process_mrelease"
conditional = "explicit-none"
tests = ["kernel/src/mm/aspace/mod.rs:oom_reap_drains_private_cow_ptes_but_keeps_vmas"]
validation_gaps = ["Existing source/helper coverage does not establish the full process_mrelease raw syscall contract in current paired Linux7.2.3 and TheKernel guest execution"]
limitations = ["Eligibility excludes a single-thread PF_EXITING task before zombie/group-exit publication although Linux permits it"]
