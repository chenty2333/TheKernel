name: Set up TheKernel
description: Provision the repository toolchain inside the shared development shell.
runs:
  using: composite
  steps:
    - name: Check development image and provision pinned toolchain
      shell: bash
      run: |
        scripts/dev-shell.sh -- bash -c 'dev-env/check-image.sh && scripts/setup-toolchain.sh'
