# Vendored: axplat-loongarch64-qemu-virt

## Upstream

- Source: `axplat-loongarch64-qemu-virt` (ArceOS workspace crate, version from workspace)
- Original manifest: `Cargo.toml.orig`

## History

| Commit | Description |
|--------|-------------|
| `c098032f` | fix: stabilize oscomp la evaluation flow |

## Changes

The current `Cargo.toml` is Cargo's auto-normalized form of `Cargo.toml.orig`.
