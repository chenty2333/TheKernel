# Vendored: kernel-elf-parser

## Upstream

- Source: `kernel-elf-parser` v0.3.4
- Original manifest: `Cargo.toml.orig`

## History

| Commit | Description |
|--------|-------------|
| `024e19d0` | fix(loader): align auxv layout for static glibc |
| `84ce9fbd` | fix: unblock rv oscomp path through ltp |

## Changes

The current `Cargo.toml` is Cargo's auto-normalized form of `Cargo.toml.orig`.
Changes relate to ELF loading and auxv layout for static glibc compatibility.
