# Vendored: axplat-riscv64-qemu-virt

## Upstream

- Source: `axplat-riscv64-qemu-virt` (ArceOS workspace crate, version from workspace)
- Original manifest: `Cargo.toml.orig`

## Changes

The current `Cargo.toml` is Cargo's auto-normalized form of `Cargo.toml.orig`.
